1	H	L9	[]	Where the judges disagree, or where the designs disagree and no judge ruled, the decision and its reason are stated.	|| 
2	P	L13	[]	A pending entry is published only at PY_RETURN or PY_YIELD, or at a PY_UNWIND whose offset is past the entry offset.	|| X135, H8 (confirmation), X119 (PY_THROW)
3	S	L14	[]	**Coherence on every visited link, F_T included.** Cases X26b, X26c and X26d cover it.	|| 
4	G	L16	[]	**Mutation-gate additions.** The G_SEM positive control (the gate must fail on v5e with v5e's frozen exam); the every-site generic operator catalog, where a mutant that compiles byte-identical (TCE) counts as equivalent by construction; and the G_FI and G_SIG companion gates with named allowed windo	|| 
5	G	L20	[]	**Named isolating witnesses.** Every SM1 catalog row is killed only by its own NAMED, isolating witness.	|| 
6	G	L20	[]	EQUIVALENT_BY_SPEC may be argued only before the freeze, and every quantitative rule gets boundary rows on both sides.	|| 
7	P	L23	[]	**Exact type before hash or compare.** Every hash or comparison in `score` and `check_metrics` is preceded by an exact-type test.	|| X96-X108, X103b, X103c, X96d (exact type before hash; code->case table)
8	H	L24	[]	D2's rule that a staticmethod or classmethod is unwrapped at *every* path step is taken so that the remedy works for class-held caches.	|| 
9	C	L24	[]	Probe p_syn1.py shows that a 3.12/3.13 staticmethod's own `__dict__` has no `__wrapped__`.	|| synth/p_syn1.py
10	G	L29	[]	- SM2's differential probe compares only spec-fixed observables, listed exhaustively under SM2.	|| 
11	H	L37	[Decisions where the judges dis]	A Python-level profile hook turns every C call on a sectioned thread into a point where CPython *skips the call* when an asynchronous exception lands there.	|| 
12	H	L37	[Decisions where the judges dis]	This is the mechanism of round-4 BLOCKER hook-exception-leaves-lock-held, whose fix sketch was "no profile hook when a C lock method is called", and no 3.11 hook can meet that.	|| 
13	H	L37	[Decisions where the judges dis]	SIGNAL_TIMER covers only the detectable precondition: SIGINT, handlers installed mid-section and pthread_kill remain.	|| 
14	H	L38	[Decisions where the judges dis]	3.11 f_lasti pairing for every target (judge 2 graft 1)	|| 
15	H	L39	[Decisions where the judges dis]	A test-only fault hook (judge 2 graft 2)	|| 
16	P	L39	[Decisions where the judges dis]	"plus a test-only fault hook" | **No hook in production code.** A test branch in the hot path would itself be mutation surface.	|| G_HYG (region names are exactly the frozen set); G_COVER (a test branch is dead code)
17	G	L39	[Decisions where the judges dis]	Instead a frozen, read-only `_v5_faultpoints()` returns `{qualname: code}` for every machinery function.	|| 
18	G	L39	[Decisions where the judges dis]	This meets judge 2's aim: the exam and the SM instruments read no private name except `_v5_state()` and `_v5_faultpoints()`.	|| 
19	H	L40	[Decisions where the judges dis]	EXAM_HOLE = 0 attainable only with a normalizer restricted to spec-fixed observables; mask N ≥ 5	|| 
20	G	L40	[Decisions where the judges dis]	An exam frozen before the implementation can only assert what the spec fixes, so SM2 compares only spec-fixed observables.	|| 
21	H	L46	[Decisions where the designs di]	NOT_A_FUNCTION stays about the *kind* of object, and no new code is needed.	|| 
22	H	L50	[Decisions where the designs di]	Refusing would refuse every dataclass `__init__` (V05) for a deliberate-only residual.	|| 
23	H	L51	[Decisions where the designs di]	With no per-thread hook there is no post-hop blind spot to report.	|| 
24	P	L54	[Decisions where the designs di]	`_ensure_tool` adopts only an id that carries *this copy's* name.	|| X142, X142b
25	H	L55	[Decisions where the designs di]	With no hook to install, it served only the 3.11 adapter and diagnostic parity.	|| 
26	P	L55	[Decisions where the designs di]	Uncredited diagnostics are now counted on every thread.	|| X123 (uncredited on two threads)
27	P	L57	[Decisions where the designs di]	Scoring-only users do not pay for the import, and no *machinery* import runs inside enter or exit.	|| G_HYG import clause
28	H	L65	[Verified in scratch, and what ]	- t_crash_sweep: an exception at every machinery bytecode.	|| 
29	C	L91	[Verified in scratch, and what ]	A staticmethod's own `__dict__` has no `__wrapped__`.	|| synth/p_syn1.py
30	C	L92	[Verified in scratch, and what ]	An exception raised in a PY_UNWIND callback *replaces* the exception being unwound, and its `__context__` is None.	|| synth/p_deliver.py
31	C	L93	[Verified in scratch, and what ]	It is confirmed by PY_UNWIND at the return offset, and no pending entry is left.	|| the Verified probe named with it
32	H	L103	[Verified in scratch, and what ]	- `p8_cdispatch` and `p9_uvloop` (uvloop 0.22.1 on 3.12.3): dispatch with no cut frame.	|| 
33	H	L121	[Verified in scratch, and what ]	`mech3.py` also has a test hook at every statement boundary of the toggle protocol, which the probes use to force a thread switch or raise a fault there:	|| 
34	C	L122	[Verified in scratch, and what ]	- `q1_test_then_call_atomic`: no thread switch separates a dict-emptiness test from the C call that follows it.	|| rev3 q1_test_then_call_atomic (superseded by G_ATOM)
35	H	L124	[Verified in scratch, and what ]	- `m1_unwind_modelcheck`: every interleaving of two sections and a retiring tracer, with up to two faults, in an abstract step model.	|| 
36	H	L130	[Verified in scratch, and what ]	- `m1_pairs_modelcheck`: every interleaving, at the granularity of single C operations, of every pair of concurrent transitions (open, close, exit of the same tracer and of another, a second exit, a reconciliation that prunes), plus four triples, with up to two faults; revision 3, revision 3 with it	|| 
37	H	L136	[Verified in scratch, and what ]	- `h4_callee_backedge`: a #130279 back-edge in a callee with no `try`, called from a `try` body;	|| 
38	H	L141	[Verified in scratch, and what ]	It carries the #130279 lint and a fault at every instruction of `_run`'s try-body path and `finally`;	|| 
39	H	L143	[Verified in scratch, and what ]	- `w5_witnesses`: every revision-5 witness under the spec and each single-rule mutant, including the new instruction-sweep witnesses, and the fifth critic's shapes re-run;	|| 
40	H	L147	[Verified in scratch, and what ]	Revision 6 corrects two of these readings: `t1_onecall_atomic` part C probed only `_unwind_off` and `_unwind_on` for audit events, and `register_callback` raises one inside `_register` (the sixth critic's B1); part D's control showed 0 violations, so part D is not evidence (M5).	|| 
41	H	L153	[Verified in scratch, and what ]	`p_first_reconcile`: the first reconciliation's `get_tool(None)` (F20);	|| 
42	H	L156	[Verified in scratch, and what ]	They run against `rev7/mech7.py`, which is `rev6/mech6.py` copied with revision 7's rules behind single-rule mutants (the repaired callback counted, a same-id re-take counted, the binding check at every `coverage_trace()`, the deque check, the version read at call time) and with mint joining, which 	|| 
43	H	L157	[Verified in scratch, and what ]	- `w7_witnesses`: X158, X154b one subprocess per trial, X154d, X157b, X157c, X137g with Q declaring f and g, X156c, X156d, X37b after import, and a reload with two live traces; every trial in its own subprocess;	|| 
44	D	L169	[Verified in scratch, and what ]	**Not verified: the v5f combination as a whole.** No prototype yet combines D1's machine with confirmation, coherence on every link, the freeze clause, `_v5_state()`/`_v5_faultpoints()`, the callback re-registration check in MONITOR_LOST, the deletion of `_TH`, and uncredited counting on every threa	|| G_FI, G_SIG, G_XVER and the exam on the implementation (the sentence names them)
45	G	L169	[Verified in scratch, and what ]	Every D1 measurement above was taken with publish-at-PY_START and must be re-run on the v5f implementation (G_FI, G_SIG, G_XVER).	|| 
46	S	L177	[]	A frame now *counts* only once confirmed (Attribution).	|| 
47	S	L178	[]	No type test is a tuple membership: `in` compares with `==`, which runs a metaclass `__eq__` (revision 1).	|| 
48	old	L178	[]	Own dicts are read only through C descriptors; a Python-level `__dict__` descriptor stops the step or walk and is never called. staticmethod and classmethod are unwrapped at every step.	|| revision 7 audit
49	S	L178	[]	Only the import and the PEP 562 calls have an `except Exception` (chained UNRESOLVED); nothing else has a handler.	|| 
50	old	L180	[]	Every FunctionType link the walk visits, F_T included, must be *coherent*: its `co_filename` starts with `'<'`, or its realpath/normcase equals that of its own globals' `__file__`.	|| revision 7 audit
51	S	L181	[]	Accepted only when the declared *objects* are identical.	|| 
52	S	L182	[]	Entry CODE_SWAPPED fires only against a mint that still has a live holder (reconciliation runs first).	|| 
53	S	L183	[]	CLONE_CALLED and CODE_SWAPPED are checked for *every* holder.	|| 
54	S	L183	[]	New tripwire: CUT_MOVED, checked at every entry event of minted code and at exit, on both watched bindings (`Handle._run` and, from revision 2, `BaseEventLoop._run_once`).	|| 
55	S	L184	[]	Unchanged anchor rule, but evaluated at the frame's entry event and credited only on confirmation.	|| 
56	S	L184	[]	The cut is a monotone identity map of every `Handle._run` and `BaseEventLoop._run_once` code object seen (`_run_once` from revision 2).	|| 
57	S	L184	[]	An opening is visible to a hit only while the thread's running asyncio loop is the one that was running when it opened.	|| 
58	S	L185	[]	An open racing the tracer's exit refuses TRACE_INACTIVE, or runs uncredited if its anchor was stored before exit began (#20); either way it leaves no anchor behind (revision 4, B1).	|| 
59	S	L187	[]	Revision 8: the registration reads its exchanges through `itertools.tee`, so a replaced callback is released only after its count.	|| 
60	S	L187	[]	Revision 7: an exit whose registration repairs a callback that a party replaced counts it in `_LOST`, E4 counts a re-take of its own id like a rebinding, and the binding check runs at every `coverage_trace()`.	|| 
61	S	L187	[]	New: the robust mutex (`_GUARD` and `_Txn` tokens), per-tracer monotone `marks`, reconciliation, sys.monitoring tool id 4 or 3 under a per-copy tool name (local events on minted code; global PY_UNWIND only while some section is open, from revision 2), per-mint pending entries, the at-fork handler, `	|| 
62	S	L187	[]	Revision 3: the anchor commit and its event toggles move into `_commit`, the first statement of `_run`'s try body, and every clear of PY_UNWIND goes through one `_unwind_off()`.	|| 
63	S	L187	[]	Revision 4: only the opening's own `finally` releases its anchor frame, `_commit` re-tests the claim after its store, and reconciliation prunes dead cores reached from registered anchors (B1).	|| 
64	S	L187	[]	Every read-decide-write on the event, and every other write styxx makes on its tool id, is instead a *one-call step*: a lazy `itertools`/`operator` pipeline consumed by a single builtin call, which no Python code, thread, signal, finalizer, instrument or greenlet can enter.	|| 
65	S	L187	[]	The anchor test and the clear are then one step, so nothing ever waits at open (M7).	|| 
66	S	L190	[]	Runs no user `__bool__`, `get` or `__missing__`.	|| 
67	S	L190	[]	It runs no user `__eq__` or `__hash__`, except those of a str-subclass key stored in a user dict whose hash collides with a key it looks up (contrived;	|| 
68	S	L190	[]	"smoke run" is used only when the trace is absent; otherwise the note starts with the refusal's `[V5:CODE]`.	|| 
69	S	L191	[]	Tracing on the verified interpreters only: CPython 3.12.3 and 3.13.12 GIL builds (`_VERIFIED`, M0).	|| 
70	S	L191	[]	Everything else, every other 3.12 and 3.13 patch level included, refuses UNSUPPORTED_VERSION.	|| 
71	S	L191	[]	Scoring works on every version.	|| 
72	S	L194	[]	No round 1–3 blocker class is reopened.	|| 
73	S	L194	[]	R1-B4, R2-B3 and R3-D3 now close with no profile hook and no lock at all, instead of by a refusal, a hook-removal rule or an RLock.	|| 
74	S	L198	[]	New cases cover all 29 exam holes and every fixed or disclosed finding.	|| 
75	S	L198	[]	The exam reads only `_v5_state()` and `_v5_faultpoints()`.	|| 
76	P	L208	[Rule (one sentence)]	- M_T is a fresh code object the tracer created at entry with `F_T.__code__.replace()`, published only by assigning it to `F_T.__code__`, and given sys.monitoring local events.	|| G_FI C5/C9 (`__code__` restored); X33
77	P	L213	[Resolution (at `__enter__`, ev]	The grammar `module:Q.u.a.l` is unchanged: `_TARGET_RE.fullmatch`, ASCII only, no duplicates (DECL).	|| DECL cases (code->case table)
78	P	L213	[Resolution (at `__enter__`, ev]	Resolution runs no user code except the import and a module's PEP 562 `__getattr__`, apart from the contrived str-subclass dict-key collision noted in M11.	|| X17b, X30d, V40
79	old	L222	[Resolution (at `__enter__`, ev]	The result must be an exact dict; otherwise `_own_dict` returns None.	|| revision 7 audit
80	P	L226	[Resolution (at `__enter__`, ev]	Every type test in the machinery is an `is` chain, or `issubclass(type(x), T)` with a builtin T.	|| V40, V40b, X17b; G_HYG tuple-membership clause
81	H	L226	[Resolution (at `__enter__`, ev]	`issubclass` ran no user hook).	|| 
82	P	L227	[Resolution (at `__enter__`, ev]	- **`issubclass(type(obj), ModuleType)`.** Allowed only as the first step; a module reached later refuses **INSTANCE_PATH**.	|| INHERITED/INSTANCE_PATH cases (code->case table)
83	P	L227	[Resolution (at `__enter__`, ev]	With no `__getattr__`, refuse **UNRESOLVED** with a message naming unimported submodules.	|| UNRESOLVED cases
84	P	L228	[Resolution (at `__enter__`, ev]	If `part` is absent, search every class in `_TYPE_MRO.__get__(obj)[1:]`.	|| INHERITED/INSTANCE_PATH cases (code->case table)
85	P	L229	[Resolution (at `__enter__`, ev]	If `d` is None or `part not in d`, refuse **INSTANCE_PATH**, naming the class to declare.	|| INHERITED/INSTANCE_PATH cases (code->case table)
86	P	L232	[Resolution (at `__enter__`, ev]	Nothing is unwrapped: a `functools.wraps` wrapper *is* the target.	|| X26d, V11c, V38 (a wraps wrapper is the target)
87	P	L234	[Resolution (at `__enter__`, ev]	None if there is not exactly one.	|| X24b, X24c
88	P	L236	[Resolution (at `__enter__`, ev]	- If `callee is None or callee is not stamped`, refuse **NOT_A_FUNCTION**: "the wrapper calls `<callee module:qualname>`; its `__wrapped__` names `<stamped>`; declare the function the wrapper calls".	|| X24b, X24c
89	H	L237	[Resolution (at `__enter__`, ev]	The holder namespace is the class dict or instance dict that held D; it is None for a PEP 562 product.	|| 
90	P	L238	[Resolution (at `__enter__`, ev]	- If another `_CACHE_WRAPPER` value `w` of the module dict or the holder has `_cache_callee(w) is callee`, refuse **NOT_A_FUNCTION**: "two cache wrappers call one body; declare `'<module>:<name>.__wrapped__'` to count every execution of the body".	|| X24d, X24e
91	H	L243	[Resolution (at `__enter__`, ev]	The message has two forms (revision 2, N2): with no exact-str `__file__`, "the declared module has no source file to tie its code to"; with a `.pyc`/`.pyo` `__file__`, "the declared module was loaded from compiled code `<__file__>`, not from source (a zip archive holding a `.pyc` does this); import 	|| 
92	P	L247	[Resolution (at `__enter__`, ev]	A None own dict stops the walk.	|| FOREIGN_DEFINITION cases X26b-X26f
93	P	L249	[Resolution (at `__enter__`, ev]	- **(d) No accept.** If the walk ends without accepting, refuse FOREIGN_DEFINITION.	|| FOREIGN_DEFINITION cases X26b-X26f
94	H	L249	[Resolution (at `__enter__`, ev]	A function exec'd into a fresh namespace has no `'__name__'`, and concatenating None raised TypeError out of `__enter__` (`rev2/p9_foreign_msg.py`).	|| 
95	P	L249	[Resolution (at `__enter__`, ev]	A str subclass also takes the fallback, so no user `__add__` runs.	|| X96d, X142b (str subclasses: no user method runs)
96	P	L251	[Resolution (at `__enter__`, ev]	Aliases share one mint, and every credited hit credits all of their names.	|| X24d (alias-by-object rule), V-alias rows
97	P	L252	[Resolution (at `__enter__`, ev]	**Resolve everything, then touch anything.** If any target refuses, nothing is minted (X33).	|| X33
98	P	L257	[Minting (the function `_mint`;]	- **An existing mint.** If `m = _BY_FN.get(F_T)` exists, reconciliation has left it only live holders.	|| G_FI C7; model
99	P	L262	[Minting (the function `_mint`;]	The global PY_UNWIND is not set here: revision 2 sets it only while a section is open (`_commit`, M7).	|| model STALE; H10 cell (a); V67
100	P	L264	[Minting (the function `_mint`;]	- **Release.** The original is restored when the last live holder leaves, and only if `F_T.__code__ is m.code` (`_retire`).	|| CODE_SWAPPED cases; G_FI C5
101	P	L267	[Tripwires (recorded; each refu]	Every holder of the mint records it, and the call credits nothing.	|| CLONE_CALLED/CODE_SWAPPED cases (code->case table)
102	C	L272	[Tripwires (recorded; each refu]	The count also falls with no freeze or unfreeze call, when frozen objects die (`rev1/p4_freeze.py`: −11 after deleting 10 frozen objects, on 3.11–3.13), and `gc.unfreeze()` sets it to 0 (3.12.3 boots with 375 frozen objects).	|| rev1/p4_freeze.py
103	C	L272	[Tripwires (recorded; each refu]	A clone can hide from `gc.get_referrers` only while it is frozen.	|| rev1/p4_freeze.py, p5_execref.py; V36b
104	C	L272	[Tripwires (recorded; each refu]	Only a `gc.freeze()` made after the mint can freeze it, and that call raises the count by every tracked object at that moment, the clone included.	|| rev1/p4_freeze.py
105	C	L274	[Tripwires (recorded; each refu]	The exit code itself holds no other reference to M_T while it counts; the constant 2 is `m.code` plus `getrefcount`'s argument.	|| V36b (CLONE_ALIVE count)
106	P	L276	[Tripwires (recorded; each refu]	At an entry event, a false result sets `flags['CUT_MOVED']` on every holder of the mint, and exit records it.	|| V62, CUT_MOVED cases
107	C	L276	[Tripwires (recorded; each refu]	`rev1/p18_transient_rebind.py` shows the case on 3.12.3 and 3.13.12: no cut frame lies between the dispatched call and the anchor, the binding at the hit is the moved one, and the binding at exit is restored.	|| rev1/p18_transient_rebind.py
108	H	L279	[Why class A cannot reopen, and]	A frame is tied to its function only by its code object and globals.	|| 
109	P	L279	[Why class A cannot reopen, and]	M_T is in no `co_consts`, so factories, closures, decorators, per-call siblings, dataclass siblings, vendored or exec'd copies, pre-trace clones, `copy`/`deepcopy` and pickle all run *other* code objects.	|| R03-R06, R13, R22 ('Why class A' residuals)
110	old	L279	[Why class A cannot reopen, and]	A `code.replace()` copy of M_T carries no monitoring events and is never observed (R22, revision 7).	|| revision 7 audit
111	old	L281	[Why class A cannot reopen, and]	- **The pre-trace swap is closed.** F_T's own code must have been compiled from its module's file, and so must every link the walk visits.	|| revision 7 audit
112	D	L294	[Why class A cannot reopen, and]	Only a deliberate harness builds these.	|| R05b, R18 (deliberate-only residuals pinned)
113	P	L303	[Rule (one sentence)]	- at H's entry event (PY_START or PY_RESUME), O's anchor is registered and is met on H's `f_back` chain before the first frame whose `f_code` is in the cut `_CUT` (every `Handle._run` and `BaseEventLoop._run_once` code seen), the thread's running asyncio loop is `O.loop` (the *loop boundary*), and n	|| X65-family, V63, X123
114	C	L309	[Rule (one sentence)]	It is a C function and runs no user code.	|| rev1/p9_uvloop.py and the loop-boundary cases X65d, V63
115	C	L309	[Rule (one sentence)]	Every stdlib loop and uvloop does (revision 2, N9).	|| rev1/p9_uvloop.py
116	C	L310	[Rule (one sentence)]	- **uvloop.** There is no Python dispatch frame at all.	|| rev1/p9_uvloop.py
117	C	L310	[Rule (one sentence)]	In `rev1/p9_uvloop.py` (uvloop 0.22.1, 3.12.3), a `call_soon_threadsafe` job from another thread ran with the section's anchor on its chain and no `Handle._run` frame.	|| rev1/p9_uvloop.py
118	P	L320	[Rule (one sentence)]	Every re-entry into the stdlib loop's dispatch runs `BaseEventLoop._run_once`, so its code is now a cut code too.	|| X65b, X65c
119	C	L322	[Rule (one sentence)]	A loop class with its own `_run_once` that dispatches through `handle._run()` (nest_asyncio's shape) passes no `BaseEventLoop._run_once` frame, and `Handle._run` stops the walk there (`rev2/p2_shared_cut.py`).	|| rev2/p2_shared_cut.py
120	P	L337	[At open (raised before fn runs]	**Pass-through.** If `core.pid != os.getpid()`, return None, and `run` just calls fn.	|| V47; G_FI C9
121	P	L337	[At open (raised before fn runs]	This is a forked child: no section, no credit, no refusal.	|| V47; G_FI C9
122	P	L337	[At open (raised before fn runs]	The pass-through then no longer depends on that handler having run (N5).	|| G_FI fork scenario (C9)
123	P	L338	[At open (raised before fn runs]	Revision 4 (N5) states when the refusal is in the record: every TRACE_INACTIVE, from this step or from step 8, is appended to `core.problems` before it is raised.	|| X90, X91 (TRACE_INACTIVE); V52
124	P	L340	[At open (raised before fn runs]	**UNDECLARED_SECTION.** No gate declares this section.	|| X78f and its variant
125	P	L342	[At open (raised before fn runs]	`o.frame` is the `_run` frame (or the `_run_async` coroutine frame); only the opening's own `finally` releases it (step 9).	|| G_HYG o.frame clause; X146
126	P	L343	[At open (raised before fn runs]	`_unwind_on(o)` is one *one-call step* (M7), a single C call that no Python code can enter.	|| G_ATOM
127	P	L343	[At open (raised before fn runs]	It does two things only if styxx's id carries `_TOOL_NAME` and o's own anchor is still registered.	|| G_ATOM (d) on _unwind_on; K8; X146e
128	P	L343	[At open (raised before fn runs]	If the event is clear, it notes `UNWIND_LOST` on the core of every armed, registered opening.	|| X137c; G_ATOM (d) capture
129	P	L343	[At open (raised before fn runs]	Such an opening was blinded by a party outside the machinery, since no machinery clear can land under a registered anchor (M7, U1).	|| model U1; X148; K7
130	P	L343	[At open (raised before fn runs]	Because the store precedes it, and every machinery clear is one C call that tests `_ANCHORS` in the same call, no clear can land after it while this anchor is registered.	|| model U1; X148; K7
131	P	L343	[At open (raised before fn runs]	Nothing waits (M7, "The unwind scope").	|| V68
132	H	L343	[At open (raised before fn runs]	Revision 3 set the event before the store and again after it, and relied on `_unwind_off`'s test and clear having no eval-breaker check between them, which instruments break (the fourth critic's MF1).	|| 
133	P	L343	[At open (raised before fn runs]	Inside the `try`, any fault in them is followed by the `finally`'s `_detach`, which pops the anchor if it was stored and clears the event if no other anchor is left (`rev5/spec_rev5_eventpath.py`: a fault at every instruction of `_commit` and `_unwind_on` leaves no anchor and no event, both versions	|| G_FI; X143, X144
134	P	L343	[At open (raised before fn runs]	If exit's X3 or a prune popped o's anchor after step 8, `_unwind_on(o)` sets nothing (the own-anchor gate), and the body runs uncredited with the event as other sections leave it (X146e).	|| X146e
135	H	L343	[At open (raised before fn runs]	Revision 4 set the event there and left it set, with no anchor, for the whole uncredited body.	|| 
136	P	L344	[At open (raised before fn runs]	The `finally`'s `_detach` then loses the claim and repeats only idempotent cleanups.	|| X146, X146b; model END
137	P	L344	[At open (raised before fn runs]	Every detacher claims o before it pops, and `_commit` stores before it tests the claim, so either the detacher's pop follows the store and removes it, or the claim precedes the test and `_commit` detaches itself.	|| X146, X146b; model END
138	P	L344	[At open (raised before fn runs]	The `'exiting'` test also covers an opening appended after X3's snapshot, which no detacher will claim; the claim test also covers a prune of a core whose facade died, where `'exiting'` is absent (revision 4, B1;	|| X146, X146b; model END
139	P	L345	[At open (raised before fn runs]	**Release (revision 4, B1).** `_run`'s `finally` runs `_detach(o, (end, ()))` and then `o.frame = None`.	|| G_HYG finally clause
140	P	L345	[At open (raised before fn runs]	No other code assigns `o.frame` after `_open`.	|| G_HYG o.frame clause
141	H	L345	[At open (raised before fn runs]	Revision 3's `_detach` set `o.frame = None` itself.	|| 
142	old	L345	[At open (raised before fn runs]	When exit's X3 detached an appended opening before its owner's store, `_commit` then read None and registered `_ANCHORS[None] = o`, which no later `_detach` could pop: PY_UNWIND stayed set for the rest of the process and `_on_entry`'s empty-registry shortcut never applied again (the fourth critic's 	|| revision 7 audit
143	P	L351	[At a hit]	- CUT_MOVED check: if `_cut_ok()` is false, set `flags['CUT_MOVED']` on every holder.	|| V62, CUT_MOVED cases
144	P	L357	[At a hit]	- `('d', tid)` when none was found and the walk hit the cut or a loop boundary;	|| X123, X65c (dispatched)
145	P	L358	[At a hit]	- `('u', tid)` when none was found and the walk reached the root.	|| V70 (unattributed)
146	P	L359	[At a hit]	- If no anchor is registered anywhere (`_ANCHORS` is empty), store nothing and return (revision 2; witness V67, revision 7).	|| V67
147	old	L359	[At a hit]	No section is open, so no count can result, and PY_UNWIND is off, so a pending entry for a frame that later raises would never be popped.	|| revision 7 audit
148	P	L360	[At a hit]	That is the only store.	|| V67, V70
149	P	L363	[At a hit]	- At PY_UNWIND, pop it and publish only if the unwind offset differs from the entry offset.	|| X135, H8
150	old	L365	[At a hit]	- **Never counted.** A `throw()` or `close()` resumption fires only PY_THROW and has no pending entry, so it is never counted, even when a handler in the body runs.	|| revision 7 audit
151	H	L365	[At a hit]	Names are expanded only in `record()`.	|| 
152	P	L368	[At close and at exit]	- **Close.** In `_run`'s `finally`, on return and on raise: `_detach(o, (end, ()))`, then `o.frame = None` (revision 4: the opening's own `finally` alone releases the anchor frame;	|| G_HYG finally clause; X146
153	P	L370	[At close and at exit]	If `o.frame` is set, it runs the `UNWIND_LOST` test below and then `_unwind_off(o.frame)`, one call that pops the anchor and clears the event iff no anchor is left and the id carries `_TOOL_NAME` (revision 5).	|| G_ATOM _unwind_off cases; X148
154	P	L370	[At close and at exit]	Every step is idempotent.	|| model END; G_FI C5
155	P	L370	[At close and at exit]	Revision 4: `_detach` no longer assigns `o.frame`, so every detacher (the owner, the race re-check, exit's X3, a prune) pops by the anchor's own key.	|| G_HYG `_detach` clause; X146
156	P	L370	[At close and at exit]	So no instant exists, fault-free or after a fault, at which the event is set with no anchor registered, except after an outside party's change (M7, U3).	|| model STALE; X152
157	old	L371	[At close and at exit]	`get_events` never raises, even on a freed id (`rev5/out_freed_id_calls.txt`: only `set_events` and `set_local_events` raise there, both versions).	|| revision 7 audit
158	H	L371	[At close and at exit]	Revision 3 tested the anchor before its two C calls: a close racing its own trace's exit could pass the anchor test, lose the GIL, and read the event that X3's pop and `_unwind_off()` had legitimately cleared, a false MONITOR_LOST with no fault (`rev4/a4r_detach_order.py`, both versions;	|| 
159	P	L371	[At close and at exit]	So `_unwind_on` itself also notes every armed, registered opening that it finds blinded (At open, step 7;	|| X137c; G_ATOM (d)
160	P	L372	[At close and at exit]	- `run()` sets `o.lazy` only when the result's exact type is a generator, coroutine or async generator.	|| X74-X74d
161	P	L373	[At close and at exit]	- **Exit.** The claim, then the credit stop (`core.by_code = {}`), then detaching every opening still open with end `"open"` and the note OPEN_AT_EXIT, then the tripwires, then the `'exited'` commit (M5).	|| model; X146, X71c
162	P	L379	[What score credits]	- Every entry in `problems` refuses every declaring gate, whether or not the harness swallowed the exception.	|| X92c, X32 (recorded problems refuse every declaring gate)
163	C	L382	[Why class B cannot reopen thro]	The `f_back` chain holds only frames on the same thread that are executing and waiting on this call.	|| CPython frame semantics; X82, X83, V35
164	old	L384	[Why class B cannot reopen thro]	The at-fork handler excludes it: in the child it clears `_ANCHORS` and retires every mint, and the pid pass-through means a child never opens a section or credits one.	|| revision 7 audit
165	P	L386	[Why class B cannot reopen thro]	- **Asyncio, a loop already running when the section opened.** Its work can reach the section's stack only by re-entering the loop's dispatch.	|| X65b, X65d, V63
166	D	L388	[Why class B cannot reopen thro]	One route is left, and only a deliberate harness builds it (R18).	|| R18
167	D	L388	[Why class B cannot reopen thro]	A section that drains its running loop's `_ready` queue itself, calling each handle's callback directly, is the same route with no loop code at all (revision 3, N4;	|| R18
168	D	L388	[Why class B cannot reopen thro]	No library we know of does both: nest_asyncio dispatches through `Handle._run`, and uvloop exposes no re-entrant dispatch.	|| R18 (outcome pinned; the survey is not testable)
169	old	L390	[Why class B cannot reopen thro]	Confirmation adds one more condition and never removes one, so it can only lose credit (I1).	|| revision 7 audit
170	P	L399	[M0. Version gate]	`coverage_trace(exp)` raises **UNSUPPORTED_VERSION** and constructs nothing unless all three hold:	|| X37, X37b, X156
171	old	L404	[M0. Version gate]	`import styxx.protocol` never touches `sys.monitoring` or asyncio, so scoring works on every version.	|| revision 7 audit
172	H	L406	[M0. Version gate]	**Revision 6 (M5): the premise is gated per patch level.** The one-call steps rest on a property of CPython that no documentation guarantees (M7, "One-call steps").	|| 
173	G	L406	[M0. Version gate]	A patch level is added to `_VERIFIED` only by running G_ATOM on it and committing the result with the change; the list is part of the frozen spec surface, and extending it after the freeze needs a new prereg entry naming the G_ATOM run.	|| 
174	old	L406	[M0. Version gate]	A runtime self-test at import or at the first `coverage_trace()` was considered and rejected: its audit-event half needs an audit hook, and `sys.addaudithook` cannot be undone, so the self-test would leave a permanent hook in every traced process.	|| revision 7 audit
175	H	L408	[M0. Version gate]	Keying `_VERIFIED` on the build string was considered and rejected: it would refuse every rebuild of a verified version, while the premise depends on C code (the pipeline's C callables, the eval loop's eval-breaker checks, gc's scheduling) that a rebuild changes only if it patches it.	|| 
176	old	L410	[M0. Version gate]	**Revision 6 (M1), revision 7 (M3): the bound functions must be the C builtins, checked at every `coverage_trace()`.** Every `coverage_trace()` runs the check below.	|| revision 7 audit
177	P	L410	[M0. Version gate]	The first one binds `_MON` only if the check passes.	|| X156, X156b-d
178	P	L410	[M0. Version gate]	Every call raises **UNSUPPORTED_VERSION** and binds nothing if it fails.	|| X156, X156b-d
179	H	L410	[M0. Version gate]	Revision 6 ran the check only when the first `coverage_trace()` bound `_MON`.	|| 
180	P	L412	[M0. Version gate]	`_CONSUME` is a builtin method named `extend` whose `__self__` has a type that passes the type test above with the pair `('collections', 'deque')`, and whose `maxlen` is 0 (revision 7, N1: revision 6 said only "a `collections.deque`", and its prototype compared the type's name, so a Python subclass 	|| X156d
181	old	L414	[M0. Version gate]	The refusal text is "`<name>` is not the C builtin (a wrapper or subclass was installed before this coverage_trace()): styxx's one-call steps must call C functions only".	|| revision 7 audit
182	G	L414	[M0. Version gate]	Only its `[V5:UNSUPPORTED_VERSION]` prefix is spec-fixed (Exam harness rules, "How cases pass").	|| 
183	P	L414	[M0. Version gate]	No function that the check has not passed is ever called inside a one-call step: the vocabulary is re-checked at every `coverage_trace()`, a reload re-binds only the vocabulary, and a wrapper on `sys.monitoring` installed after the first binding is not in `_MON`.	|| X156c, X156d, V69; G_ATOM K1
184	P	L417	[M1. Module state]	Every piece of shared state is one of three kinds:	|| model (its state is these three kinds)
185	P	L418	[M1. Module state]	- a registry changed only by single GIL-atomic C operations;	|| model (every commit is one step); G_FI C1-C8
186	P	L420	[M1. Module state]	- a monotone map changed only by `setdefault`.	|| X34b, V61, V62 (monotone cut); model (succession chain)
187	H	L424	[M1. Module state]	_CACHE_WRAPPER = type(functools.lru_cache(None)(lambda: 0))	|| 
188	P	L427	[M1. Module state]	_ANCHORS = {}   # anchor frame -> _Opening (the key holds the frame, so no id reuse)	|| X146d (anchors keyed by the frame object)
189	H	L428	[M1. Module state]	_CUT     = {}   # id(code) -> code: every Handle._run and BaseEventLoop._run_once code seen; setdefault only	|| 
190	H	L429	[M1. Module state]	_GUARD   = {"hint": _Txn(None, 0, None)}   # head of the robust-mutex succession chain	|| 
191	P	L430	[M1. Module state]	_TOOL    = [None]                          # sys.monitoring tool id; re-validated at every enter	|| X137e, X142
192	P	L431	[M1. Module state]	_TOOL_NAME = "styxx.protocol/" + os.urandom(6).hex()   # per loaded copy of the module; the only name adopted	|| X142
193	H	L432	[M1. Module state]	_HANDLE_DICT = [None]                      # asyncio.events.Handle's class dict, read by the first coverage_trace()	|| 
194	H	L433	[M1. Module state]	_LOOP_DICT = [None]                        # asyncio.base_events.BaseEventLoop's class dict, read with it	|| 
195	P	L434	[M1. Module state]	_LOST    = []                              # a counter, read as len(_LOST); grows only by list.append, under the mutex (revision 7: a list, so _register can count in C)	|| G_HYG (`_LOST_APPEND` only in `_register`); model
196	H	L436	[M1. Module state]	_LOCAL   = PY_START | PY_RESUME | PY_RETURN | PY_YIELD       # local, on minted code only	|| 
197	P	L437	[M1. Module state]	# revision 5: the one-call vocabulary (M7), bound once at import; every name is a C callable or a C iterator	|| X156c, X156d
198	H	L443	[M1. Module state]	_PYU1, _ZERO1, _NONE1, _NAME1 = (PY_UNWIND,), (0,), (None,), (_TOOL_NAME,)	|| 
199	P	L449	[M1. Module state]	_MON     = [None]   # (get_tool, get_events, set_events, set_local_events, register_callback, use_tool_id): bound by the first coverage_trace(); checked at every one (M0)	|| X156c
200	H	L453	[M1. Module state]	`_CALLBACKS5` is bound after the three callbacks are defined; the other names precede every function.	|| 
201	H	L455	[M1. Module state]	**Revision 6 (M1).** A monkeypatch made *before* the binding was captured and then called inside every one-call step (the sixth critic's M1).	|| 
202	P	L455	[M1. Module state]	The binding now checks that every bound name is the C object it names, and refuses UNSUPPORTED_VERSION otherwise (M0).	|| X156-X156d
203	P	L455	[M1. Module state]	**Revision 7 (M3).** The check runs at every `coverage_trace()`, because a reload re-binds the vocabulary.	|| X156c
204	P	L457	[M1. Module state]	Every other name in the block is a plain binding, re-bound by a reload: the constants `_TRACER_ID`, `_CACHE_WRAPPER`, `_BUSY_SECONDS`, `_LOCAL` and `_VERIFIED`, the M1 one-call vocabulary (`_map` through `_CALLBACKS5`;	|| G_HYG reload clause
205	P	L457	[M1. Module state]	So that exit's trace and every trace live at that exit note MONITOR_LOST, including traces entered after the reload that lost nothing; traces entered after that exit do not (`rev7/w7_witnesses.py` RELOAD: two traces live across a reload both note it, and without the count only the first to exit does	|| X158e (revision 8 self-audit, W3); before it: prototype RELOAD only  [FIX W3]
206	H	L457	[M1. Module state]	Revision 3 (N11) and revision 6 said the note lands "once", on the first trace to exit; with the count it lands on every trace live then.	|| 
207	old	L457	[M1. Module state]	Notes never refuse, so the effect is a MONITOR_LOST note on those traces and nothing else.	|| revision 7 audit
208	P	L462	[M1. Module state]	- `holders` is a tuple, rebuilt only under the mutex and read lock-free by callbacks.	|| model (holders read lock-free)
209	C	L463	[M1. Module state]	Only the thread running a frame writes that frame's entry.	|| CPython: a frame runs on one thread; X123
210	H	L472	[M1. Module state]	- `lost_note`: None or the MONITOR_LOST text;	|| 
211	H	L474	[M1. Module state]	- `facade`: a `weakref.ref` with **no callback**;	|| 
212	H	L479	[M1. Module state]	`loop` is the running asyncio loop at open, or None.	|| 
213	P	L479	[M1. Module state]	`frame` is assigned by `_open` and, to None, only by the opening's own `finally` (revision 4), on whichever thread runs it (a hopped `run_async` section's runs on the thread that resumed it).	|| G_HYG o.frame clause
214	P	L479	[M1. Module state]	Revision 5: `_Opening` and `_Core` are plain classes with no `__getattribute__`, `__getattr__`, property or other Python-level descriptor.	|| G_HYG slots clause; G_ATOM slot check
215	P	L479	[M1. Module state]	Revision 6 (M4 iv) picks one form: both classes define `__slots__` as a literal tuple of exactly their fields, and define nothing else but `__init__`.	|| G_HYG slots clause; G_ATOM slot check
216	C	L479	[M1. Module state]	Every field is then a C member descriptor, which `attrgetter` reads without running Python code;	|| G_ATOM slot check (MemberDescriptorType); K2
217	H	L479	[M1. Module state]	Revision 5 required "no Python-level descriptor" in M1 and "plain slotted classes" in its weakest points, and its model slotted `_Opening` but not `_Core` (the sixth critic's M4 iv).	|| 
218	P	L480	[M1. Module state]	`_Txn` defines no `__eq__` or `__hash__`, so it hashes and compares by identity.	|| model (tokens by identity); H1
219	P	L480	[M1. Module state]	The check is exact, holds no frame, and needs no callback.	|| H1, H2 (mutex steal); exactness disclosed as a least-sure item
220	H	L480	[M1. Module state]	From revision 5 the robust mutex is the only user of `_Txn` and `_alive`; revision 4 also used them for a clearer's announcement.	|| 
221	P	L483	[M2. The robust mutex]	It serializes enter and exit transactions only.	|| V68
222	H	L487	[M2. The robust mutex]	me = _txn(); _acquire(me); out = fn(*args); _release(me); return out   # no try/finally, by design	|| 
223	H	L494	[M2. The robust mutex]	`_release(me)` is `me.succ.setdefault('next', _Txn(None, 0, None))`.	|| 
224	old	L494	[M2. The robust mutex]	The loop contains no `try`, so CPython #130279 (a signal at a loop back-edge escapes the enclosing `try` table on 3.13) cannot leave anything held.	|| revision 7 audit
225	P	L497	[M3. Reconciliation (the first ]	**Step 0, reclaim (revision 2, M3; the function `_reclaim`).** If `_TOOL[0]` is not None and `get_tool(_TOOL[0]) is None`, the id was freed and nobody owns it.	|| X137 free variant; X137e
226	P	L498	[M3. Reconciliation (the first ]	`_take(_TOOL[0])`: one call `{ if get_tool(t) is None: use_tool_id(t, _TOOL_NAME) }` (revision 5).	|| G_ATOM _take cases; X154
227	P	L498	[M3. Reconciliation (the first ]	A tool that takes the id between a separate test and the call can no longer make styxx raise ValueError out of the transaction.	|| X154 (_take sweep); K9
228	old	L500	[M3. Reconciliation (the first ]	Its owner is then another tool, which every live trace's exit notes (its registration on that id cannot be complete), or None, and the id is re-taken and counted later: by E4's fall-through in the same transaction when the reclaim was E4's (revision 7, M5;	|| revision 7 audit
229	old	L500	[M3. Reconciliation (the first ]	The id is left named, since step 1 took it, and the re-take is never counted: no later reconciliation finds the id freed.	|| revision 7 audit
230	D	L500	[M3. Reconciliation (the first ]	But a live trace that lost nothing is not told of the free (revision 7, F25;	|| X157b (F25)
231	P	L502	[M3. Reconciliation (the first ]	No owner exists at step 1, so nobody is clobbered by it.	|| X137 free variant; X154
232	P	L502	[M3. Reconciliation (the first ]	Step 0 replaces its callbacks for styxx's five events, and the reconciliation's last step clears the global events only when PY_UNWIND is among them.	|| X137 free variant; X154
233	P	L504	[M3. Reconciliation (the first ]	For every distinct mint in `_BY_FN.values()` ∪ `_MINTED.values()`, taken from `list(...)` snapshots, `keep` is its live holders.	|| G_FI C7; model
234	P	L511	[M3. Reconciliation (the first ]	For each dead holder h, `_prune(h)` detaches every opening of h with `('open', ("[V5:OPEN_AT_EXIT] pruned: the trace's own exit did not run",))`.	|| X146d; model
235	H	L511	[M3. Reconciliation (the first ]	Revision 5 deletes `_prune`'s credit stop (`h.by_code = {}`), because it has no observable effect.	|| 
236	H	L511	[M3. Reconciliation (the first ]	A dead facade leaves nothing to call `record()` on.	|| 
237	H	L511	[M3. Reconciliation (the first ]	A dead entering token without `'active'` means no section could open.	|| 
238	H	L511	[M3. Reconciliation (the first ]	No harness can observe a count added to a dead core, so the rule could have no witness, and the model check finds no property that depends on it (Revision 5 table: `prune_credit_stop` restored changes nothing).	|| 
239	P	L513	[M3. Reconciliation (the first ]	`_retire(m)` is idempotent and consistent at every prefix:	|| G_FI C5; model
240	old	L515	[M3. Reconciliation (the first ]	`set_local_events` raises no audit event (`critic6/a1_audit_monitoring.py`), so this write is one C call with its gate: an id that no longer carries this copy's name is never touched by it, not even by another tool freeing and re-taking it between a test and the write (L-MONITOR;	|| revision 7 audit
241	old	L519	[M3. Reconciliation (the first ]	Every pop now clears in the same call when it leaves no anchor, so retirement never leaves the event to clear.	|| revision 7 audit
242	P	L521	[M3. Reconciliation (the first ]	The prune above reaches only cores that still hold a registered mint.	|| model; X146d
243	old	L521	[M3. Reconciliation (the first ]	A key maps to one opening only (it is that opening's `_run` frame), so the unguarded pop cannot remove another opening's anchor.	|| revision 7 audit
244	P	L525	[M3. Reconciliation (the first ]	**Last step of every reconciliation (revision 3, MF2; revision 5 form; revision 6 guard).** If `_TOOL[0]` is not None: `_unwind_off(None)`, one call that pops nothing and clears the event iff no anchor is registered and the id carries `_TOOL_NAME`.	|| model STALE; X152; rev6/p_first_reconcile.py
245	H	L525	[M3. Reconciliation (the first ]	The process's first reconciliation, at the first enter's E4 step 1 or at an X0 after a failed first enter, runs before `_ensure_tool` has stored any id, and revision 5's unguarded pipeline then called `get_tool(None)`, which raises TypeError out of the transaction (`rev6/p_first_reconcile.py`, both 	|| 
246	P	L525	[M3. Reconciliation (the first ]	The test needs no gate of its own: `_TOOL[0]` changes only under the mutex, which the reconciler holds.	|| model (the reconciler holds the mutex)
247	P	L525	[M3. Reconciliation (the first ]	From revision 5, no machinery step and no fault leaves the event set with no anchor (M7, U3).	|| model STALE; X152
248	P	L525	[M3. Reconciliation (the first ]	This step repairs an outside party's leftovers only.	|| model REPAIR; X152
249	P	L532	[M4. `__enter__` (the facade ca]	- if `_CUT.get(id(x.__code__)) is x.__code__`, x's code is already a cut code: nothing more;	|| X35, X35b, X35c, V61 (CUT_UNAVAILABLE cases)
250	H	L533	[M4. `__enter__` (the facade ca]	The message is "`<binding>` is bound to `<qualname>`, whose code other functions share; every function with that code would stop attribution for the rest of the process.	|| 
251	H	L535	[M4. `__enter__` (the facade ca]	Before this check, a generic decorator's inner function bound as `Handle._run` put a code object into the cut that every other function with that decorator shares.	|| 
252	H	L535	[M4. `__enter__` (the facade ca]	Each of those functions then stopped attribution, in that trace and in every later trace of the process (`rev2/p2_shared_cut.py`, reproducing the second critic's A5).	|| 
253	P	L536	[M4. `__enter__` (the facade ca]	Resolve.** Resolve every target, outside the mutex.	|| X33
254	P	L536	[M4. `__enter__` (the facade ca]	The declared module's import here is the only import inside enter or exit.	|| G_HYG import clause
255	P	L540	[M4. `__enter__` (the facade ca]	Revision 5: reclaiming here, not only in step 1's reconciliation, makes a free that lands between step 1 and this step count in `_LOST` like any other free.	|| X137e
256	H	L540	[M4. `__enter__` (the facade ca]	Together with revision 4's name-gated read at close (`_detach`), that lost a section's raising calls silently: the section of another trace that opened meanwhile got no event, its close's gated read saw a foreign-looking id and noted nothing, and that trace's exit saw `_LOST` unchanged and the id na	|| 
257	H	L540	[M4. `__enter__` (the facade ca]	In `ext: enter(X) ∥ open(Y)` the model needs both removed (`ensure_nocount` alone violates no property anywhere; with `blind_read_gated` it gives LOSTNOTE;	|| 
258	P	L540	[M4. `__enter__` (the facade ca]	So the count's only observable effect on its own is a MONITOR_LOST note on a trace that lost nothing through the free, which X137e witnesses.	|| X137e
259	P	L540	[M4. `__enter__` (the facade ca]	The close's ungated read is then the only detector of a call lost while the id was freed (`rev6/m6_modelcheck.py`: `blind_read_gated` alone gives LOSTNOTE in `ext+greenlet: [exit(X), rec] ∥ open(X)`; on greenlet 3.5.6, `rev6/w6_greenlet.py`; case X137f).	|| X137f
260	P	L540	[M4. `__enter__` (the facade ca]	`_named(i)` is `get_tool(i) is _TOOL_NAME` (revision 5): an identity test, which runs no `__eq__` at all.	|| X142b; G_HYG identity clause
261	H	L540	[M4. `__enter__` (the facade ca]	Revision 4's `type(n) is str and n == _TOOL_NAME` also ran no foreign `__eq__` (N1, `critic4/a5_tool_name_eq.py`, X142b).	|| 
262	P	L541	[M4. `__enter__` (the facade ca]	- Otherwise, that is, when `_TOOL[0]` is None, or is set and is still not named after `_reclaim()` (another tool holds it; revision 6 states the fall-through, N1), for id 4 then 3:	|| X142, X137g
263	P	L545	[M4. `__enter__` (the facade ca]	- for a usable id: if `_TOOL[0]` is not None, whether `id` is the other id (a *rebinding*, revision 6) or `_TOOL[0]` itself (a *re-take*, revision 7): `_LOST.append(True)`, then `_set_local(id, m.code, _LOCAL)` for every mint in a `list(_MINTED.values())` snapshot.	|| X137g, X137h, X154d
264	P	L545	[M4. `__enter__` (the facade ca]	**The one E4 counting rule (revision 7, M5).** While `_TOOL[0]` is not None, every take that ends in a complete registration is counted once in `_LOST`, whichever step makes it: the reclaim (M3 step 3) or this line.	|| X154d, X137h, X137e
265	P	L545	[M4. `__enter__` (the facade ca]	With `_TOOL[0]` None (a first acquisition or an adoption), nothing is counted: no trace has entered, so none can be live.	|| weakened (revision 8 self-audit, W4): E4 makes no count of its own; the in-call count still appends one per None previous callback, which no live trace sees (X137h)  [FIX W4]
266	P	L545	[M4. `__enter__` (the facade ca]	A re-take happens when both of this transaction's reclaims (the reconciliation's and `_ensure_tool`'s) had their registration split with owner None, because an audit hook freed the id again.	|| X154d
267	H	L545	[M4. `__enter__` (the facade ca]	Revision 6's text counted it (it said "if `_TOOL[0]` is set"), but called every such case a rebinding.	|| 
268	old	L545	[M4. `__enter__` (the facade ca]	Its prototype counted only `t != i`, and its model never re-took the same id: three rules for one path (the seventh critic's M5 item 2).	|| revision 7 audit
269	H	L549	[M4. `__enter__` (the facade ca]	**Rebinding (and re-taking) while traces are live (revision 6, N1; revision 7).** Revision 5 rebound `_TOOL[0]` (X137b's "a second tracer takes id 3") without saying so in E4, and set no local events on the new id for mints that already existed.	|| 
270	H	L549	[M4. `__enter__` (the facade ca]	A tracer that then joined such a mint (Minting, "An existing mint") got no events on the new id for that function, and only X5's local-events test noted it.	|| 
271	P	L549	[M4. `__enter__` (the facade ca]	Revision 6 sets local events for every registered mint on the new id, so a joiner and every later hit are delivered on the new id (X137g).	|| X137g
272	P	L549	[M4. `__enter__` (the facade ca]	The count makes every trace live at the rebinding note MONITOR_LOST at its exit: its sections may have lost calls between the other tool's take and the rebinding, and a section open at the rebinding has no global PY_UNWIND on the new id until the next `_unwind_on` sets it (whose capture then notes i	|| X137g, X137h
273	P	L549	[M4. `__enter__` (the facade ca]	If both ids then deliver an event of one minted frame, the pending entry is keyed by the frame, so the second delivery finds no entry and publishes nothing: no double count.	|| X137i (revision 8 self-audit, W1); no case reached two delivering ids before  [FIX W1]
274	P	L549	[M4. `__enter__` (the facade ca]	Openers that read the old `_TOOL[0]` before the rebinding act on the old id, and their name gate makes that a no-op.	|| X154 (name gate no-op); X137b
275	H	L551	[M4. `__enter__` (the facade ca]	4. join or mint every F_T in the Minting order;	|| 
276	P	L553	[M4. `__enter__` (the facade ca]	Until then the entering token is live (E0's frame is executing), so no reconciler prunes the core.	|| model (entering token live)
277	P	L558	[M5. `__exit__` (the facade cal]	`_locked` has no `try`, so the exit token dies with it and the next acquirer steals the mutex.	|| model; H1, H2
278	P	L560	[M5. `__exit__` (the facade cal]	This completes any abandoned enter, and makes an exit after a failed enter a no-op.	|| X92c; model exit(X)+... configurations; G_FI
279	P	L561	[M5. `__exit__` (the facade cal]	If `core.marks.setdefault('exiting', me) is not me`, return: a second exit is a no-op.	|| X92c; model exit(X)+... configurations; G_FI
280	P	L568	[M5. `__exit__` (the facade cal]	In that case nothing below is tested, and the id is not touched again by this exit: no event is changed and no local event is cleared;	|| X137b, X154b
281	H	L570	[M5. `__exit__` (the facade cal]	Another live trace that had lost calls while the callback was replaced then found all five previous callbacks styxx's, `_LOST` unchanged, local events intact and no flag, and its loss was silent (the seventh critic's B1;	|| 
282	P	L570	[M5. `__exit__` (the facade cal]	Each exchange whose previous callback is not styxx's appends to `_LOST` in C before the next exchange's audit hook can run, and (revision 8, M1) before that previous callback can be released, so no finalizer of it runs first.	|| X158, X158b, X158d; G_ATOM finalizer case
283	P	L570	[M5. `__exit__` (the facade cal]	So every trace live then notes MONITOR_LOST at its own exit, whichever trace lost the calls.	|| X158, X158c, X158e
284	H	L574	[M5. `__exit__` (the facade cal]	Revision 5 gated the whole registration once, and claimed that made it one call that could overwrite only a callback on styxx's own id.	|| 
285	P	L574	[M5. `__exit__` (the facade cal]	Revision 6 gates every exchange and reads the owner after the last one: at most one exchange can land on another tool's id, and the report names the owner (X154's `_register` sweep).	|| X154b, X154c
286	P	L587	[M5. `__exit__` (the facade cal]	- *Before E4:* nothing global has changed.	|| G_FI (prefix)
287	H	L593	[M6. The event path: tool id 4 ]	def _on_entry(code, offset):                    # PY_START, PY_RESUME (local, minted code only)	|| 
288	H	L595	[M6. The event path: tool id 4 ]	if m is None or m.code is not code: return	|| 
289	P	L600	[M6. The event path: tool id 4 ]	if f.f_globals is not m.globals:            # CLONE_CALLED: credits nothing	|| CLONE_CALLED cases
290	P	L603	[M6. The event path: tool id 4 ]	if not _ANCHORS: return                     # no section open anywhere: nothing to credit, PY_UNWIND off (rev. 2)	|| V67, V70
291	P	L605	[M6. The event path: tool id 4 ]	if out: m.pend[id(f)] = (f, offset, out)   # pending: the only store	|| V67, V70
292	H	L609	[M6. The event path: tool id 4 ]	if m is None or m.code is not code: return	|| 
293	H	L610	[M6. The event path: tool id 4 ]	f = sys._getframe(1); p = m.pend.pop(id(f), None)	|| 
294	H	L611	[M6. The event path: tool id 4 ]	if p is not None and p[0] is f: _publish(code, p[2])	|| 
295	H	L613	[M6. The event path: tool id 4 ]	def _on_unwind(code, offset, exc):              # PY_UNWIND (global, set only while a section is open; rev. 2)	|| 
296	H	L615	[M6. The event path: tool id 4 ]	if m is None or m.code is not code: return	|| 
297	H	L616	[M6. The event path: tool id 4 ]	f = sys._getframe(1); p = m.pend.pop(id(f), None)	|| 
298	H	L617	[M6. The event path: tool id 4 ]	if p is not None and p[0] is f and offset != p[1]: _publish(code, p[2])	|| 
299	C	L620	[M6. The event path: tool id 4 ]	Every per-opening counter has a single writer: an opening's anchor is on one stack at a time, and entry and confirmation of one resumption run on one thread.	|| CPython (a frame runs on one thread); V28b (manual hop), X123
300	old	L621	[M6. The event path: tool id 4 ]	- **Where callbacks run.** They never run at a C call: the only events are PY_START, PY_RESUME, PY_RETURN and PY_YIELD (local) and PY_UNWIND (global, and from revision 2 set only while some section is open). styxx never enables INSTRUCTION, LINE, JUMP or BRANCH events.	|| revision 7 audit
301	old	L621	[M6. The event path: tool id 4 ]	Code run inside any sys.monitoring callback raises no events for any tool (synth/p_syn1.py), so the callbacks cannot re-enter themselves.	|| revision 7 audit
302	P	L627	[M7. `_open`, `_commit`, `_run`]	def _run(core, section, fn, args, kwargs):      # module level: its frame is the anchor, its locals hold no facade	|| X146c
303	H	L628	[M7. `_open`, `_commit`, `_run`]	o = _open(core, section, sys._getframe())   # checks and append only (At open, steps 1-6)	|| 
304	P	L629	[M7. `_open`, `_commit`, `_run`]	if o is None: return fn(*args, **kwargs)    # forked child: pass-through	|| V47
305	P	L635	[M7. `_open`, `_commit`, `_run`]	_detach(o, (end, ()))                   # no loop in the try body, nor in any function it calls	|| G_HYG loop clause; H9
306	H	L636	[M7. `_open`, `_commit`, `_run`]	o.frame = None                          # revision 4 (B1): the opening's own finally releases the anchor frame	|| 
307	P	L642	[M7. `_open`, `_commit`, `_run`]	_ANCHORS[o.frame] = o                       # the store; o.frame is set: only this opening's finally releases it	|| G_HYG o.frame clause
308	P	L647	[M7. `_open`, `_commit`, `_run`]	_unwind_on(o)                               # one call, after the store: no clear can follow while o is registered	|| model U1; K8
309	H	L666	[M7. `_open`, `_commit`, `_run`]	def _unwind_off(key):                           # _detach (key = the anchor frame), and last in every reconciliation (None)	|| 
310	H	L667	[M7. `_open`, `_commit`, `_run`]	# ONE CALL { _ANCHORS.pop(key, None); if get_tool(t) is _TOOL_NAME and not _ANCHORS: set_events(t, 0) }	|| 
311	old	L675	[M7. `_open`, `_commit`, `_run`]	fr = o.frame                                # never assigned here: every detacher pops by the anchor's own key	|| revision 7 audit
312	H	L676	[M7. `_open`, `_commit`, `_run`]	if fr is not None:	|| 
313	H	L679	[M7. `_open`, `_commit`, `_run`]	_unwind_off(fr)                         # one call: pop, then clear iff no anchor is left	|| 
314	H	L682	[M7. `_open`, `_commit`, `_run`]	# ONE CALL { if get_tool(t) is None: use_tool_id(t, _TOOL_NAME) }; the caller then tests _named(t)	|| 
315	H	L690	[M7. `_open`, `_commit`, `_run`]	# then owner = get_tool(t); returns [None] * (number of previous callbacks that were not styxx's) + [owner]	|| 
316	P	L691	[M7. `_open`, `_commit`, `_run`]	# Revision 8 (M1): the exchanges are read through _tee, whose buffer keeps every prev referenced until the pipeline	|| G_ATOM finalizer case, K14; X158d
317	P	L692	[M7. `_open`, `_commit`, `_run`]	# is released after _list returns, so a replaced callback's finalizer runs only after every count is made.	|| G_ATOM finalizer case, K14; X158d
318	H	L698	[M7. `_open`, `_commit`, `_run`]	_map(_LOST_APPEND, _map(_is_not, _compress(a, _map(_is_not, b, _CALLBACKS5)), _repeat(None))),	|| 
319	P	L706	[M7. `_open`, `_commit`, `_run`]	def _named(i): return _MON[0][0](i) is _TOOL_NAME          # revision 5: identity, so no __eq__ of any kind runs	|| X142b
320	H	L708	[M7. `_open`, `_commit`, `_run`]	def _ours(): return _TOOL[0] is not None and _named(_TOOL[0])	|| 
321	P	L710	[M7. `_open`, `_commit`, `_run`]	styxx's global event set is only ever PY_UNWIND or nothing.	|| G_HYG event-name clause; model
322	P	L710	[M7. `_open`, `_commit`, `_run`]	`set_events`, `set_local_events`, `register_callback` and `use_tool_id` are called only inside the five step functions (`_unwind_on`, `_unwind_off`, `_take`, `_register`, `_set_local`), except `_forget_in_child` in the single-threaded child (G_HYG).	|| G_HYG call-site clause
323	P	L710	[M7. `_open`, `_commit`, `_run`]	Every test of a name goes through `_named` or a step's `_is` gate (G_HYG).	|| G_HYG identity clause
324	P	L710	[M7. `_open`, `_commit`, `_run`]	So only `_unwind_on` and `_unwind_off` can write S (G_HYG).	|| G_HYG frozen index sets; K13
325	P	L710	[M7. `_open`, `_commit`, `_run`]	Revision 8 (M1): `_register` has one more statement between the two, `a, b = _tee(...)`, which builds the exchange pipeline and its two branches and reads nothing.	|| G_HYG step clause 2; G_ATOM (c) (tee reads nothing)
326	P	L712	[M7. `_open`, `_commit`, `_run`]	It calls only C callables: `operator` functions, `dict.get`, `dict.pop` and `dict.values`, `attrgetter` on `__slots__` fields, and the `sys.monitoring` functions in `_MON`, each checked to be the C builtin when it was bound (M0, revision 6).	|| G_HYG name list; X156-X156d; G_ATOM K1-K3
327	P	L712	[M7. `_open`, `_commit`, `_run`]	Building the pipeline reads no shared state.	|| G_HYG step clauses; model (mutex)
328	P	L712	[M7. `_open`, `_commit`, `_run`]	The statement reads only `_TOOL[0]` and `o.frame`, which only the mutex and the opening's own thread change.	|| G_HYG step clauses; model (mutex)
329	P	L712	[M7. `_open`, `_commit`, `_run`]	Every read and every write happens inside the one consuming call, in C.	|| G_ATOM (c), (d); G_HYG operator ban (K11, K12, K12b)
330	C	L712	[M7. `_open`, `_commit`, `_run`]	CPython runs no Python code inside a C call that makes no Python-level call and raises no audit event: no eval-breaker check runs there.	|| G_ATOM t1 A, B, E and D2
331	C	L712	[M7. `_open`, `_commit`, `_run`]	So no thread switch, signal handler, cyclic-gc finalizer (3.12 and later run gc only at eval-breaker checks, and schedule it only from `_PyObject_GC_Link`: `critic6/a6_gc_inside_step.py`, 0 of about 3,600 gc callbacks and finalizers inside a consuming call with 2,000 tracked allocations at threshold	|| G_ATOM D2, E; critic6/a6
332	C	L712	[M7. `_open`, `_commit`, `_run`]	No trace or profile function and no sys.monitoring callback fires for calls made from C.	|| G_ATOM part B
333	old	L712	[M7. `_open`, `_commit`, `_run`]	RecursionError cannot land after a step's first write: every pipeline callable is a vectorcall callable on both versions (`critic6/a4_vectorcall.py`, `a5_recursion_inside_step.py`), and a depth sweep across the recursion boundary finds only full effects or none (`critic8/c4`; frozen as G_ATOM part R	|| revision 7 audit
334	C	L712	[M7. `_open`, `_commit`, `_run`]	Of the `sys.monitoring` functions styxx calls, `get_tool`, `get_events`, `set_events`, `set_local_events` and `use_tool_id` raise no audit event;	|| G_ATOM (b); critic6/a1_audit_monitoring.py
335	D	L713	[M7. `_open`, `_commit`, `_run`]	In `_unwind_off` that can leave S set with no anchor after the pop, until the next close that leaves no anchor or the next reconciliation (U3).	|| untestable in-process without an allocation-fault injector; disclosed (N2), like id 182
336	D	L713	[M7. `_open`, `_commit`, `_run`]	This matters only when the process is out of memory;	|| untestable in-process without an allocation-fault injector; disclosed (N2), like id 182
337	H	L717	[M7. `_open`, `_commit`, `_run`]	The BRANCH row fires no event inside the one-call form on either version, so its 0 is vacuous and it is not evidence (revision 7, M1 item 5);	|| 
338	H	L717	[M7. `_open`, `_commit`, `_run`]	The two-statement control (`A.pop(key)`, then `if not A: set_events(t, 0)`) violates under every one of the five, in 5–6 / 1 / 1 / 1 / 5–6 trials.	|| 
339	C	L718	[M7. `_open`, `_commit`, `_run`]	- *What instruments see (part B).* During a one-call step, a profile function sees one `c_call`, the consumer's, and a CALL tool sees only the pipeline's constructors.	|| G_ATOM part B
340	H	L719	[M7. `_open`, `_commit`, `_run`]	- *Audit events (part C).* None from `_unwind_off` and `_unwind_on`, the only steps part C ran.	|| 
341	H	L719	[M7. `_open`, `_commit`, `_run`]	Revision 5 wrote "none, from any one-call step";	|| 
342	H	L719	[M7. `_open`, `_commit`, `_run`]	G_ATOM now probes every step in every gate outcome of its frozen case matrix (`rev6/atom6.py`; revision 7's matrix and hookup criteria: `rev7/atom7.py`).	|| 
343	old	L720	[M7. `_open`, `_commit`, `_run`]	- *Gc and signals (part D2, revision 6; count-bounded in revision 8, G_ATOM).* Revision 5's part D did not discriminate: its two-statement control also showed 0 violations (the sixth critic's M5), because its finalizer and handler registered an anchor only while `'closer'` was in the registry, which	|| revision 7 audit
344	old	L720	[M7. `_open`, `_commit`, `_run`]	A cyclic-gc finalizer and a 20 µs SIGALRM handler, each registering an anchor, at gc threshold 1, 3 s per form: the one-call step 0 violations in 8,701 / 9,162 closes (3.12.3 / 3.13.12); the Python-call control 2,303 / 3,336; revision 5's two-statement control 0 / 0, which is why part D proved nothi	|| revision 7 audit
345	H	L721	[M7. `_open`, `_commit`, `_run`]	- *Threads (part E; count-bounded in revision 8, G_ATOM).* A pure-Python profiler that yields the GIL at every C call in the close path, over 4 threads: 0 of about 71,000 body samples found the event clear.	|| 
346	P	L723	[M7. `_open`, `_commit`, `_run`]	For each one that differs (another party's callback, or None) `_LOST_APPEND` runs in C, before the pipeline pulls the next gate, so before the next exchange's audit hook can run.	|| X158b; G_ATOM split cases (audit count)
347	H	L723	[M7. `_open`, `_commit`, `_run`]	`_is_not(prev, None)` turns it into a bool, and `_LOST_APPEND` appends that bool.	|| 
348	C	L723	[M7. `_open`, `_commit`, `_run`]	The tee's buffer keeps every previous callback referenced until the pipeline is released: after `_list` returns, when both branches are dropped.	|| G_ATOM finalizer case on each listed patch level; rev8/c1_on_mech8.py
349	H	L723	[M7. `_open`, `_commit`, `_run`]	If the slot held the only reference to an outside party's replacement, its deallocation ran Python code at that point, with the slot repaired and the count not yet made: a `__del__`, a `weakref.finalize`, or a finalizer of something the callable's closure held.	|| 
350	P	L723	[M7. `_open`, `_commit`, `_run`]	Under revision 8 every such finalizer runs after the consuming call has returned, with every count made.	|| G_ATOM finalizer case (b), K14; X158d; model count_after_release
351	P	L723	[M7. `_open`, `_commit`, `_run`]	`r` is `[None] * k + [owner]`, where k is the number of such counts.	|| G_ATOM (d) expected results
352	P	L723	[M7. `_open`, `_commit`, `_run`]	Between exchange i, its comparison and count, and gate i+1, and between the last exchange and the owner read, CPython runs only C code.	|| G_ATOM finalizer case (b), K14; X158d; model count_after_release
353	H	L723	[M7. `_open`, `_commit`, `_run`]	Revision 7 said "nothing runs" there and its pipeline did not ensure it.	|| 
354	P	L723	[M7. `_open`, `_commit`, `_run`]	From revision 8, the only objects whose last reference the pipeline drops inside the call are bools and None, and it releases the previous callbacks only after the call.	|| G_ATOM finalizer case (b), K14; X158d; model count_after_release
355	P	L725	[M7. `_open`, `_commit`, `_run`]	- exchange n landed on the id as `owner` holds it: on styxx's id if `owner is _TOOL_NAME`, on an unowned id if `owner` is None (nobody's callbacks are replaced), and otherwise on the id of the tool that owns it now, whose callback for `_EVENTS5[n-1]` styxx then replaced with its own. styxx does not 	|| G_ATOM split cases (d); X154b
356	P	L726	[M7. `_open`, `_commit`, `_run`]	If some gate k fails, the gates after it and the owner read run back to back in C with nothing between them (the seventh critic's reading of the pipeline, confirmed on both versions), so the owner read sees the same non-styxx owner that failed gate k;	|| G_ATOM split cases (d); X154b
357	P	L727	[M7. `_open`, `_commit`, `_run`]	- every repair of a slot that did not hold styxx's callback is counted in `_LOST` in the same C call as the exchange that made it (revision 7).	|| X158, X158b
358	old	L727	[M7. `_open`, `_commit`, `_run`]	From revision 8, a replaced callback's finalizer cannot land there either, because it runs only after the call.	|| revision 7 audit
359	P	L727	[M7. `_open`, `_commit`, `_run`]	So no repair is ever left uncounted (`rev7/m7_modelcheck.py`: `cb2:` configurations with a fault, a greenlet switch and a re-entered exit;	|| model cb2 configurations, count_after_release; X158b
360	P	L729	[M7. `_open`, `_commit`, `_run`]	Every caller acts on `r`: E4 uses the id only if `r` is complete; the reclaim counts a complete `r`;	|| X154b, X154c
361	P	L729	[M7. `_open`, `_commit`, `_run`]	So a split is noted by the exit that made it, and by every other trace live at that moment: while another tool holds the id, no exit's registration on it is complete, and a later complete registration needs a reclaim of that id (after its owner frees it), a re-take, or a rebinding to the other id, e	|| model REGSILENT, LOSTNOTE; X154b
362	D	L729	[M7. `_open`, `_commit`, `_run`]	A split at a first acquisition or an adoption (`_TOOL[0]` None) has no live trace to note it.	|| X154c
363	D	L729	[M7. `_open`, `_commit`, `_run`]	The report there only moves E4 to the other id, and the one replaced callback of the other tool is surfaced nowhere (X154c; revision 7, N3).	|| X154c
364	H	L729	[M7. `_open`, `_commit`, `_run`]	A count at the split itself would therefore change no record.	|| 
365	H	L729	[M7. `_open`, `_commit`, `_run`]	Revision 6 has none (the model check's REGSILENT property checks exactly this, with no such count).	|| 
366	P	L729	[M7. `_open`, `_commit`, `_run`]	At a first acquisition, a rebinding or an adoption onto a fresh id, every previous callback is None, so the registration counts five.	|| X137h
367	P	L729	[M7. `_open`, `_commit`, `_run`]	No trace is live at a first acquisition or an adoption, and a rebinding counts anyway, so this changes no record.	|| X137h
368	P	L729	[M7. `_open`, `_commit`, `_run`]	The rebinding's own count stays necessary: a rebinding onto an id that still carries styxx's own callbacks from an earlier use counts nothing in-call (X137h, revision 7 form).	|| X137h
369	H	L729	[M7. `_open`, `_commit`, `_run`]	`rev6/r6_b1_register.py` runs the critic's shape at each of the five hooks on both versions: revision 5's form replaced 1–3 of the foreign tool's callbacks and reported nothing; revision 6's replaced at most one (at hooks 1, 3 and 5; at hooks 2 and 4 the foreign tool had no callback for that event),	|| 
370	H	L731	[M7. `_open`, `_commit`, `_run`]	Registration is kept to where it is needed: the first acquisition of an id, a reclaim, a rebinding, and X5's test, which is the only way to learn a callback (sys.monitoring has no getter).	|| 
371	P	L733	[M7. `_open`, `_commit`, `_run`]	Each pipeline names only the M1 one-call vocabulary, the step's locals and parameters, `o.frame`, `_ANCHORS`, `_ANCHORS.get`, `_ANCHORS.pop` and literal tuples (G_HYG gives the exact list).	|| G_HYG name list
372	C	L735	[M7. `_open`, `_commit`, `_run`]	On 3.13 it is JUMP_BACKWARD_NO_INTERRUPT, which only an injected fault reaches.	|| rev1/p19_run_async_throw_path.py
373	P	L735	[M7. `_open`, `_commit`, `_run`]	No count, note, refusal or termination depends on that `finally`.	|| X140, H9; G_FI
374	P	L735	[M7. `_open`, `_commit`, `_run`]	The opening's anchor frame is dead, so it is on no live chain, and exit's X3 or a prune finalises the opening as `('open', OPEN_AT_EXIT)`, inside I6.	|| X140; model END
375	D	L735	[M7. `_open`, `_commit`, `_run`]	Until then the dead anchor keeps PY_UNWIND set, which is L-DELIVERY scope only (U4 below), and the opening keeps its dead anchor frame (M1).	|| H10 (L-DELIVERY cells)
376	P	L735	[M7. `_open`, `_commit`, `_run`]	`_run`'s try body, `_commit(o)` and the call, has no loop.	|| G_HYG loop clause
377	P	L735	[M7. `_open`, `_commit`, `_run`]	Revision 5 has no loop in any function that `_run`'s try body or `finally` calls: the one-call steps iterate in C, not at a bytecode back-edge.	|| G_HYG loop clause
378	H	L735	[M7. `_open`, `_commit`, `_run`]	`rev5/spec_rev5_eventpath.py` finds no backward jump in `_run`, `_commit`, `_unwind_on`, `_unwind_off`, `_detach`, `_take`, `_register`, `_set_local` or `_named`.	|| 
379	H	L735	[M7. `_open`, `_commit`, `_run`]	It then faults every instruction of `_commit` and `_unwind_on`.	|| 
380	H	L735	[M7. `_open`, `_commit`, `_run`]	In every trial `_run`'s `finally` ran and left no anchor and no event (24 and 113–117 trials per version).	|| 
381	H	L735	[M7. `_open`, `_commit`, `_run`]	No signal position skipped `_run`'s `finally` (p10).	|| 
382	P	L737	[M7. `_open`, `_commit`, `_run`]	**The unwind scope (revision 2, M4; restated in revision 3; made atomic in revision 5).** The global PY_UNWIND is set only while some anchor is registered in this copy, so only while some section is open anywhere in the process.	|| model STALE; V67; H10 cell (a)
383	P	L737	[M7. `_open`, `_commit`, `_run`]	The protocol has no lock and no wait:	|| V68
384	P	L739	[M7. `_open`, `_commit`, `_run`]	- every clear is `_unwind_off(key)`: one call that pops the key and clears iff the id is named and no anchor is left.	|| G_HYG `_detach` clause; G_ATOM
385	P	L739	[M7. `_open`, `_commit`, `_run`]	The closer and every other detacher call it with the anchor frame, and the reconciliation calls it last with None.	|| G_HYG `_reconcile` clause
386	P	L741	[M7. `_open`, `_commit`, `_run`]	*Why no clear can land under a registered anchor.* A machinery clear happens only inside an `_unwind_off` call, and only if that same call finds `_ANCHORS` empty.	|| G_HYG frozen index sets; G_ATOM
387	P	L741	[M7. `_open`, `_commit`, `_run`]	Nothing runs inside that call.	|| G_ATOM _unwind_off cases; t1, D2, E
388	P	L741	[M7. `_open`, `_commit`, `_run`]	So at the instant of any machinery clear, no anchor is registered.	|| model U1
389	H	L741	[M7. `_open`, `_commit`, `_run`]	The argument uses no order between threads, no liveness test and no wait.	|| 
390	H	L741	[M7. `_open`, `_commit`, `_run`]	Revision 3 relied on `_ANCHORS or set_events(_TOOL[0], 0)` having no eval-breaker check between the test and the call.	|| 
391	old	L750	[M7. `_open`, `_commit`, `_run`]	An armed section's body never runs with S clear while its core is crediting and alive.** This holds on every interleaving, fault-free or not, whatever Python code runs between two machinery steps, on any thread, this one included.	|| revision 7 audit
392	P	L750	[M7. `_open`, `_commit`, `_run`]	The body starts after the opener's `_unwind_on`, and by the argument above no machinery clear lands while its anchor is registered.	|| model U1; X143, X145
393	P	L750	[M7. `_open`, `_commit`, `_run`]	After X3 nothing counts.	|| X71c (credit stop)
394	old	L750	[M7. `_open`, `_commit`, `_run`]	A fault in the machinery never clears S: faults only cut a step sequence short, and a fault cannot land inside a one-call step, other than a MemoryError from an allocation inside it (N2, "One-call steps").	|| revision 7 audit
395	old	L750	[M7. `_open`, `_commit`, `_run`]	`_register` never writes S: from revision 8 it binds no `set_events` (G_HYG's index clause), and G_ATOM's `_register` cases with S set check it (the eighth critic's M3).	|| revision 7 audit
396	P	L750	[M7. `_open`, `_commit`, `_run`]	**Revision 8 (M1): the model also takes as an axiom that a registration's exchange and its in-call count are one step.** No Python code runs between them on CPython: the tee form holds the previous callback until the call returns, and G_ATOM's finalizer case and X158d test this.	|| G_ATOM finalizer case; X158d
397	P	L751	[M7. `_open`, `_commit`, `_run`]	A clear S with A set lasts only while an opener is between its anchor store and its `_unwind_on`,** on the opener's own thread, before its body starts, or after an outside party cleared S.	|| model A0, U2
398	old	L752	[M7. `_open`, `_commit`, `_run`]	A set S with no anchor never exists**, fault-free or after a fault, except after an outside party set S or freed styxx's id, or after a MemoryError inside `_unwind_off`'s `set_events` (N2).	|| revision 7 audit
399	P	L752	[M7. `_open`, `_commit`, `_run`]	Then it lasts until the next reconciliation's `_unwind_off(None)` or the next close that leaves no anchor.	|| model STALE; X152
400	P	L752	[M7. `_open`, `_commit`, `_run`]	S is set only by `_unwind_on`, in the same call as a test that o's anchor is registered.	|| G_HYG frozen index sets; G_ATOM
401	P	L752	[M7. `_open`, `_commit`, `_run`]	A becomes empty only by a pop inside `_unwind_off`, in the same call as the clear.	|| G_HYG `_detach` clause; K7
402	H	L752	[M7. `_open`, `_commit`, `_run`]	`rev5/m5_modelcheck.py` checks this as STALE in every state of every configuration.	|| 
403	P	L753	[M7. `_open`, `_commit`, `_run`]	After faults**, S set with no live section lasts only while a *dead anchor* stays registered.	|| model; X140
404	D	L753	[M7. `_open`, `_commit`, `_run`]	This is L-DELIVERY scope only.	|| H10
405	P	L753	[M7. `_open`, `_commit`, `_run`]	The dead anchor's frame is on no live chain, so no count, note or refusal depends on it.	|| X146d
406	H	L753	[M7. `_open`, `_commit`, `_run`]	Revision 4's other fault window, a fault between a pop and its clear, no longer exists.	|| 
407	old	L753	[M7. `_open`, `_commit`, `_run`]	In `rev5/spec_rev5_eventpath.py`'s fault sweep, every trial in the close path left either nothing or a dead anchor with S set, never S set with no anchor.	|| revision 7 audit
408	P	L755	[M7. `_open`, `_commit`, `_run`]	A fault anywhere in `_commit`, which is revision 2's MF2 case, leaves neither: the `finally` pops the anchor (`o.frame` is still set) and clears S if no anchor is left.	|| X144, X143b; G_FI
409	P	L756	[M7. `_open`, `_commit`, `_run`]	- **What is lost (MONITOR_LOST completeness, revision 5; revision 7 adds callbacks and local events).** Nothing is lost through the machinery, on any interleaving, fault-free or not (U1).	|| model U1, LOSTNOTE; G_FI C8
410	P	L758	[M7. `_open`, `_commit`, `_run`]	- the next `_unwind_on` anywhere in the process finds S clear, and notes every armed, registered opening in the same call in which it sets S;	|| X137c
411	P	L760	[M7. `_open`, `_commit`, `_run`]	No machinery step repairs a mint's local events while any holder is live (only a rebinding or re-take sets them, and those count), so every holder of that mint finds them changed at its exit.	|| X137 (local events cleared); model
412	D	L762	[M7. `_open`, `_commit`, `_run`]	The silent shapes, all outside parties that undo their own change before a machinery step reads it: a party that cleared S sets it again itself; a party that replaced a callback restores it before any exit registers; a party that cleared a mint's local events sets them again before every holder's ex	|| X158, X158b (A12); the model excludes restoring parties
413	H	L762	[M7. `_open`, `_commit`, `_run`]	Revision 6 had a fourth, silent through the machinery itself: an exit's registration repaired a replaced callback for every trace, but only the exiting trace was noted (the seventh critic's B1, closed by the count;	|| 
414	P	L762	[M7. `_open`, `_commit`, `_run`]	The model check covers every place in this list with two cores from revision 7 (`cb2:` configurations).	|| model cb2 configurations
415	P	L762	[M7. `_open`, `_commit`, `_run`]	Every flag follows a state in which S was clear under an armed, registered anchor, which by U1 only an outside party can cause.	|| model U1, FALSEFLAG
416	H	L768	[M7. `_open`, `_commit`, `_run`]	Revision 7 violates no property in 58 configurations and 99 runs (10,465,412 state hashes); revision 6 violates LOSTNOTE in the 7 runs that combine a replaced callback with a second core (B1), and nowhere else.	|| 
417	H	L772	[M7. `_open`, `_commit`, `_run`]	Each id carries callbacks and the mint carries local events per id, so a hit counts only if its entry and unwind reach styxx's callbacks, and X5's callback and local-events notes are modelled.	|| 
418	H	L772	[M7. `_open`, `_commit`, `_run`]	**The model takes the atomicity of `_unwind_on`, `_unwind_off`, `_take` and `_set_local` as an axiom** (each is one step), so its results hold only if that premise does;	|| 
419	H	L772	[M7. `_open`, `_commit`, `_run`]	Revision 6 violates no property in any of 44 configurations and 83 runs (9,625,175 state hashes); revision 7's two-core configurations show that this was so only because none combined a replaced callback with a second core (B1).	|| 
420	H	L776	[M7. `_open`, `_commit`, `_run`]	Revision 6 corrects two readings of it: part C probed only `_unwind_off` and `_unwind_on`, and part D does not discriminate.	|| 
421	H	L777	[M7. `_open`, `_commit`, `_run`]	- `rev5/m5_modelcheck.py`: every interleaving of 33 configurations (revision 5 said 35; the sixth critic counted `CONFIGS`, M2 v).	|| 
422	H	L777	[M7. `_open`, `_commit`, `_run`]	Revision 6 (M2): that includes `_register`, which the model did not model at all (it had no callback registration), and the model takes the one-call premise as an axiom, so it gives no evidence for the premise itself.	|| 
423	H	L778	[M7. `_open`, `_commit`, `_run`]	- same-thread re-entry at every step boundary: a section, a suspended `run_async` section, a user-lock wait, or a transaction run inside another transition;	|| 
424	H	L785	[M7. `_open`, `_commit`, `_run`]	Revision 5 violates none of U1, A0, STALE, FALSEFLAG, LOSTNOTE, CLOBBER, P8, HANG, CYC_OPEN, END and REPAIR in 70 runs (4.9 million state hashes), where CLOBBER and FALSEFLAG covered global and local events only, not callbacks or X5's two other note sources (revision 6, M2).	|| 
425	H	L785	[M7. `_open`, `_commit`, `_run`]	Its only cycle is the robust mutex's through a user lock, broken by the bound (CYC_TX).	|| 
426	H	L785	[M7. `_open`, `_commit`, `_run`]	Revision 4 in the same model shows every finding of the fifth critic (R19, the masked loss, the cycles, the tool-id race) and the greenlet loss.	|| 
427	H	L786	[M7. `_open`, `_commit`, `_run`]	- `rev5/w5_witnesses.py`: every revision-5 witness passes under the spec and fails under its single-rule mutant, on both versions.	|| 
428	H	L787	[M7. `_open`, `_commit`, `_run`]	- R19's shape at every point of a close: `{t:1}`, no note.	|| 
429	H	L787	[M7. `_open`, `_commit`, `_run`]	The loss appears only under a split `_unwind_off`;	|| 
430	H	L793	[M7. `_open`, `_commit`, `_run`]	- `rev4/m1_pairs_modelcheck.py`: every interleaving, at single-C-operation granularity, of every pair of concurrent transitions (open, close, exit of the same and of another tracer, a second exit, a pruning reconciliation) and four triples, with up to two faults.	|| 
431	H	L793	[M7. `_open`, `_commit`, `_run`]	Revision 4 violates none of U1, A0, HANG, END, REPAIR and P8 in any of 15 configurations (49.5 million state hashes).	|| 
432	H	L793	[M7. `_open`, `_commit`, `_run`]	Revision 3 leaves `_ANCHORS[None]` and sets false flags when exit races its own tracer's sections, and with its test and clear split it violates U1 fault-free.	|| 
433	H	L794	[M7. `_open`, `_commit`, `_run`]	An anchor left registered after the exit and the join: revision 2 in 69 / 49 (a frame key), revision 3 in 62 / 51 (the None key), revision 4 in 0 / 0 (3.12.3 / 3.13.12).	|| 
434	H	L795	[M7. `_open`, `_commit`, `_run`]	- `rev4/s4_stress.py`: revision 3's s1 with a forced hand-off (probability 0.3) at every protocol statement boundary, including between the test and the clear, plus 3 open-versus-own-exit races per round. 100 rounds × 3 seeds × 3 modes (clean, faults without dead anchors, all faults) × with and with	|| 
435	H	L795	[M7. `_open`, `_commit`, `_run`]	Revision 4: 0 of 636,500 in-body samples with S clear; all 318,250 raising calls counted; no MONITOR_LOST; 5,400 exit races left no anchor and set no false flag; 88,092 injected faults; no stale S at any fault-free quiescent point (0 of 1,200).	|| 
436	H	L795	[M7. `_open`, `_commit`, `_run`]	In the no-dead-anchor fault runs, 59 of 1,200 quiescent points had a stale S, and one close cleared each.	|| 
437	H	L795	[M7. `_open`, `_commit`, `_run`]	Every run ended with `anchors`, `global_events` and `clearing` all 0 after its exit.	|| 
438	H	L795	[M7. `_open`, `_commit`, `_run`]	Revision 3 with the split (no exit races): 525 of 281,090 samples clear, 281 of 140,545 calls lost, MONITOR_LOST in 12 of 12 runs.	|| 
439	H	L795	[M7. `_open`, `_commit`, `_run`]	Revision 3 as specified, with exit races: an anchor left after 1,665 of 5,400 races, and every such run ended with `anchors 1` and `global_events 4096`.	|| 
440	H	L799	[M7. `_open`, `_commit`, `_run`]	- `rev3/m1_unwind_modelcheck.py`: every interleaving of two whole sections and one retiring `_retire` step 6, with 0–2 faults, with a step boundary after every C call.	|| 
441	H	L799	[M7. `_open`, `_commit`, `_run`]	Revision 3: U1 holds on all 1,223 / 4,300 / 7,216 reachable states, and `UNWIND_LOST` is set on none; revision 2 has 46 fault-free hits with S clear; revision 3 with its test and clear split has 54, which is what revision 4 removes.	|| 
442	H	L800	[M7. `_open`, `_commit`, `_run`]	- `rev3/s1_unwind_stress.py`: real threads, a forced GIL hand-off with probability 0.3 at every protocol statement boundary, 1 µs switch interval.	|| 
443	H	L800	[M7. `_open`, `_commit`, `_run`]	Revision 3: 0 of 736,048 in-body samples with S clear, every one of 368,024 raising calls counted, no MONITOR_LOST.	|| 
444	H	L805	[M7. `_open`, `_commit`, `_run`]	- each toggle bumps the interpreter's monitoring version, so every code object that then runs is re-instrumented once, about 0.05–0.10 µs per distinct function per section (p4);	|| 
445	C	L806	[M7. `_open`, `_commit`, `_run`]	- other tools' `DISABLE`d locations stay disabled: no location fired more than twice in 1,000 toggling sections, and a location firing twice is a line that spans two offsets (`rev2/p4b_disable_kept.py`).	|| rev2/p4b_disable_kept.py
446	P	L818	[M9. The at-fork handler]	`os.register_at_fork(after_in_child=_forget_in_child)` is registered at import on every version; it is a no-op until a tracer has been constructed.	|| G_FI fork scenario (C9)
447	P	L818	[M9. The at-fork handler]	Revision 2 (N5) orders the steps so that every step that runs no user code comes before any step that can:	|| G_HYG `_forget_in_child` clause
448	H	L820	[M9. The at-fork handler]	`_ANCHORS.clear()` (revision 5: `_CLEARING` no longer exists);	|| 
449	P	L826	[M9. The at-fork handler]	Step 6 is the only one that runs user code: a `__code__` write raises the `object.__setattr__` audit event.	|| G_HYG `_forget_in_child` clause
450	H	L826	[M9. The at-fork handler]	In revision 2 the child keeps only the minted code still installed on the remaining functions (`rev2/p8_atfork_abort.py`, both versions).	|| 
451	C	L826	[M9. The at-fork handler]	That code has no events and runs as the original would, since M_T is an equal copy.	|| G_FI C9 (`__code__` equal, no local events)
452	P	L828	[M9. The at-fork handler]	The pass-through no longer depends on this handler.	|| G_FI C9
453	P	L830	[M9. The at-fork handler]	Inherited cores have another pid: `run()` passes through, `__exit__` is a no-op, and `record()` refuses.	|| V47; G_FI C9
454	H	L839	[M10. The introspection interfa]	- `"tool"`: int or None;	|| 
455	P	L848	[M10. The introspection interfa]	It has no nested function, lambda, generator expression or other class; the facade and the plain-data classes `_Mint`, `_Core`, `_Opening` and `_Txn` have no other methods.	|| G_HYG co_consts clause
456	P	L848	[M10. The introspection interfa]	Revision 4 (MF4) makes this mechanical: no region code object has a code object in its `co_consts`.	|| G_HYG co_consts clause
457	H	L852	[M10. The introspection interfa]	- **The G_ATOM interface (revision 7, M1; frozen with the prereg, read by G_ATOM only).** G_ATOM has to put each step function into each of its gate outcomes, and some of those outcomes need an opening that is not registered, several armed openings, a registered key that no section owns, and the id 	|| 
458	H	L852	[M10. The introspection interfa]	Revision 6 said G_ATOM needed only `_v5_faultpoints()`, the public API and `sys.monitoring`; its own prototype wrote `_ANCHORS` and built openings directly (the seventh critic's M1 item 1).	|| 
459	old	L852	[M10. The introspection interfa]	The implementation must provide them with exactly these meanings, and no exam case may read them:	|| revision 7 audit
460	H	L855	[M10. The introspection interfa]	`_named` is an identity test, so no other object will do);	|| 
461	G	L857	[M10. The introspection interfa]	- `_Opening` and `_Core` (read: G_ATOM builds objects with `_Opening.__new__(_Opening)` and `_Core.__new__(_Core)`, assigns only the slots the steps read, `core`, `frame` and `armed` on an opening and `flags` on a core, and checks the four member descriptors; no constructor signature is frozen).	|| 
462	G	L857	[M10. The introspection interfa]	It is hashable by identity, owned by no section and on no stack, so it serves as an anchor key as a frame does;	|| 
463	P	L864	[M11. Scoring and check_metrics]	- **`_check_coverage` step 1 (NO_TRACE).** Accepts a result iff `issubclass(type(result), dict)`, and reads it only with `dict.get`.	|| V53, X93d
464	N	L864	[M11. Scoring and check_metrics]	"no 'coverage_trace' key";	|| 
465	P	L866	[M11. Scoring and check_metrics]	- **Step 3 (BAD_TRACE).** Every clause tests the exact type before any hash, membership or comparison.	|| X96-X115, X103b, X103c
466	P	L866	[M11. Scoring and check_metrics]	`end` is tested with `type(end) is not str or end not in _ENDS`; the same applies to `gates_sha256` (exact str), every dict key, and every note or problem string before its regex.	|| X96-X115, X103b, X103c
467	P	L867	[M11. Scoring and check_metrics]	Coverage is checked for **every declaring gate, whatever its bar** (X122).	|| X122
468	old	L868	[M11. Scoring and check_metrics]	- **NOT_EXERCISED.** The fixed sentence reads: "work on other threads, pools, executors, child processes or asyncio tasks is credited only to a section that work opens itself; generator and coroutine objects created before the trace are never credited; a call counts only when it returns, yields or r	|| revision 7 audit
469	H	L869	[M11. Scoring and check_metrics]	This is a v3-path error and carries no [V5] code.	|| 
470	old	L870	[M11. Scoring and check_metrics]	- **check_metrics.** It never raises for a JSON-shaped result: dicts, lists, str, bool, None, floats and ints of any size.	|| revision 7 audit
471	P	L871	[M11. Scoring and check_metrics]	It is truthy only for exact bool, int, float or str with a truthy value, so no user `__bool__` runs.	|| X117c
472	P	L872	[M11. Scoring and check_metrics]	`_finite` catches **OverflowError only**, so asynchronous exceptions still propagate.	|| G_HYG except clause; X117b
473	P	L874	[M11. Scoring and check_metrics]	- The coverage note is "smoke run" only when the trace is absent.	|| X117d
474	H	L882	[Cost]	about ×1.0 (local events only: D1 t_cost)	|| 
475	H	L885	[Cost]	Only while some section is open (revision 2).	|| 
476	H	L885	[Cost]	Outside every section, ×0.99–×1.04: no callback (`rev2/p11_cost.py`; critic2/t_unwind_cost.py measured ×1.49–×2.17 under revision 1's always-on scope)	|| 
477	H	L886	[Cost]	Revision 4, uncontended: the clearer's announcement and withdrawal (`_txn()`, a store, a pop) add 0.46–0.56 µs to every close that clears, and the re-check with the `_CLEARING` test 0.04–0.05 µs to every open, while the open drops one `_unwind_on()` (0.22–0.23 µs): net +0.29 µs (3.13.12) and +0.38 µ	|| 
478	H	L886	[Cost]	With a pure-Python profile function on every thread, 0.70–0.82% waited, 0.55–0.61 ms on average, at most 2.1 ms.	|| 
479	H	L886	[Cost]	Every call was counted and no MONITOR_LOST was noted (`rev4/b4c_contention.py`).	|| 
480	P	L886	[Cost]	No open ever waits, so contention costs nothing extra.	|| V68
481	H	L886	[Cost]	With no other section open: 9.03–9.27 → 10.76–11.48 µs on 3.12.3, and 7.24–7.53 → 9.16–9.18 µs on 3.13.12.	|| 
482	H	L888	[Cost]	CLONE_ALIVE's `gc.get_referrers` scan runs only when the refcount gate fires (about 17–19 ms on a 1M-object heap under v5e)	|| 
483	old	L893	[What is removed from v5e]	- every `sys.setprofile` call; styxx never calls `sys.setprofile`, `threading.setprofile`, `sys.settrace` or `threading.settrace` (G_HYG, revision 7);	|| revision 7 audit
484	P	L906	[]	**Rule:** no machinery state may depend on a `finally` or with-exit running across a loop, `await` loops included.	|| G_HYG loop clause; X140; H9
485	P	L908	[]	Revision 5 (N2) lists every machinery site that raises an audit event, where a raising audit hook is such a source:	|| V71 and the corrected Sources list (revision 8 self-audit, W2): the list claimed every site and missed `builtins.id`, `object.__getattr__` (f_code) and `_run`'s `sys._getframe`  [FIX W2]
486	C	L909	[]	- `object.__setattr__` at every `__code__` write (E4 and `_retire`);	|| critic5/c5_audit_events.py; PEP 578 audit table
487	C	L910	[]	- `sys._getframe` in `_txn()`, at E0, at X1 and in every `_locked`, and in every callback (`sys._getframe(1)`).	|| critic5/c5_audit_events.py; PEP 578 audit table
488	D	L910	[]	A hook that raises on it makes every hit raise out of its callback (L-DELIVERY);	|| L-DELIVERY; H10
489	C	L911	[]	- `sys._current_frames` in `_alive`, called by `_acquire` on contention and by every reconciliation's holder test;	|| critic5/c5_audit_events.py; PEP 578 audit table
490	C	L913	[]	- (revision 6, B1) `sys.monitoring.register_callback`, five times inside every `_register`: at E4 (`_ensure_tool`: a first acquisition, an adoption or a rebinding), at M3 step 0 (`_reclaim`, in any transaction after a free), and at X5, which runs on **every exit**, inside the robust mutex.	|| critic6/a1_audit_monitoring.py; G_ATOM (b)
491	H	L913	[]	Revision 5's N2 row said this list named every site; it omitted this one.	|| 
492	P	L915	[]	No one-call step raises an audit event: `_unwind_on`, `_unwind_off`, `_take` and `_set_local` call only `sys.monitoring` functions that raise none (`critic6/a1_audit_monitoring.py`), and G_ATOM checks every gate outcome of each (`rev6/atom6.py`).	|| G_ATOM (b)
493	P	L915	[]	So revision 5 removed revision 4's two open- and close-path sites, `_txn()` in every clearing `_unwind_off` and `_alive` in every waiting open (the fifth critic's `critic5/c5_audit_events.py`), and the open and close paths still raise no audit event.	|| V71 (revision 8 self-audit, W2): corrected, the open path raises one audit event, `sys._getframe` in `_run`/`_run_async`, before `_open`  [FIX W2]
494	P	L917	[]	- **Commits are single C operations:** dict setitem, `setdefault`, `pop` and `get` with str, int, frame, function, `_Core` or `_Txn` keys (identity or str hashing, so no Python `__hash__` or `__eq__` runs);	|| model (one step per commit); X142b
495	P	L917	[]	The one comparison with a foreign object, another tool's name from `get_tool`, is an identity test (`_named`, revision 5), so no foreign `__eq__` runs.	|| model (one step per commit); X142b
496	P	L917	[]	They use the order of these operations on each thread, and the fact that nothing runs inside the C call of a one-call step, which calls no Python code and raises no audit event (revision 5; revision 6 restricts the claim to those steps).	|| G_ATOM
497	P	L918	[]	- **No `try … finally`, no `with` statement and no threading lock** exist anywhere in the machinery, with one exception: `_run`/`_run_async`'s single `try … finally` around fn.	|| G_HYG with/lock/try clause
498	P	L919	[]	- `_run`'s try body is `_commit(o)` and the call of fn, and contains no loop;	|| G_HYG loop clause; rev8/h8_backedges.py
499	P	L919	[]	`_commit` has none either (revision 3).	|| G_HYG loop clause; rev8/h8_backedges.py
500	P	L919	[]	Revision 5: nothing `_commit` or `_detach` calls has a loop either.	|| G_HYG loop clause; rev8/h8_backedges.py
501	P	L919	[]	Revision 4's `_await_clearers` loop is deleted, and a one-call step iterates in C, where there is no bytecode back-edge (`rev5/spec_rev5_eventpath.py`: no backward jump in any of them, both versions).	|| G_HYG loop clause; rev8/h8_backedges.py
502	P	L919	[]	The `finally` itself is `_detach(o, …)` and `o.frame = None`, with no loop.	|| G_HYG finally clause
503	P	L920	[]	No count, note, refusal or termination depends on that `finally` running.	|| X140, H9; G_FI
504	P	L920	[]	If it is skipped, the only machinery state left is a dead anchor, which exit's X3 or a reconciliation pops, and which keeps PY_UNWIND set until then (U4, L-DELIVERY scope).	|| X140; model END
505	P	L921	[]	- The only `try … except` clauses are the two user-code sites (the import and PEP 562), which chain into UNRESOLVED, and exit's two `except GateSpecError` clauses around `_locked`, at X0 and X5.	|| G_HYG except clause
506	P	L924	[Invariants preserved by every ]	- **I1: no over-credit.** A count is added to opening O for a frame F of M_T only if all three hold:	|| model; G_FI C1
507	P	L930	[Invariants preserved by every ]	- **I2: mint integrity.** `fn.__code__ is m.code` only while m is registered with at least one holder that is active or has a live token.	|| G_FI C5, C7; model
508	P	L932	[Invariants preserved by every ]	- every registered mint has only live holders;	|| G_FI C5, C7; model
509	P	L933	[Invariants preserved by every ]	- no mint without a live holder stays registered;	|| G_FI C5, C7; model
510	P	L934	[Invariants preserved by every ]	- a retired mint has no local events and no pending entries;	|| G_FI C5
511	P	L935	[Invariants preserved by every ]	- global PY_UNWIND is clear if no anchor is registered (M7, U3).	|| model STALE; G_FI C5; X152
512	P	L935	[Invariants preserved by every ]	Revision 5 makes this hold in every state, not only after a reconciliation, unless an outside party set the event or freed the id.	|| model STALE; G_FI C5; X152
513	P	L935	[Invariants preserved by every ]	Every reconciliation ends with `_unwind_off(None)`, which repairs that case.	|| model STALE; G_FI C5; X152
514	H	L935	[Invariants preserved by every ]	Revision 2 said "set only while some anchor is registered; a close or reconciliation repairs a stale set", and both halves were false (the third critic's MF1, MF2).	|| 
515	H	L935	[Invariants preserved by every ]	Revision 3's version of this clause was false after an open that raced its trace's exit, which left `_ANCHORS[None]` registered for the rest of the process (the fourth critic's B1); revision 4 closes that (At open, steps 8 and 9);	|| 
516	P	L936	[Invariants preserved by every ]	- (revision 4) no registered anchor belongs to a dead core (the prune of cores reached from anchors);	|| X146d; model
517	P	L937	[Invariants preserved by every ]	- no freed, unowned tool id still carries styxx's events or callbacks: step 0 re-takes it (revision 2).	|| X137 free variant
518	P	L938	[Invariants preserved by every ]	Every cleanup is idempotent and unconditional.	|| model REPAIR; G_FI
519	P	L938	[Invariants preserved by every ]	After its core exits or is pruned, an opening holds no anchor, once its opener has left `_commit` (revision 4: an opener that stores after X3 or a prune detaches itself at step 8, and one that stored before is popped by X3 or the prune, which read the same `o.frame` key; the model check's END proper	|| model END
520	P	L939	[Invariants preserved by every ]	- **I5: no hang, and the only wait is the robust mutex (revision 5).** The mutex serializes transactions (enter and exit).	|| V68; model HANG
521	old	L939	[Invariants preserved by every ]	No threading lock exists, so a skipped release cannot hang anyone.	|| revision 7 audit
522	D	L941	[Invariants preserved by every ]	One cycle of waits can still form, and only through user code.	|| model CYC_TX (bounded); X138
523	H	L941	[Invariants preserved by every ]	Examples: an audit hook on a `__code__` write, or on `sys.monitoring.register_callback` (raised five times inside every exit's X5; revision 6, B1), or a finalizer, that takes a lock another thread holds around `cov.__exit__()` or `with coverage_trace(...)`.	|| 
524	old	L943	[Invariants preserved by every ]	Revision 4's claim here, "a clearer never waits for anything, so no cycle of waits exists inside the machinery", was false.	|| revision 7 audit
525	P	L943	[Invariants preserved by every ]	With the bounds, revision 5 has no reachable state where live work exists and nothing can step, and no cycle except the mutex's (Revision 5 table).	|| model HANG
526	P	L944	[Invariants preserved by every ]	- **I6: the envelope.** A fault inside the machinery can change a trace's record only within these bounds:	|| G_FI C8
527	old	L947	[Invariants preserved by every ]	No fault, no instrument and no re-entrant code clears the event under a registered anchor (U1), so a fault cannot cause `UNWIND_LOST`.	|| revision 7 audit
528	D	L953	[Invariants preserved by every ]	An asynchronous exception delivered inside the PY_UNWIND callback, which exists only while some section is open (revision 2), replaces the exception being unwound; its `__context__` is None, so the original is lost (L-DELIVERY;	|| L-DELIVERY; H10
529	P	L953	[Invariants preserved by every ]	The only other conversions are at the two documented user-code sites, the declared-module import and a PEP 562 `__getattr__`, each giving a chained UNRESOLVED.	|| UNRESOLVED chained cases
530	P	L957	[Per-transition prefixes]	- Before E4: nothing global has changed.	|| G_FI (prefixes)
531	P	L958	[Per-transition prefixes]	If `_TOOL[0]` was None, the next enter adopts the id and registers again (Tool acquisition).	|| X154e (revision 8 self-audit, W7); no case reached adoption before  [FIX W7]
532	P	L965	[Per-transition prefixes]	- Before the append: nothing has changed.	|| G_FI (prefixes)
533	old	L967	[Per-transition prefixes]	- Inside `_commit` (revision 3), at any point, including inside `_unwind_on` (a fault lands before or after its one call, never inside): the fault is inside `_run`'s `try`, so the `finally`'s `_detach` pops the anchor if it was stored (by `o.frame`, which only this `finally` releases, after the pop)	|| revision 7 audit
534	P	L967	[Per-transition prefixes]	`o.armed` is still False, so no `UNWIND_LOST`.	|| X144
535	H	L967	[Per-transition prefixes]	A fault after the first set left the event on with no anchor, and a fault after the commit left a dead anchor.	|| 
536	H	L967	[Per-transition prefixes]	Either kept PY_UNWIND on outside every section until the trace exited, and the next close did not clear it (`rev3/c7r3_stale_unwind_after_open_fault.py`: 33,552–51,880 lost handlers in 300,000 iterations after one fault, against 0).	|| 
537	P	L972	[Per-transition prefixes]	Scope only, no count.	|| U4; X140
538	P	L973	[Per-transition prefixes]	Exit's X3 later re-detaches this opening if `o.frame = None` did not run.	|| X146; model
539	P	L974	[Per-transition prefixes]	- After `_detach`, before `o.frame = None` (revision 4): the opening keeps its anchor frame until the core is dropped (M1).	|| X146c
540	P	L974	[Per-transition prefixes]	No registry is affected.	|| G_FI (prefixes)
541	P	L975	[Per-transition prefixes]	- **Callbacks.** Their only writes are `m.pend[...]` stores and pops, single-key increments and clone flags.	|| G_FI callback-only sweep (driver); C1
542	old	L975	[Per-transition prefixes]	A fault loses at most that one credit and can never add one: an entry killed before its store has no pending entry, and a pending entry killed at its entry offset never publishes.	|| revision 7 audit
543	P	L976	[Per-transition prefixes]	The prune of cores reached from anchors is idempotent too: it pops only what belongs to a dead core, and `_detach` of an already-finalised opening repeats only cleanups.	|| model; X146d
544	P	L977	[Per-transition prefixes]	A tool left named ours with missing callbacks (a fault, including a raising audit hook, inside `_register`) is adopted and re-registered at the next enter when `_TOOL[0]` is None.	|| X154e (revision 8 self-audit, W7)  [FIX W7]
545	P	L977	[Per-transition prefixes]	When `_TOOL[0]` is set (the fault was in a reclaim's registration), the id is kept as it is, and the next exit's X5 registration repairs it (revision 7 corrects revision 6's "at the next enter", which held only for `_TOOL[0]` None;	|| X157b (R)
546	H	L978	[Per-transition prefixes]	- Revision 5 claimed that no race with another tool can make styxx raise or write on a foreign id.	|| 
547	P	L978	[Per-transition prefixes]	Revision 6 (B1): no race can make styxx *raise*, and no race can make any write other than a `_register` exchange land on a foreign id.	|| X154 sweeps; X154b
548	H	L979	[Per-transition prefixes]	- `_ensure_tool` and `_reclaim` tested `get_tool(id) is None`, then called `use_tool_id`.	|| 
549	old	L983	[Per-transition prefixes]	Every write of events or local events is now a one-call step gated on the name (X154), and the one read on the close path, `get_events`, never raises.	|| revision 7 audit
550	D	L983	[Per-transition prefixes]	No library does this.	|| R21
551	P	L984	[Per-transition prefixes]	- **At-fork.** It runs in the child's only thread, and every step is idempotent.	|| G_FI C9
552	P	L984	[Per-transition prefixes]	Only its last step runs user code (M9, revision 2).	|| G_HYG `_forget_in_child` clause
553	P	L990	[Residuals, disclosed]	From revision 2 the process-wide PY_UNWIND callback persists only while one of its sections is still open.	|| model STALE; H10 cell (a)
554	D	L990	[Residuals, disclosed]	A zombie whose sections closed normally leaves no global callback.	|| X92c
555	D	L991	[Residuals, disclosed]	With no later transaction, that is until the process exits.	|| X92c
556	D	L995	[Residuals, disclosed]	Two sites run audit hooks inside a transaction: `__code__` writes (E4's install and `_retire`'s restore), and, from revision 6's correction, the five `sys.monitoring.register_callback` events of `_register` at X5 on **every exit**, at E4 and at a reclaim (B1).	|| model `with L: exit(X) || exit(Y)+[lock]` (any site); corrected in revision 8 (W5): the list omitted `sys._current_frames` in the reconciliation holder test  [FIX W5]
557	H	L996	[Residuals, disclosed]	In `rev5/m5_modelcheck.py` (`greenlet: [exit(X), rec] || open(X)`) no property is violated when they do, because every reconciliation step is idempotent, and revision 6's model adds outside parties to that configuration with the same result (`rev6/m6_modelcheck.py`, `ext+greenlet: [exit(X), rec] || 	|| 
558	P	L996	[Residuals, disclosed]	Only a transaction is affected.	|| model greenlet configurations
559	old	L996	[Residuals, disclosed]	A switch cannot land inside a one-call step, so the event path has no greenlet exposure (`greenlet: [close(X), open(X)] || open(Y)`: revisions 5 and 6 clean; revision 4 has U1 violations and 2 end states whose loss is silent).	|| revision 7 audit
560	D	L996	[Residuals, disclosed]	A switch inside a reclaim, after its re-take and before its `_LOST` count, lets another greenlet's exit run its MONITOR_LOST test before the count; a call lost while the id was freed is then noted only by the close's ungated event read (F14, X137f), which revision 6 keeps for that reason.	|| X137f
561	H	L1001	[Residuals, disclosed]	The fifth critic showed that R19 needs no second thread (`critic5/c2_r19_same_thread_resume.py`), and that its loss is silent once any other section opens before the blinded close (`critic5/c2b_r19_silent.py`).	|| 
562	P	L1001	[Residuals, disclosed]	Revision 5's clear is one call, so there is no window.	|| mutation row: `_unwind_off`'s test and clear split (X148)
563	H	L1001	[Residuals, disclosed]	`rev5/w5_witnesses.py` opens the suspended section at every point of a close: `{t:1}`, no note, at every point.	|| 
564	P	L1001	[Residuals, disclosed]	The loss reappears only under a split `_unwind_off` mutant.	|| mutation row: `_unwind_off`'s test and clear split (X148)
565	D	L1002	[Residuals, disclosed]	- **Interpreter shutdown.** Exit during finalization runs inline (there is no helper thread).	|| Stated limits' 'Not covered' list
566	H	L1004	[Residuals, disclosed]	**Evidence and what must be re-run.** D1's crash sweep, run with its monitoring adapter, injected an exception at every machinery bytecode: 6337 points on 3.12 and 6283 on 3.13, 0 failures on D1's invariants C1–C8, with the C7 check using live facades. v5f's C1–C9 are defined under G_FI (revision 3,	|| 
567	H	L1004	[Residuals, disclosed]	Revision 2 (BF1, M8) reaches it another way: the frozen direct-call driver `cover_driver_v5f.py` calls the five callback-only functions outside any callback, as `FunctionType(code, vars(styxx.protocol))` built from `_v5_faultpoints()` code objects.	|| 
568	H	L1012	[]	There is no sys.monitoring. 3.10's `call_trampoline` runs FastToLocals/LocalsToFast around every Python profile callback.	|| 
569	H	L1012	[]	That reverts closure writes made by other threads and keeps deleted locals alive (round-4 BLOCKER), and no Python hook avoids it.	|| 
570	H	L1013	[]	There is no sys.monitoring.	|| 
571	H	L1013	[]	Any Python-level profile hook makes every C call on a sectioned thread skippable, and it leaked the harness's own lock in about 7% of SIGALRM timeouts (see Decisions).	|| 
572	P	L1014	[]	The global PY_UNWIND is set only while some section is open (M7, "The unwind scope"; revision 3 corrects revision 1's "while a mint exists" here, MF8).	|| model STALE; V67; H10
573	P	L1014	[]	No profile or trace function is ever installed.	|| G_HYG (A3)
574	H	L1015	[]	every other CPython 3.12.x and 3.13.x (GIL builds)	|| 
575	G	L1015	[]	A patch level joins `_VERIFIED` only with a passing G_ATOM run committed beside it (M0, Process gates).	|| 
576	P	L1015	[]	Revision 6 said results on other patch levels "are reported as unvalidated"; under M0 no tracing result can be produced there (revision 7, M2).	|| X37b
577	G	L1018	[]	**One mechanism, no version-keyed cases.** Every exam case has the same expected outcome on the two verified interpreters, 3.12.3 and 3.13.12 (G_XVER).	|| 
578	G	L1018	[]	No enumerated difference remains.	|| 
579	H	L1022	[]	So publish-at-entry is over-credited under signals on 3.12 only.	|| 
580	G	L1026	[]	**The refused interpreters in the exam.** On 3.10 and 3.11 the exam runs the refusal cases (X37) and every scoring-only case with a prebuilt `/3` trace.	|| 
581	G	L1026	[]	No real unverified 3.12 or 3.13 patch level is required: X37b pins M0's patch-level refusal on a verified interpreter by replacing `sys.version_info` after import (revision 7, M2 and N6).	|| 
582	C	L1030	[]	`set_events` on the freed id raises ValueError, so nothing can clear it without re-taking it (`rev2/p3_free_reclaim.py`).	|| rev2/p3_free_reclaim.py
583	P	L1030	[]	Revision 2 (M3): while the id has no owner, the next transaction re-takes it under `_TOOL_NAME`, which clobbers nobody, and clears its events.	|| X137 free variant
584	P	L1031	[]	From revision 4 no machinery property depends on that not happening (U1).	|| model U1
585	P	L1031	[]	Revision 5 does not rely on a wait for this either: no such tool can run inside a one-call step (`rev5/t1_onecall_atomic.py`).	|| G_ATOM
586	old	L1037	[]	Every code is a literal at its own emission site (G7) and must survive the literal-census refusal gate (G_REF).	|| revision 7 audit
587	P	L1043	[]	`coverage_trace(exp)` when no gate declares `exercises`	|| the code's cases (Mutation audit code->case rows; Exam cases)
588	P	L1044	[]	`coverage_trace()` or `__enter__` on anything but CPython 3.12.3 or 3.13.12 (`_VERIFIED`; revision 6: any other patch level too) GIL builds; also any `coverage_trace()` (revision 7: every one, not only the first) at which a `sys.monitoring` function to be bound, or already bound in `_MON`, or a one-	|| the code's cases (Mutation audit code->case rows; Exam cases)
589	P	L1044	[]	raise; nothing constructed, nothing bound; only the code prefix is spec-fixed	|| the code's cases (Mutation audit code->case rows; Exam cases)
590	P	L1046	[]	The stdlib codes themselves are new to the cut at the process's first `coverage_trace()`, so a clone of `Handle._run`'s code alive then makes every `coverage_trace()` refuse while it lives (MF4, #21)	|| the code's cases (Mutation audit code->case rows; Exam cases)
591	P	L1046	[]	raise; nothing minted	|| the code's cases (Mutation audit code->case rows; Exam cases)
592	P	L1047	[]	entry: the import raised; the `sys.modules` entry is not a module; the name is absent with no module `__getattr__`; the module `__getattr__` raised;	|| the code's cases (Mutation audit code->case rows; Exam cases)
593	P	L1048	[]	entry: a class step whose name exists only in some class of the MRO after the first (not only the direct bases)	|| the code's cases (Mutation audit code->case rows; Exam cases)
594	P	L1052	[]	entry: the declared module has no exact-str, non-`.pyc` `__file__`; a visited function link is incoherent (its `co_filename` is not its module's file); or no visited link has the module's globals	|| the code's cases (Mutation audit code->case rows; Exam cases)
595	P	L1053	[]	raise; nothing minted	|| the code's cases (Mutation audit code->case rows; Exam cases)
596	P	L1056	[]	entry: a mint with a live holder is no longer `T.__code__`.	|| the code's cases (Mutation audit code->case rows; Exam cases)
597	P	L1056	[]	Exit: `T.__code__ is not M_T`, checked by **every** holder; the swap is left in place	|| the code's cases (Mutation audit code->case rows; Exam cases)
598	P	L1058	[]	open: no gate declares the section, **or the section is not a str** (never compared).	|| the code's cases (Mutation audit code->case rows; Exam cases)
599	P	L1060	[]	entry event: M_T running with foreign globals; recorded by **every** holder	|| the code's cases (Mutation audit code->case rows; Exam cases)
600	P	L1066	[]	score: the tracer is not exactly the str `/3` (every `/1` and `/2` trace refuses)	|| the code's cases (Mutation audit code->case rows; Exam cases)
601	P	L1069	[]	every declaring gate refuses with the first problem's code	|| the code's cases (Mutation audit code->case rows; Exam cases)
602	P	L1070	[]	score: no opening of the gate's *declared* section	|| the code's cases (Mutation audit code->case rows; Exam cases)
603	D	L1074	[]	A free whose re-take was cut short by a raising audit hook is not noted on a trace that lost nothing (F25)	|| X157b (F25)
604	P	L1074	[]	never refuses; appended to every opening of the trace	|| X137 free variant
605	P	L1075	[]	styxx never touches profilers; every profiler coexists	|| G_HYG (A3); V48, V57
606	H	L1076	[]	there is no hook to lose and no post-hop blind spot	|| 
607	S	L1090	[]	- **RULE_RETIRED**: the rule no longer exists in v5f; a regression case pins the successor behaviour.	|| 
608	S	L1095	[Blockers (6)]	FIXED | Coherence on every visited function link, F_T included.	|| 
609	S	L1095	[Blockers (6)]	Residual R05b covers `'<'` or forged filenames (deliberate only). | X26b, X26c, X26d, X26e, X26f;	|| 
610	S	L1096	[Blockers (6)]	No threading lock and no profile or trace function anywhere.	|| 
611	old	L1096	[Blockers (6)]	Transitions exclude each other through the robust mutex (dead owners stolen, REENTRANT, MACHINERY_BUSY), and the callbacks never run at a C call. 3.11 is refused because its only mechanism re-creates the user-code form of this bug. | H6, H7, X138, X139, X140	|| revision 7 audit
612	S	L1097	[Blockers (6)]	Step 4, L-CACHE and "nothing is unwrapped" are corrected. | X24b, X24c	|| 
613	S	L1099	[Blockers (6)]	CUT_MOVED is checked at every entry event of minted code as well as at exit, so a transient rebind is caught.	|| 
614	S	L1099	[Blockers (6)]	Residual R18 (deliberate only). | X34, X34b, X34c, X35, X35b, X35c, X65b, X65c, X65d, X65e, X65f, X65g, X141, X141b, X141c, V44, V61, V62, V63, V64;	|| 
615	S	L1100	[Blockers (6)]	REFUSED_NOW | UNSUPPORTED_VERSION below 3.12. v5f installs no Python-level profile or trace function on any version. | X37 (3.10/3.11)	|| 
616	S	L1105	[Spec-false (7)]	Task-aware NESTED was rejected because it would change the rule for no soundness gain. | X83, V35 (unkeyed)	|| 
617	S	L1107	[Spec-false (7)]	pep562-message-no-module	|| 
618	S	L1107	[Spec-false (7)]	FIXED | The FOREIGN_DEFINITION message names the innermost `module:qualname` reached, using descriptor reads only. | X29b	|| 
619	S	L1109	[Spec-false (7)]	FIXED | `_finite` catches OverflowError only. score raises a GateSpecError. smoke is read with exact types. | X117b, X117c	|| 
620	S	L1111	[Spec-false (7)]	The only window left is before exit's claim (L-ZOMBIE), and `__exit__` again completes it.	|| 
621	S	L1118	[Defects (15)]	dict-subclass-result-no-trace	|| 
622	S	L1123	[Defects (15)]	Entry CODE_SWAPPED fires only against live holders. | X92b	|| 
623	S	L1124	[Defects (15)]	FIXED | "smoke run" only when the trace is absent; otherwise the note starts with the code. | X117d	|| 
624	S	L1125	[Defects (15)]	FIXED + DISCLOSED_LIMIT | Descriptor reads with no handler.	|| 
625	S	L1125	[Defects (15)]	Only the import and PEP 562 keep `except Exception`, as a chained UNRESOLVED (L-ASYNC-EXC). | X131	|| 
626	S	L1126	[Defects (15)]	No per-thread registry exists.	|| 
627	S	L1126	[Defects (15)]	Every detacher now pops by the anchor's own key, and a reconciliation sweeps anchors of dead cores, which also bounds the two-fault case. | X132, X146, X146d, X147, G_FI	|| 
628	S	L1127	[Defects (15)]	FIXED | The cut is validated and asyncio imported before resolution; nothing is imported in a transition.	|| 
629	S	L1130	[Defects (15)]	FIXED | Real-type branching with `is` comparisons only, and C-descriptor reads.	|| 
630	S	L1135	[Note (1)]	LAZY_RESULT fires for exact types only, with the new text and the unstarted suffix. | X76b, X74d	|| 
631	S	L1146	[Exam holes (29)]	X34 plus the `cut_current` invariant checked in every case.	|| 
632	S	L1153	[Exam holes (29)]	examhole-inherited-direct-base-only	|| 
633	S	L1158	[Exam holes (29)]	EXAM_CASE_ONLY (clearing no longer exists)	|| 
634	S	L1165	[Exam holes (29)]	exam-mut-foreign-profiler-first-open-only	|| 
635	S	L1167	[Exam holes (29)]	exam-mut-profiler-lost-only-if-none	|| 
636	S	L1175	[Round-4 items outside the 58]	Closed structurally: there is no lock, and G_HYG's lint forbids a loop in any machinery `try` body, except `_run_async`'s one `await`, whose skippable `finally` nothing depends on (M7, p19).	|| 
637	S	L1178	[Round-4 items outside the 58]	No code change.	|| 
638	S	L1184	[]	No row moves from CLOSED to open.	|| 
639	S	L1189	[]	The cut holds every `Handle._run` and `BaseEventLoop._run_once` code seen (revision 2).	|| 
640	S	L1189	[]	CUT_MOVED is checked on both bindings at every hit.	|| 
641	S	L1189	[]	The one route left is the deliberate-only residual R18: re-entrant same-loop dispatch through code that is neither cut code (X34, X65b–X65g, X141b, X141c, V44, V63, V64).	|| 
642	old	L1190	[]	styxx never calls `setprofile`, so no profiler is ever called, chained, replaced or removed (V33, V48, V57–V60).	|| revision 7 audit
643	old	L1190	[]	Among sys.monitoring tools, styxx adopts only an id that carries its own per-copy name, and never changes events or local events on an id that lost that name (X137, X142, X154).	|| revision 7 audit
644	S	L1192	[]	CLOSED_P | Real-type branching with `is` comparisons (no tuple membership: V40b) and C-descriptor reads.	|| 
645	S	L1192	[]	User code runs only in the import and PEP 562, and any Exception there becomes a chained UNRESOLVED (V40, X17b, X131, X10–X13b).	|| 
646	S	L1193	[]	CLOSED_P | The exact type is tested before every hash or compare (X96–X108, X103b, X103c, X96d).	|| 
647	S	L1194	[]	There is no hook.	|| 
648	S	L1194	[]	The leftover check requires `sys.getprofile()` and `sys.gettrace()` unchanged on every case thread.	|| 
649	S	L1194	[]	A zombie exists only after a fault has propagated out of the harness's own `with`;	|| 
650	S	L1194	[]	An open racing its own trace's exit registered `_ANCHORS[None]`, which no later close could pop, so the process-wide PY_UNWIND callback outlived the completed exit for the rest of the process.	|| 
651	S	L1194	[]	Closed by the owner-only release of `o.frame`, step 8's re-check after the store, and the reconciliation's prune of cores reached from anchors (At open, steps 8 and 9;	|| 
652	S	L1194	[]	`rev4/m1_pairs_modelcheck.py`: no anchor left at any fault-free end state of any pair or triple.	|| 
653	S	L1196	[]	CLOSED_S | Callbacks have no handler (H2 with its mutant).	|| 
654	S	L1197	[]	No locks exist.	|| 
655	S	L1197	[]	The robust mutex refuses REENTRANT instead of waiting, and callbacks take nothing (H1 with its mutant, X139).	|| 
656	S	L1198	[]	CLOSED_S | No Python-level profile or trace function exists, so there is no trampoline.	|| 
657	S	L1198	[]	The only `f_locals` reads are `_alive`'s, in the mutex's liveness check on contention and in reconciliation's holder test.	|| 
658	S	L1198	[]	Revision 5 removes revision 4's third caller, the opener's wait, so no open or close ever reads `f_locals` (N5).	|| 
659	S	L1199	[]	CLOSED_S | Nothing is chained.	|| 
660	S	L1201	[]	CLOSED_P | `__globals__` identity plus code/file coherence on every visited link (X26–X30, X26b–f).	|| 
661	H	L1208	[]	- **Runner change.** The only change is the v5e `cov.run(...)` line, and the interpreter is now a verified one (3.12.3 or 3.13.12).	|| 
662	H	L1210	[]	Every one of the 12 calls returns, so all 12 are confirmed.	|| 
663	D	L1219	[]	With `eager_task_factory`, only a child's first synchronous step runs on the parent's stack (X82).	|| X82
664	old	L1220	[]	**Event loops must run outside the section.** `cov.run(name, asyncio.run, main())` counts nothing (X73).	|| revision 7 audit
665	D	L1220	[]	Every task step and callback of a loop started inside the section fails the loop boundary; with the stdlib loop it is also beyond the cut.	|| X73, V63
666	D	L1222	[]	**No same-tracer nesting on one stack above the cut or a loop boundary** (NESTED_SECTION).	|| V15b, X76b
667	D	L1223	[]	Every declaring gate scored against that trace refuses, including unrelated gates.	|| X92c, X32
668	D	L1230	[]	- (revision 6, M5) every 3.12 and 3.13 patch level other than 3.12.3 and 3.13.12, until G_ATOM has passed on it and it is added to `_VERIFIED` (M0).	|| X37b
669	D	L1231	[]	From revision 7 (M3) this also covers a replacement of those `itertools`, `operator` or builtin objects installed later and then picked up by `importlib.reload(styxx.protocol)`, which re-binds them: every `coverage_trace()` after that reload refuses.	|| X156c
670	D	L1232	[]	- (revision 7, N2; a scope note, not a refusal) nothing refuses a different *build* of 3.12.3 or 3.13.12; such a build is accepted but its premise is unverified (M0).	|| G_ATOM build record (id 56)
671	P	L1234	[]	Scoring works on every version.	|| X37 on 3.10 and 3.11
672	D	L1239	[]	While the id has no owner, styxx's next transaction re-takes it and clears its events (revision 2).	|| X137 free variant
673	D	L1239	[]	A section opened after the free, before the re-take, gets no PY_UNWIND, so its calls that raise are not counted.	|| X137f, X157c
674	P	L1241	[]	If styxx's id is taken while traces are live, the next enter rebinds to the other id, and every trace live at the rebinding notes MONITOR_LOST (E4).	|| X137g
675	D	L1242	[]	**Declarable objects.** Declarable: a FunctionType; a staticmethod or classmethod of one; a C `lru_cache`/`cache` wrapper whose real callee is its own-dict `__wrapped__`, with that callee not bound by name in the module or holder and no sibling wrapper of it there.	|| X24b-X24e, V05, V38
676	D	L1249	[]	**Remedy:** declare `'<module>:<name>.__wrapped__'`, which counts every execution of the body;	|| X24b-X24e, V05, V38
677	D	L1253	[]	- Refused as in v5e: re-exports (declare the defining module); products of a no-`wraps` decorator or factory from another module; attrs- and namedtuple-generated methods whose globals are private; functions exec'd into private namespaces; autospec mocks and spies installed before the trace;	|| v5e's refused cases
678	C	L1257	[]	A zip holding only source is coherent on both versions (`relp/t_zip.py`).	|| relp/t_zip.py
679	D	L1259	[]	- a wrapper defined in an IPython/Jupyter cell, where `__main__` has no `__file__`, bound around a declared function.	|| X26e
680	D	L1265	[]	**A section open at trace exit** counts only calls confirmed before the exit's credit stop.	|| X71c
681	D	L1267	[]	**Child processes are invisible.** In a forked child, `run()` passes through: no section, no credit, no refusal.	|| V47
682	old	L1267	[]	A tracer inherited across fork cannot exit in the child (a no-op), and its `record()` refuses TRACE_INCOMPLETE there.	|| revision 7 audit
683	D	L1269	[]	A global PY_UNWIND callback runs for every frame that unwinds by exception, anywhere in the process, while any section is open (revision 2; revision 1: while any mint was registered).	|| H10
684	P	L1270	[]	**Confirmation.** A call counts only if its frame returns, yields, or unwinds from inside its body while the tracer is active.	|| X135, H8
685	old	L1279	[]	Frozen objects dying and `gc.unfreeze()` only lower the count, so they never trigger it (p4).	|| revision 7 audit
686	D	L1283	[]	The audit-hook sites inside a transaction are `__code__` writes and, as revision 6 corrects (B1), `sys.monitoring.register_callback`, which X5 raises five times on every exit: a hook on that event that takes such a lock closes the cycle at every exit, not only at mints and retires.	|| corrected in revision 8 (W5), as id 556  [FIX W5]
687	D	L1284	[]	- A section opened concurrently with the tracer's exit either refuses TRACE_INACTIVE, which is recorded and refuses the whole trace, or opens and counts only the calls confirmed before exit's credit stop, with end `"returned"`, `"raised"` or `"open"` (OPEN_AT_EXIT).	|| X146, X146b, X90
688	H	L1284	[]	The TRACE_INACTIVE entry reaches the record only once the racing thread has finished, so a harness should join such threads before `record()`.	|| 
689	H	L1284	[]	Revision 3 could also leave `_ANCHORS[None]` registered for the rest of the process on this race (B1), and could note a false MONITOR_LOST (MF2).	|| 
690	P	L1285	[]	- (Revision 5) Nothing waits at open.	|| V68
691	D	L1287	[]	- CUT_MOVED: a harness or library that rebinds `Handle._run` or `BaseEventLoop._run_once`, or reassigns either's `__code__`, during a trace refuses every declaring gate.	|| V62
692	D	L1287	[]	A rebind and restore with no hit in between is accepted (V62).	|| V62
693	D	L1289	[]	While it is enabled, every `coverage_trace()` in the process refuses.	|| critic3/c10_aiodebug_overblock.py
694	D	L1289	[]	For this shape the refusal buys no soundness: dispatch still passes the original `Handle._run` code and `_run_once`, both cut codes, so without the check the X65c shape is still `dispatched` (`critic3/c10_aiodebug_overblock.py`, both versions).	|| critic3/c10_aiodebug_overblock.py
695	old	L1291	[]	If a function sharing either code is alive then, such as `FunctionType(Handle._run.__code__, g)` built by a module imported at start-up, that code is "shareable" and never enters the cut, and every `coverage_trace()` in the process refuses CUT_UNAVAILABLE for as long as the clone lives (`critic3/c9_	|| revision 7 audit
696	H	L1291	[]	**Remedy:** create such a function only after the first `coverage_trace()` of the process, when the codes are already cut codes;	|| 
697	D	L1292	[]	- **Order dependence (MF4).** A code stays a cut code for the rest of the process, and E2 scans only codes new to the cut.	|| X34b (harness rule)
698	P	L1298	[]	- #6 foreign profilers: styxx calls neither `sys.setprofile` nor `sys.settrace`, so every such tool is expected to coexist.	|| G_HYG (A3); V48, V57
699	P	L1299	[]	- #7 hook loss through `setprofile(None)`, RecursionError or signals: no hook exists.	|| G_HYG (A3); V48, V57
700	D	L1307	[]	Only declared gates are checked.	|| X121, X78g (declared-section scoring)
701	D	L1312	[]	- non-asyncio schedulers (trio, Twisted, a thread's own work queue, and any loop that does not register itself through `_set_running_loop`), which set no running asyncio loop;	|| X82, X83, V35
702	H	L1315	[]	Any asyncio loop, uvloop included, is no longer here.	|| 
703	D	L1315	[]	The one asyncio route left is R18, deliberate only.	|| R18
704	P	L1324	[]	It checks that every visited function's `co_filename` names its own module's file, or is a generated `'<…>'` filename.	|| X26b-X26d
705	D	L1327	[]	- a stub whose code has a `'<…>'` filename, or whose `co_filename` was forged to the module's file, bound or swapped in before the trace (R05b; deliberate only).	|| R05b
706	D	L1328	[]	Every execution of the body counts, whatever route reaches it: a direct call, `__wrapped__`, a reference captured before decoration (R07), or a miss in a sibling cache wrapper of the same body held outside the module and holder namespaces (R13).	|| R07, R13
707	P	L1329	[]	- **L-ASYNC-EXC (rewritten as the envelope I6).** A fault anywhere in the machinery can only do these:	|| G_FI C8
708	C	L1336	[]	For a module already in `sys.modules` and not initializing, `import_module` takes no module lock.	|| CPython importlib `_find_and_load` fast path (reading; a remedy, not relied on)
709	D	L1338	[]	- while a mint is registered: every entry (PY_START/PY_RESUME), return and yield of a declared target;	|| H10; model STALE
710	D	L1339	[]	- while *some section is open* anywhere in the process (revision 2; revision 1: while any mint existed): every exception unwind of any Python frame, on every thread.	|| H10; model STALE
711	D	L1339	[]	From revision 5 there is only one: after a fault at a close's first instructions, or a `_run_async` `finally` skipped by #130279, a dead anchor keeps the event set.	|| X140; U4
712	C	L1344	[]	- **At any frame's unwind:** it replaces the exception being unwound, and on 3.12.3 and 3.13.12 its `__context__` is None, so the original exception is lost (synth/p_deliver.py).	|| synth/p_deliver.py
713	P	L1346	[]	No C call is ever skipped, because styxx enables no C-call event and installs no profile or trace function.	|| G_HYG event-name clause and A3; H7
714	H	L1346	[]	D1 measured 0 user-lock leaks in 51,075 and 52,456 SIGALRM timeouts with its PY_START/PY_RESUME-only adapter.	|| 
715	C	L1349	[]	- Untraced CPython has no eval-breaker check between a `raise` and the matching `except` clause.	|| H10 (untraced cell)
716	old	L1349	[]	A pending asynchronous exception therefore takes effect only after the handler has started, and the handler's first instructions, up to its first eval-breaker check, always run.	|| revision 7 audit
717	C	L1352	[]	- Exception chaining is lost: the new exception's `__context__` is None.	|| synth/p_deliver.py
718	C	L1353	[]	- CPython documents no guarantee that a handler starts before a pending asynchronous exception takes effect, but untraced programs can depend on it, and the second critic's probe does.	|| H10 (untraced cell)
719	H	L1360	[]	trace active, no section open: revision 1	|| 
720	H	L1361	[]	trace active, no section open: revision 2	|| 
721	H	L1364	[]	That breaks code that the language's own `with`/`finally` rules make correct under asynchronous exceptions, and it reached every thread with a section.	|| 
722	P	L1364	[]	L-DELIVERY keeps every `with`/`finally` guarantee.	|| H10 cells
723	P	L1364	[]	From revision 2 it does so only while a section is open.	|| H10 cells
724	G	L1367	[]	- (a) an `except ValueError` flow in undeclared code, run while a trace is active and no section is open: gated at **0** lost handlers, the untraced value.	|| 
725	G	L1367	[]	The cell's trace opens and closes one section, fault-free, before the flood is armed, and opens none while it is armed.	|| 
726	G	L1367	[]	The flood's handler raises only while the flow runs, so no signal lands in the machinery during the cell.	|| 
727	G	L1367	[]	After a fault-free close no stale event exists (U3), and `_v5_state()["global_events"] == 0` is asserted before arming.	|| 
728	G	L1371	[]	Every other G_SIG harness flow uses only `with`/`finally` for cleanup and checks results only on iterations in which no asynchronous exception was delivered.	|| 
729	H	L1373	[]	**The alternative, not taken.** First-LINE confirmation would need no global event at all, at the cost of a LINE callback per line executed in declared functions.	|| 
730	P	L1379	[]	The process-wide PY_UNWIND callback stays only while one of its sections is still open (revision 2).	|| model STALE; H10 cell (a)
731	P	L1382	[]	- any section opening anywhere in the process finds it clear and notes every armed, registered opening in the same call in which it sets it again (`_unwind_on`;	|| X137c
732	D	L1385	[]	The loss is **silent** only when the party that changed something restores it before any of these reads: it clears PY_UNWIND and sets it again itself, or it clears a mint's local events or replaces a callback and restores it before exit.	|| X158 (A12)
733	P	L1385	[]	An exit's registration that repairs a replaced callback counts the repair in `_LOST` (revision 7, B1), so every trace live then is noted.	|| X158, X158e
734	C	L1386	[]	- **`free_tool_id` does not blind.** On 3.12.3 and 3.13.12 it only removes the name and leaves events and callbacks in place (`rev1/p3_free.py`), so counting continues.	|| rev1/p3_free.py; X137 free variant
735	D	L1386	[]	A trace live then that lost nothing carries no note (X157b).	|| X157b
736	P	L1387	[]	Afterwards no styxx callback outlives the completed exit (`rev2/p3_free_reclaim.py`).	|| X137 free variant
737	old	L1388	[]	- **An id another tool took is not written, with one bounded exception (revision 6 narrows revision 5's claim).** styxx changes no event or local event there, so it cannot clobber the new owner's events.	|| revision 7 audit
738	old	L1388	[]	Every such write is a one-call step gated on the name, so a tool that frees and re-takes the id between two of styxx's statements is never written to (the fifth critic's N1;	|| revision 7 audit
739	P	L1388	[]	The owner read right after it reports the other tool's name, and the registration is treated as incomplete: the exit that made it notes MONITOR_LOST, as does every trace live then (M7, "The registration").	|| X154b
740	D	L1388	[]	At a first acquisition or an adoption no trace is live.	|| X154c
741	D	L1388	[]	The report there only moves E4 to the other id, and the replaced callback is surfaced nowhere (revision 7, N3;	|| X154c
742	P	L1388	[]	Registrations happen at a first acquisition, a reclaim, a rebinding and every exit's X5 (Exception-safety, Sources).	|| G_HYG call-site clause
743	P	L1389	[]	- **Races.** Revision 5: none raises.	|| X154 sweeps
744	P	L1389	[]	`_take` takes an unowned id in one call, and no write is attempted on an id styxx does not hold (the Tool acquisition prefix;	|| G_ATOM _take; X154
745	D	L1392	[]	The only exception is L-ZOMBIE.	|| X92c
746	D	L1393	[]	- **Diagnostic buckets are partial.** `uncredited` counts confirmed, uncredited hits on every thread while the tracer is crediting and some section is open somewhere in the process (revision 2: a hit while no section is open stores nothing).	|| X123, V67
747	D	L1394	[]	- **Scope of testing.** CPython 3.12.3 and 3.13.12, GIL builds, and of those the builds G_ATOM ran on (revision 7, N2: another build of the same patch level is accepted by M0 but unverified); from revision 6, tracing refuses every other patch level (M0, `_VERIFIED`).	|| G_ATOM build record
748	D	L1394	[]	Greenlet switches are covered by the model check only (`rev5/m5_modelcheck.py`).	|| X137f, X158d; model. Corrected in revision 8 (W6): the text said "the model check only" and "X137f's one shape"  [FIX W6]
749	P	L1394	[]	A switch made from inside a transaction defeats the mutex's liveness test, so two transactions can interleave, and the model finds no property violated when they do.	|| model greenlet configurations
750	old	L1394	[]	A switch cannot land inside a one-call step, so the event path has no greenlet exposure.	|| revision 7 audit
751	G	L1401	[Exam harness rules (frozen wit]	On any other 3.12 or 3.13 patch level every tracing case would refuse UNSUPPORTED_VERSION, so such an interpreter is not an exam interpreter;	|| 
752	old	L1401	[Exam harness rules (frozen wit]	On 3.10.20 and 3.11.15 (revision 8, N8: pinned by patch level, with their builds recorded like the verified ones') the runner executes X37 and every scoring-only case, and those outcomes must equal the verified interpreters' ones.	|| revision 7 audit
753	G	L1401	[Exam harness rules (frozen wit]	The scoring-only cases read prebuilt `/3` traces.	|| 
754	G	L1406	[Exam harness rules (frozen wit]	- Every case that rebinds `Handle._run` or `BaseEventLoop._run_once`: X35, X35b, X35c, X65e–X65g, X141, X141b, X141c, V61, V62, V64, R18 and (revision 7) R20.	|| 
755	G	L1407	[Exam harness rules (frozen wit]	- Revision 2 (M6): every case that uses the id-5 injector on a machinery function, which is X92b, X131 and X132.	|| 
756	old	L1407	[Exam harness rules (frozen wit]	Before the self-trace nothing is minted, so `_v5_faultpoints()` returns the code that runs, and the self-trace's own calls cannot be faulted.	|| revision 7 audit
757	G	L1408	[Exam harness rules (frozen wit]	- Revision 3 (MF3): every case whose premise is that no other section is open anywhere in the process: X137c, X143, X143b, X143c, X144, X145 and the H10 cells.	|| 
758	G	L1408	[Exam harness rules (frozen wit]	`_ANCHORS` is process-global, so no `_unwind_off()` clears the event there, and a witness of the toggle protocol scores the same under the spec and under its mutant (`critic3/c6_x143_inside_selftrace.py`;	|| 
759	G	L1408	[Exam harness rules (frozen wit]	Revision 4 adds X146, X146b, X146c, X146d, X147 and X148, whose outcomes read `anchors` or `global_events`, or need exit or a reconciliation to run while no other section is open.	|| 
760	G	L1408	[Exam harness rules (frozen wit]	Revision 7 adds V67, whose premise is that no anchor is registered anywhere.	|| 
761	G	L1409	[Exam harness rules (frozen wit]	Their subprocess starts no thread before it forks.	|| 
762	G	L1409	[Exam harness rules (frozen wit]	With the fork context, `ProcessPoolExecutor` launches all its workers before it starts its manager thread (3.12, 3.13), so every fork happens in a single-threaded process.	|| 
763	G	L1409	[Exam harness rules (frozen wit]	V47 asserts that no multi-threaded-fork DeprecationWarning was raised.	|| 
764	G	L1410	[Exam harness rules (frozen wit]	The leftover check then requires `gc.get_freeze_count()` to be no higher than before the case.	|| 
765	G	L1410	[Exam harness rules (frozen wit]	Revision 3 (MF4) adds two rules, because the cut is process-monotone and every rebinding case runs in one process:	|| 
766	old	L1411	[Exam harness rules (frozen wit]	- *Fresh wrapper per case.* Every case that binds a function as `Handle._run` or `BaseEventLoop._run_once` uses a wrapper whose code object has never been bound, scanned or put in the cut in that process: a top-level `def` of its own in a fixture module, used by no other case, with no other function	|| revision 7 audit
767	G	L1411	[Exam harness rules (frozen wit]	Then X35c is accepted and X141 has no CUT_MOVED (`critic3/c8_cut_order_dependence.py`); with fresh wrappers both keep their outcomes (`rev3/c8r3_fresh_wrapper_per_case.py`, both versions).	|| 
768	G	L1412	[Exam harness rules (frozen wit]	No fixture module may hold such a clone at import.	|| 
769	G	L1412	[Exam harness rules (frozen wit]	A clone alive at the first construction makes every later `coverage_trace()` in the process refuse CUT_UNAVAILABLE (`critic3/c9_x34b_fixture_poisons_first_e2.py`, #21).	|| 
770	G	L1414	[Exam harness rules (frozen wit]	- **How cases pass.** A violation passes only if the refusal message *starts with* its expected `[V5:CODE]`.	|| 
771	G	L1414	[Exam harness rules (frozen wit]	A valid case passes only with exactly the listed union counts, notes and ends.	|| 
772	G	L1414	[Exam harness rules (frozen wit]	A residual case passes only with its documented outcome.	|| 
773	G	L1415	[Exam harness rules (frozen wit]	- **What the exam may read.** The public API, fixture modules, `_v5_state()` and `_v5_faultpoints()`, and no other private name of `styxx.protocol`.	|| 
774	G	L1415	[Exam harness rules (frozen wit]	Machinery functions are named only by the frozen `_v5_faultpoints()` keys (M10).	|| 
775	G	L1415	[Exam harness rules (frozen wit]	It alone may also read the frozen *G_ATOM interface* (M10, revision 7), and no exam case may.	|| 
776	G	L1416	[Exam harness rules (frozen wit]	- **Leftover checks after every case.** The snapshots are taken *outside* the case's self-trace section (revision 3, MF3).	|| 
777	G	L1416	[Exam harness rules (frozen wit]	With no section open at either snapshot, a fault-free run has `anchors == 0` and `global_events == 0` at both (U3).	|| 
778	G	L1420	[Exam harness rules (frozen wit]	- every fixture function's `__code__` is its original;	|| 
779	G	L1423	[Exam harness rules (frozen wit]	The harness compares a tool's name first by identity with the pre-case object, and by `==` only when `type(name) is str`.	|| 
780	G	L1423	[Exam harness rules (frozen wit]	So the check itself runs no foreign `__eq__` (revision 5, N7: X142b's name is a str subclass whose `__eq__` counts its calls, and X142b reads that counter before the leftover check as well).	|| 
781	G	L1424	[Exam harness rules (frozen wit]	- **Threads named by ident (revision 5, MF3).** Any case that names a thread by its ident, for a filter, a hold or a state read, keeps that thread alive, parked on an Event, until every check of the case that uses the ident is done.	|| 
782	G	L1424	[Exam harness rules (frozen wit]	Every case below states which thread performs each step and each read.	|| 
783	G	L1425	[Exam harness rules (frozen wit]	The case passes only if every counted trial gives the case's outcome.	|| 
784	old	L1425	[Exam harness rules (frozen wit]	No timeout decides anything: the interference is a sequence of complete transitions that never waits for T.	|| revision 7 audit
785	G	L1425	[Exam harness rules (frozen wit]	The sweep places the interference between every two consecutive instructions of F, which is where any instrument, signal handler, finalizer or thread switch could run code.	|| 
786	G	L1425	[Exam harness rules (frozen wit]	So the sweep is the witness for every rule that says "this read and this write are one call" (the revision-5 row of the mutation audit).	|| 
787	G	L1426	[Exam harness rules (frozen wit]	It takes id 3 only for the duration of each case that uses it and frees it afterwards.	|| 
788	G	L1426	[Exam harness rules (frozen wit]	Before freeing id 3 or id 5, the harness sets the id's global events and every local event it set to 0, and registers None for every event it registered a callback for.	|| 
789	G	L1426	[Exam harness rules (frozen wit]	That is harmless here: the machinery has no `finally` or `with` across a loop other than M7's `await`, and nothing depends on that.	|| 
790	old	L1426	[Exam harness rules (frozen wit]	Revision 5 tolerates that everywhere, because no callback can run inside a one-call step (M7), so the tool is not an instrument the machinery must be protected from.	|| revision 7 audit
791	G	L1429	[Exam harness rules (frozen wit]	- *Thread filter.* It faults only events on the case's own thread.	|| 
792	G	L1430	[Exam harness rules (frozen wit]	- *No settrace.* A settrace function active over a code object silences other tools' INSTRUCTION events on that code, and they stay silent after `settrace(None)` until `sys.monitoring.restart_events()` (`rev2/p5b_settrace_blinds_injector.py`, 3.12.3 and 3.13.12).	|| 
793	G	L1430	[Exam harness rules (frozen wit]	G_COVER has no settrace pass (revision 3, MF6).	|| 
794	old	L1432	[Exam harness rules (frozen wit]	- *No other instruction-level tool (revision 3, N5).* While the injector is armed, no other sys.monitoring tool has LINE, INSTRUCTION, JUMP or BRANCH events on a machinery code object, unless it never returns `DISABLE` for machinery code.	|| revision 7 audit
795	G	L1432	[Exam harness rules (frozen wit]	A LINE tool returning `DISABLE`, as coverage.py's sysmon core does, makes the injector miss 3 of 17 (3.12.3) and 3 of 15 (3.13.12) offsets on a code object's first execution; one returning None misses none (`critic3/c3_line_disable_blinds_injector.py`).	|| 
796	G	L1432	[Exam harness rules (frozen wit]	V50, which runs coverage.py's sysmon core, uses no injector.	|| 
797	G	L1442	[v5e cases whose outcome change]	NOT_EXERCISED, message contains PROFILER_LOST | retired; replaced by V49: PASS `{f:1}`, no note	|| 
798	G	L1442	[v5e cases whose outcome change]	no hook to lose	|| 
799	G	L1445	[v5e cases whose outcome change]	`{f:1}` + PROFILER_LOST | `{f:1}`, no note	|| 
800	G	L1446	[v5e cases whose outcome change]	`{f:1}` + THREAD_HOP | `{f:1}`, no note	|| 
801	G	L1447	[v5e cases whose outcome change]	retired (no epochs);	|| 
802	G	L1448	[v5e cases whose outcome change]	`getprofile()` None after one event	|| 
803	G	L1449	[v5e cases whose outcome change]	3.12+ only	|| 
804	G	L1451	[v5e cases whose outcome change]	no hook exists	|| 
805	G	L1453	[v5e cases whose outcome change]	No exact `_run` count: a case's own loops make some `_run` calls fail the self-trace's loop boundary	|| 
806	old	L1460	[v5e cases whose outcome change]	A handler tripped from inside a monitoring callback cannot be used: code it runs raises no monitoring events (`rev1/p16_handler_events.py`).	|| revision 7 audit
807	G	L1460	[v5e cases whose outcome change]	X71c passes iff some trial lands and no landed trial credits f to G.	|| 
808	G	L1460	[v5e cases whose outcome change]	It fails if no trial lands in 20, or if any landed trial credits G.	|| 
809	G	L1460	[v5e cases whose outcome change]	In `rev2/p7_x71c_landing.py`, 1,000 openings landed in only 6 of 10 trials on 3.12.3, which is the critic's point. 10,000 and 100,000 landed in 10 of 10 on both versions, and the M10 mutant (credit stop after detach) credited G in every landed trial	|| 
810	G	L1464	[v5e cases whose outcome change]	the `prev is not P._hook` restore is deleted; no hook exists	|| 
811	G	L1466	[v5e cases whose outcome change]	Every other v5e case keeps its id and its expected outcome.	|| 
812	N	L1477	[New violation cases (expected ]	round-4 repro: `power = functools.wraps(power_ref)(lru_cache(None)(fast))`, with `power_ref` reachable only as `Ref.power`, so `power.__wrapped__` is not the function the wrapper calls	|| 
813	N	L1481	[New violation cases (expected ]	`Scorer.fast = lru_cache(None)(_impl)`, bare and as a staticmethod, with `_impl` bound at module level	|| 
814	N	L1482	[New violation cases (expected ]	a cache wrapper served only through PEP 562 (holder None) whose body is bound at module level	|| 
815	N	L1486	[New violation cases (expected ]	declared module built with `types.ModuleType`, with no `__file__`, registered in `sys.modules`, whose function has a `'<m>'` filename	|| 
816	old	L1489	[New violation cases (expected ]	a `__wrapped__` chain link whose class defines a Python `__dict__` property (never called), with no accepting link before it	|| revision 7 audit
817	N	L1494	[New violation cases (expected ]	`Handle._run` replaced by a MagicMock around `__enter__`; nothing minted afterwards	|| 
818	N	L1497	[New violation cases (expected ]	MONITOR_BUSY; nothing minted	|| 
819	N	L1498	[New violation cases (expected ]	X37 (3.10/3.11 only)	|| 
820	N	L1501	[New violation cases (expected ]	nested tracers on f; the inner section calls only a same-globals clone, kept alive past the inner exit and dropped before the outer exit	|| 
821	N	L1506	[New violation cases (expected ]	inside a task of a running loop L, `cov.run('A', section)` where `section` calls `L._run_once()`, which runs a callback another task scheduled; the callback calls f (same loop at open and at the hit; only the cut separates them; p17)	|| 
822	N	L1507	[New violation cases (expected ]	a `SelectorEventLoop` subclass whose `_run_once` runs ready callbacks through `map(operator.call, …)`, with no `Handle._run` frame (p8), run inside section A and serving another thread's `call_soon_threadsafe` job that calls f	|| 
823	N	L1508	[New violation cases (expected ]	A: NOT_EXERCISED with `dispatched {f:1}`; no CUT_MOVED | M1 (rev. 2): `_run_once` in the cut	|| 
824	N	L1510	[New violation cases (expected ]	**After `__enter__`**, with no hit of a declared target before the section, the harness rebinds `Handle._run` to a module-level W that first restores the original binding and then runs the job.	|| 
825	N	L1510	[New violation cases (expected ]	If any other handle ran first, the job would be dispatched through the original `Handle._run`, and X65g would no longer witness its row	|| 
826	N	L1510	[New violation cases (expected ]	A: NOT_EXERCISED with `dispatched {f:1}`; no CUT_MOVED.	|| 
827	N	L1538	[New violation cases (expected ]	`Injected` propagates from `__enter__` (not a GateSpecError); nothing minted	|| 
828	N	L1539	[New violation cases (expected ]	the injector faults every line of `_open` and `_detach` in turn, on a worker thread	|| 
829	N	L1543	[New violation cases (expected ]	first trace: PASS `{f:1, g:1}` with MONITOR_LOST on every opening, because CPython 3.12.3 and 3.13.12 keep the freed id's events and callbacks (p3).	|| 
830	N	L1543	[New violation cases (expected ]	**Revision 2 (M3):** after that exit, `_v5_state()` shows `tool_ours` True and `global_events` 0, and no styxx callback runs for the 1,000 raises.	|| 
831	N	L1544	[New violation cases (expected ]	first trace: PASS `{f:2, g:1}` with MONITOR_LOST (styxx's local events and callbacks stay on the taken id, as in the free variant); after exit the other tool still owns the id, its global events are still RAISE and its RAISE callback is its own: styxx changed none of its events or callbacks, not at 	|| 
832	N	L1546	[New violation cases (expected ]	The waiter thread waits on `blocking`, sets `waiter_started` immediately before entering its tracer, and records the time (revision 2, M12 and N6: the block is measured from the waiter's start, so no timing is left open)	|| 
833	N	L1548	[New violation cases (expected ]	For every JUMP_BACKWARD, and every JUMP_BACKWARD_NO_INTERRUPT, instruction in every `_v5_faultpoints()` code object (found with `dis`), one trial.	|| 
834	N	L1548	[New violation cases (expected ]	**Revision 2 (M8).** The five callback-only functions (G_COVER's frozen list) raise no events while they run as callbacks.	|| 
835	N	L1548	[New violation cases (expected ]	No function that `_run`'s try body or `finally` calls has a back-edge (`rev5/spec_rev5_eventpath.py`).	|| 
836	old	L1548	[New violation cases (expected ]	The scenario runs to completion and never waits for the offset, so no trial can hang.	|| revision 7 audit
837	N	L1548	[New violation cases (expected ]	every trial is clean by G_FI's outcome rules.	|| 
838	N	L1548	[New violation cases (expected ]	A back-edge in `_exit` gives TRACE_INCOMPLETE, since M5 has no loop before X1.	|| 
839	N	L1549	[New violation cases (expected ]	No hang; leftovers clean after the next enter	|| 
840	N	L1552	[New violation cases (expected ]	Without the `_run_once` binding in `_cut_ok()`, f is credited to A with no refusal (`rev2/p1b_runonce_binding.py`, both versions)	|| 
841	N	L1553	[New violation cases (expected ]	`_v5_state()["tool"]` is 4 for A and 3 for B; no MONITOR_LOST in A | R1-B4 (c)	|| 
842	N	L1554	[New violation cases (expected ]	every trial: A PASS `{t:1}`, no MONITOR_LOST.	|| 
843	N	L1554	[New violation cases (expected ]	Without `_unwind_on` in `_commit`, t's unwind is not seen and A refuses NOT_EXERCISED with MONITOR_LOST (every trial).	|| 
844	N	L1555	[New violation cases (expected ]	A's end is `"raised"`; the trace's notes contain no MONITOR_LOST; after exit `anchors == 0` and `global_events == 0`.	|| 
845	N	L1556	[New violation cases (expected ]	Right after it, `global_events` is 0: from revision 5 the pop and the clear are one call, so no window follows the pop.	|| 
846	N	L1556	[New violation cases (expected ]	The trace PASS `{f:1}`, A's end `"returned"`, no MONITOR_LOST.	|| 
847	N	L1557	[New violation cases (expected ]	revision 3, MF2; main thread before the self-trace, no other section open.	|| 
848	N	L1557	[New violation cases (expected ]	After exit: A NOT_EXERCISED, end `"raised"`, no MONITOR_LOST; a later section PASS `{f:1}`.	|| 
849	N	L1557	[New violation cases (expected ]	With `_commit` before the `try` (revision 2's placement), `global_events` is PY_UNWIND with no anchor until the trace exits (`rev3/w1_rev3_witnesses.py`;	|| 
850	N	L1557	[New violation cases (expected ]	The only `_unwind_on` call is after the store, so the fault lands after the set	|| 
851	N	L1558	[New violation cases (expected ]	every trial: A PASS `{t:1}`, no MONITOR_LOST.	|| 
852	N	L1558	[New violation cases (expected ]	With the `_ANCHORS` test and the clear as two statements (revision 3's shape, or any split of `_unwind_off`'s one call), the trials that land between them lose t: A NOT_EXERCISED with MONITOR_LOST (`rev5/w5_witnesses.py` IS-OFF: 17 of 57 trials fail on 3.12.3, 20 of 60 on 3.13.12; the spec fails non	|| 
853	N	L1558	[New violation cases (expected ]	The sweep's callback is the instrument that split revision 3's form, so this case also shows that no instrument can split it.	|| 
854	N	L1559	[New violation cases (expected ]	The id-3 tool, PY_START on `_v5_faultpoints()['_commit']` filtered to T1, holds T1 there until the main thread's `cov.__exit__()` has returned (X3 claimed A while A had no anchor).	|| 
855	N	L1559	[New violation cases (expected ]	With `_detach` assigning `o.frame = None` (revision 3), `anchors == 1` after the join (`_ANCHORS[None]`).	|| 
856	N	L1559	[New violation cases (expected ]	owner-only release of `o.frame`; step 8 (rev. 4)	|| 
857	N	L1560	[New violation cases (expected ]	A is appended after X3's snapshot, so no detacher claims it	|| 
858	N	L1560	[New violation cases (expected ]	With step 8 testing only the claim (not `'exiting'`), `run()` returns f's value	|| 
859	N	L1561	[New violation cases (expected ]	T1's target function and `af` hold no reference to `cov`, and T1 receives only `co`.	|| 
860	N	L1561	[New violation cases (expected ]	With step 8 testing only `'exiting'`, `co` returns af's value	|| 
861	N	L1563	[New violation cases (expected ]	Every trial is one of: (i) T1's `run()` raised TRACE_INACTIVE and `record()` has the problem TRACE_INACTIVE; or (ii) `run()` returned f's value, `record()` has no problem, A's end is `"returned"` or `"open"`, and A counts at most `{f:1}`.	|| 
862	N	L1563	[New violation cases (expected ]	In every trial: after the join `anchors == 0`, `global_events == 0`, and no MONITOR_LOST (f returns, so every MONITOR_LOST here is false).	|| 
863	N	L1563	[New violation cases (expected ]	Revision 3 fails it: `_ANCHORS[None]` in 51–62 of 20,000 natural races per version, and a false MONITOR_LOST in 17–23 (`rev4/b1_open_races_exit.py`)	|| 
864	N	L1564	[New violation cases (expected ]	every trial: A PASS `{f:1}`, no MONITOR_LOST (f returns, so any note is false).	|| 
865	N	L1564	[New violation cases (expected ]	With `_detach` testing the anchor before the event (revision 3's order), the trials that land between the two reads flag `UNWIND_LOST`: MONITOR_LOST (`rev5/w5_witnesses.py` IS-BLIND: 15 of 62 trials on 3.12.3, 17 of 66 on 3.13.12; the spec fails none; revision 4's hold form: `rev4/a4r_detach_order.p	|| 
866	N	L1565	[New violation cases (expected ]	The fifth critic's MF3 (a new T1 reuses T2's ident and skips T2's dead token) is answered by the harness rule on threads named by ident, which every remaining case follows	|| 
867	N	L1569	[New violation cases (expected ]	revision 5 (the own-anchor gate in `_unwind_on`); main thread before the self-trace, no other section open.	|| 
868	N	L1569	[New violation cases (expected ]	Without the own-anchor gate, T1's `_unwind_on` sets the event with no anchor registered, and body reads `global_events == PY_UNWIND` with `anchors == 0` (`rev5/w5_witnesses.py`, both versions)	|| 
869	N	L1570	[New violation cases (expected ]	Q: PASS `{h:1}`, no MONITOR_LOST.	|| 
870	N	L1570	[New violation cases (expected ]	Without `_unwind_on`'s capture, B's `_unwind_on` sets the event again, G's close finds it set, and P has no MONITOR_LOST (`critic5/c7_external_clear_masked.py`;	|| 
871	N	L1571	[New violation cases (expected ]	`_v5_state()["tool_ours"]` is True. f returned before the free, so P lost nothing: this case witnesses the *note on a loss-free free* that the count gives, and nothing else.	|| 
872	N	L1571	[New violation cases (expected ]	With `_ensure_tool` re-taking its own freed id without `_reclaim` (without counting it), P has no MONITOR_LOST (`rev5/w5_witnesses.py`, both versions: `first_ML False` under the mutant).	|| 
873	N	L1573	[New violation cases (expected ]	revision 5, U3; main thread before the self-trace, no other section open.	|| 
874	N	L1573	[New violation cases (expected ]	no trial reads `anchors == 0` with `global_events != 0`.	|| 
875	N	L1573	[New violation cases (expected ]	With the pop as its own statement before the clearing call, the trials between them read it (`rev5/w5_witnesses.py` IS-POPOFF: 48 of 83 trials on 3.12.3; the spec fails none)	|| 
876	N	L1574	[New violation cases (expected ]	revision 5, MF2; main thread before the self-trace, no section open.	|| 
877	N	L1574	[New violation cases (expected ]	every trial: A PASS `{f:1}`, B PASS `{g:1}`, no MONITOR_LOST on either.	|| 
878	N	L1574	[New violation cases (expected ]	With the capture outside the one call that reads the event and sets it, the trials in between note B although nothing was cleared under it.	|| 
879	N	L1574	[New violation cases (expected ]	With the capture not gated on the event being clear, every trial notes B (`rev5/w5_witnesses.py` IS-ON: 20 of 83 and 130 of 146 trials on 3.12.3; the spec fails none of 156)	|| 
880	N	L1575	[New violation cases (expected ]	Four instruction sweeps, one per name-gated write function: `_unwind_on` (T1 opens A with no other section open), `_unwind_off` (T1 closes A, the last anchor), `_set_local` (the exit's `_retire`) and `_register` (the exit's MONITOR_LOST re-registration).	|| 
881	N	L1575	[New violation cases (expected ]	The interference frees styxx's id; another tool takes it, registers a RAISE callback, sets its global events to RAISE and its local events on the target's minted code to LINE | every trial: afterwards that tool's global events are exactly RAISE, its local events on that code exactly LINE, its RAISE 	|| 
882	N	L1575	[New violation cases (expected ]	With the name test split from the write (`if _named(t): set_events(...)`), the trials in between clear or replace that tool's events or register styxx's callbacks on its id (`rev5/w5_witnesses.py` IS-GATE: 92/122, 31/65, 12/25 and 67/85 failing trials on 3.12.3; the spec fails none;	|| 
883	N	L1575	[New violation cases (expected ]	Revision 6: the `_register` sweep witnesses only that its gates are inside its one statement.	|| 
884	N	L1575	[New violation cases (expected ]	every write is one call gated on the name (rev. 5)	|| 
885	N	L1576	[New violation cases (expected ]	The hook acts only on `sys.monitoring.register_callback` events raised on the case thread during that `__exit__`: on the k-th, it starts a thread that frees styxx's id (from `_v5_state()["tool"]`) and has another tool take it and register a callback of its own for each of the five events styxx uses,	|| 
886	N	L1576	[New violation cases (expected ]	After `__exit__` returns, the case reads the other tool's callbacks by *exchange and restore*: for each event, `c = register_callback(t, e, None)` then `register_callback(t, e, c)`, with the hook disarmed (sys.monitoring has no callback getter; the ten audit events this raises are outside every tria	|| 
887	N	L1576	[New violation cases (expected ]	every trial: `__exit__` returns; the trace PASS `{f:1}` with MONITOR_LOST; the other tool owns the id; its callbacks for the four events other than the k-th (in the order PY_START, PY_RESUME, PY_RETURN, PY_YIELD, PY_UNWIND) are its own, and its callback for the k-th event is styxx's.	|| 
888	N	L1577	[New violation cases (expected ]	`_v5_state()["tool"] == 3` after the enter; the other tool owns id 4, and its PY_UNWIND callback is styxx's (the one disclosed replacement); the trace PASS `{f:1}`, no MONITOR_LOST.	|| 
889	N	L1578	[New violation cases (expected ]	Then greenlet g2 runs a reconciling transaction (the enter of a tracer Y declaring g), and an audit hook, at the 5th `sys.monitoring.register_callback` event of g2's reclaim, switches to greenlet g1, whose parent is g2; g1 runs `X.__exit__(None, None, None)` to completion and returns to g2, which fi	|| 
890	N	L1578	[New violation cases (expected ]	With the close's read gated on the name (revision 4's form), X has no MONITOR_LOST: a silent loss.	|| 
891	N	L1578	[New violation cases (expected ]	Y PASS `{g:1}`, no MONITOR_LOST (`rev6/w6_greenlet.py` used `mech6.py`'s bare reconciliation as g2's transaction;	|| 
892	N	L1579	[New violation cases (expected ]	Q PASS `{f:1, g:1}` with no MONITOR_LOST.	|| 
893	N	L1579	[New violation cases (expected ]	Without the rebinding's local events for existing mints, f's hits after the rebinding are delivered on neither id: P and Q refuse NOT_EXERCISED for f (`rev6/w6_witnesses.py` ran P's half only, because `mech6.py` re-minted instead of joining;	|| 
894	N	L1579	[New violation cases (expected ]	`rev7/w7_witnesses.py` runs the whole case on `mech7.py`, which joins: spec P `{f:1}` with MONITOR_LOST and Q `{f:1, g:1}` with none; mutant P `{}` and Q `{g:1}`, both versions)	|| 
895	N	L1579	[New violation cases (expected ]	local events for every registered mint at a rebinding (rev. 6)	|| 
896	N	L1580	[New violation cases (expected ]	**revision 7 form** (revision 7's in-call count also notes the revision-6 shape, because the rebinding's registration on the fresh id 3 finds None callbacks, so the revision-6 shape no longer isolates the rebinding's count).	|| 
897	N	L1580	[New violation cases (expected ]	U registers only a RAISE callback, so styxx's five callbacks stay on id 4 (revision 8, N7: revision 7 called this tool "A", the name of R's section).	|| 
898	N	L1580	[New violation cases (expected ]	A's close found PY_UNWIND set on id 3, so it flagged nothing.	|| 
899	N	L1580	[New violation cases (expected ]	The rebinding's registration onto id 4 found styxx's callbacks, so nothing was counted in-call, and R's exit registers completely on id 4.	|| 
900	N	L1580	[New violation cases (expected ]	Only the rebinding's count notes the loss.	|| 
901	N	L1580	[New violation cases (expected ]	Without that count, R has no MONITOR_LOST: a silent loss (`rev7/w7_witnesses.py` `X137h7`, both versions).	|| 
902	N	L1580	[New violation cases (expected ]	The other three traces open no section.	|| 
903	N	L1580	[New violation cases (expected ]	S refuses NOT_EXERCISED for g with no MONITOR_LOST.	|| 
904	N	L1580	[New violation cases (expected ]	Without the rebinding's count, Q has no MONITOR_LOST either, and P keeps its note, which the in-call count of id 3's None callbacks also makes (revision 8, N7: revision 7 stated R's outcome only;	|| 
905	old	L1581	[New violation cases (expected ]	Only the `[V5:UNSUPPORTED_VERSION]` prefix is checked (revision 7, N6: revision 6 quoted a text, "set_events is not the C builtin", that the harness rules never compare).	|| revision 7 audit
906	N	L1583	[New violation cases (expected ]	UNSUPPORTED_VERSION (the code prefix only is checked); scoring works (`rev7/w7_witnesses.py`, both versions)	|| 
907	N	L1584	[New violation cases (expected ]	every trial: `__exit__` raises nothing and the trace notes MONITOR_LOST; if the other tool won, it keeps id 4.	|| 
908	N	L1584	[New violation cases (expected ]	With the unowned test and `use_tool_id` as two statements, the trials between them make the exit raise ValueError (`rev5/w5_witnesses.py` IS-TAKE: 13 of 26 trials on 3.12.3; the spec fails none of 28)	|| 
909	N	L1585	[New violation cases (expected ]	Then an outside party, which holds no tool id, replaces styxx's PY_START callback with its own: `register_callback(tool, PY_START, cb)`, `tool` from `_v5_state()`.	|| 
910	N	L1586	[New violation cases (expected ]	With no count (revision 6), Q is silent too (`rev7/w7_witnesses.py`, with a test hook at that point standing for the injector, both versions)	|| 
911	N	L1587	[New violation cases (expected ]	X158 with the outside party registering **None** over styxx's PY_START callback (`register_callback(tool, PY_START, None)`), both variants	|| 
912	N	L1587	[New violation cases (expected ]	A None previous callback is a removal, and the registration counts it like any other callback that is not styxx's.	|| 
913	N	L1587	[New violation cases (expected ]	With a registration that does not count None previous callbacks, neither P nor Q has MONITOR_LOST in either variant, and Q's lost call is silent (`rev8/w8_new.py` on `rev8/mech8.py`, mutant `count_excl_none`, both versions;	|| 
914	N	L1587	[New violation cases (expected ]	a None previous callback counted in `_LOST` (rev. 7; witnessed from rev. 8)	|| 
915	N	L1588	[New violation cases (expected ]	X158's variant (a), except that the outside party's replacement for styxx's PY_START callback is an instance of a class whose `__call__` returns None and whose `__del__` switches to a new greenlet g2, whose parent is the case's greenlet. g2 runs `Q.__exit__(None, None, None)` and returns.	|| 
916	N	L1588	[New violation cases (expected ]	The slot holds the only reference to the instance.	|| 
917	N	L1588	[New violation cases (expected ]	Q's exit steals the mutex, finds all five callbacks styxx's and `_LOST` unchanged, and Q has no MONITOR_LOST: a silent loss (`rev8/c1_on_mech8.py`, the `greenlet` rows, greenlet 3.5.6, both versions;	|| 
918	N	L1589	[New violation cases (expected ]	Both reclaims in Q's transaction (the reconciliation's and `_ensure_tool`'s) make one exchange and split with owner None.	|| 
919	N	L1589	[New violation cases (expected ]	Q PASS `{g:1}`, no MONITOR_LOST.	|| 
920	N	L1589	[New violation cases (expected ]	With the re-take not counted (revision 6's prototype rule), P has no MONITOR_LOST, and the free is noted by nobody. f returned before the free, so like X137e this witnesses the note on a loss-free free.	|| 
921	N	L1589	[New violation cases (expected ]	Q PASS `{g:1}`, no note.	|| 
922	N	L1589	[New violation cases (expected ]	Both the rebinding's count and the in-call count of the fresh id's None callbacks note P here, so (b) pins the outcome and isolates no rule;	|| 
923	N	L1590	[New violation cases (expected ]	With the close's read gated on the name (revision 4's form), P has no MONITOR_LOST: a silent loss (`rev7/w7_witnesses.py`, both versions;	|| 
924	N	L1591	[New violation cases (expected ]	With the check run only when `_MON` is first bound (revision 6), the reload binds the subclass, `coverage_trace()` succeeds, and every one-call step then calls it (`rev7/w7_witnesses.py`, both versions)	|| 
925	N	L1591	[New violation cases (expected ]	the binding check at every `coverage_trace()` (rev. 7)	|| 
926	N	L1592	[New violation cases (expected ]	With revision 6's name-only test, the subclass is bound and `_CONSUME`'s deque has `maxlen` None, so it keeps every pipeline value (`rev7/w7_witnesses.py`, both versions)	|| 
927	N	L1597	[New valid cases (expected unio]	a module whose `__class__` is a ModuleType subclass serves the target only through PEP 562	|| 
928	N	L1600	[New valid cases (expected unio]	B opens (no NESTED_SECTION: the cut lies between);	|| 
929	N	L1601	[New valid cases (expected unio]	No event loop is involved	|| 
930	N	L1601	[New valid cases (expected unio]	B's end is `returned`; no notes	|| 
931	N	L1603	[New valid cases (expected unio]	a benign `gc.freeze()` inside a section, with no clone	|| 
932	N	L1604	[New valid cases (expected unio]	PASS `{f:1}`, no CLONE_ALIVE.	|| 
933	N	L1604	[New valid cases (expected unio]	A worker that entered f before the trace runs the original code, adds no reference to M_T (excess 0), and would witness neither weakening (critic2/t_v36b.py, re-run in `rev2/out_m9_v36b.txt`: excess 0 before, 1 after)	|| 
934	N	L1608	[New valid cases (expected unio]	`{target:1}` each; no user side effects	|| 
935	N	L1617	[New valid cases (expected unio]	the harness calls `sys.setprofile(None)` and `sys.settrace(None)` before the target (replaces X75)	|| 
936	N	L1617	[New valid cases (expected unio]	`{f:1}`, no note	|| 
937	N	L1619	[New valid cases (expected unio]	a pdb/bdb settrace tracer active over the section (no `debug` command)	|| 
938	N	L1622	[New valid cases (expected unio]	a target whose every call raises inside its body, caught by the harness	|| 
939	N	L1625	[New valid cases (expected unio]	`{f:1}`; no note	|| 
940	N	L1627	[New valid cases (expected unio]	`{f:1, g:1}`, no note; the profiler saw g	|| 
941	N	L1628	[New valid cases (expected unio]	PASS `{f:1}`, no CUT_MOVED: E2 adds the wrapper's code to the cut.	|| 
942	N	L1629	[New valid cases (expected unio]	inside the trace, the harness rebinds `Handle._run` and restores it with no call of a declared target in between; then section G calls f	|| 
943	N	L1630	[New valid cases (expected unio]	B opens without NESTED_SECTION: A's loop is None, not the running loop, so A is not visible at B's open.	|| 
944	N	L1630	[New valid cases (expected unio]	A counts nothing from the loop	|| 
945	N	L1631	[New valid cases (expected unio]	outer PASS `{f:1}`, no CUT_MOVED.	|| 
946	N	L1634	[New valid cases (expected unio]	revision 7 (self-audit); main thread before the self-trace, no section open anywhere.	|| 
947	N	L1634	[New valid cases (expected unio]	A tracer declaring t, which raises out of its body, is entered; t is called and caught 100 times outside every section;	|| 
948	N	L1634	[New valid cases (expected unio]	With `_on_entry` storing a pending entry while no anchor is registered, `pending == 100`: each raise unwinds with PY_UNWIND off, so nothing pops them (`rev7/w7_audit.py` V67, both versions)	|| 
949	N	L1634	[New valid cases (expected unio]	M6: no pending entry while no anchor is registered	|| 
950	N	L1636	[New valid cases (expected unio]	PASS `{f:1}`, no MONITOR_LOST, and the counter is 0: `_MON` keeps the C function bound at the first binding.	|| 
951	N	L1637	[New valid cases (expected unio]	Tracer P declares f, whose body is `if box is not None: signal entered; wait go; raise KeyError`, then `return 1`.	|| 
952	N	L1637	[New valid cases (expected unio]	T2 calls `f(True)`, so f's entry is stored as pending with outcome `u`: f is under no anchor of P on T2.	|| 
953	N	L1637	[New valid cases (expected unio]	T2 catches and drops the exception, and at once calls `f(None)`, which returns.	|| 
954	N	L1637	[New valid cases (expected unio]	No anchor is registered at that entry.	|| 
955	N	L1637	[New valid cases (expected unio]	`calls {}`, `unattributed {}`, `dispatched {}`; no MONITOR_LOST.	|| 
956	N	L1640	[New documented residuals (pinn]	Outcome: PASS (deliberate only).	|| 
957	N	L1645	[New documented residuals (pinn]	That is a residual outcome changing before any prereg, so no prereg is affected.	|| 
958	N	L1646	[New documented residuals (pinn]	No cut frame lies between f and A's anchor; the running loop is `A.loop`; and no hit or exit sees a moved binding (`rev2/p1_runonce_cut.py`, both versions; deliberate only;	|| 
959	N	L1647	[New documented residuals (pinn]	- **X157 (revision 6, B1 item 5; pinned residual).** In a fresh subprocess: a tracer on f runs `cov.run('A', f)`; then an audit hook is installed that raises `Denied(Exception)` at the second `sys.monitoring.register_callback` event on the case thread, once; then `cov.__exit__(None, None, None)`.	|| 
960	N	L1647	[New documented residuals (pinn]	`_v5_state()["guard"]` is `"dead"`; a second tracer on g then PASS `{g:1}` with no MONITOR_LOST, and after its exit `guard` is `"free"`, `anchors` 0 and `global_events` 0 (`rev6/r6_b1_register.py`, `exit-raise`, both versions, on the model).	|| 
961	N	L1650	[New documented residuals (pinn]	Outcome: PASS `{f:2}` with no MONITOR_LOST.	|| 
962	N	L1651	[New documented residuals (pinn]	Outcome: NOT_EXERCISED for f; the copy is in no bucket (`dispatched {}`, `unattributed {}`); no problem is recorded.	|| 
963	N	L1651	[New documented residuals (pinn]	The copy carries no monitoring events (`rev7/w7_audit.py` R22, both versions).	|| 
964	N	L1655	[New documented residuals (pinn]	- P exits and PASS `{f:1}` with **no** MONITOR_LOST.	|| 
965	N	L1655	[New documented residuals (pinn]	The free was re-taken by a reclaim whose count the raise skipped, and P lost nothing (F25);	|| 
966	N	L1656	[New documented residuals (pinn]	- a tracer R on g then PASS `{g:1}` with no MONITOR_LOST, and after its exit `guard` is `"free"`, `anchors` 0 and `global_events` 0 (`rev7/w7_witnesses.py`, both versions).	|| 
967	N	L1666	[The exam-hole kill cases, from]	"Nested tracers on f whose inner section calls only a same-globals clone … kept alive past the inner exit and dropped before the outer exit, … inner trace expected to refuse CLONE_ALIVE" | X57c	|| 
968	N	L1669	[The exam-hole kill cases, from]	"A gate that declares asyncio.events:Handle._run and f … or simply assert P._STOP is asyncio.events.Handle._run.__code__ during the trace" | X34 (declaring it is now RESERVED_TARGET) and `cut_current` asserted in every case	|| 
969	N	L1670	[The exam-hole kill cases, from]	"gate A declares f and gate B declares g, section A runs only g and section B runs f … expecting A to refuse NOT_EXERCISED; … two gates both declare f … union {f:1} per gate" | X120, V41	|| 
970	N	L1675	[The exam-hole kill cases, from]	"sets `sys.modules[__name__].__class__` to a types.ModuleType subclass and serves the target only through PEP 562 … PASS …; an X16 variant … INSTANCE_PATH" | V10b, X16b	|| 
971	N	L1676	[The exam-hole kill cases, from]	examhole-inherited-direct-base-only	|| 
972	N	L1677	[The exam-hole kill cases, from]	"Scorer.fast = lru_cache(None)(_impl), bare or as a staticmethod … _impl bound at module level, or a wrapper served only through PEP 562 … NOT_A_FUNCTION" | X25c, X25d	|| 
973	N	L1684	[The exam-hole kill cases, from]	"cov.run('G', body), where body calls f and then sys.setprofile(prof); … require sys.getprofile() is prof (and the PROFILER_LOST note)" | V58 (adapted: prof kept, and no note because PROFILER_LOST is retired)	|| 
974	N	L1685	[The exam-hole kill cases, from]	"a pure-Python profiler is installed on the main thread before `with coverage_trace(exp)`, the section runs on a worker thread, and after the exit … sys.getprofile() is that profiler" | V57 (adapted: the case thread plays the main thread's part, because `sys.setprofile` is per-thread and cases run o	|| 
975	N	L1687	[The exam-hole kill cases, from]	"resume section B (opened and suspended on a worker thread) to completion inside section A on the main thread, then call A's target, expecting A {f:1} with no PROFILER_LOST and B's THREAD_HOP note" | V28b (adapted: THREAD_HOP is retired, so no notes.	|| 
976	N	L1688	[The exam-hole kill cases, from]	exam-mut-foreign-profiler-first-open-only	|| 
977	N	L1690	[The exam-hole kill cases, from]	exam-mut-profiler-lost-only-if-none	|| 
978	G	L1696	[Hazard sweeps (main thread, be]	Every detection mutant below is an SM1 row.	|| 
979	G	L1698	[Hazard sweeps (main thread, be]	Expected: no hang, and no problem other than REENTRANT from the tracer-entering finalizer.	|| 
980	G	L1702	[Hazard sweeps (main thread, be]	- **H5 (gc cost).** No gc at resolution or close.	|| 
981	G	L1703	[Hazard sweeps (main thread, be]	- **H6 (no-hang fault sweep, the deterministic core of G_FI).** For every code object in `_v5_faultpoints()`, the id-5 injector raises at each executed instruction, one point per run.	|| 
982	G	L1707	[Hazard sweeps (main thread, be]	- **H10 (L-DELIVERY; revision 2, M4).** G_SIG's three frozen L-DELIVERY cells (Stated limits): (a) outside every section with a trace active, 0 lost handlers, with the cell's shape fixed in revision 3 (no section is opened while the flood is armed, and `global_events == 0` is asserted before arming)	|| 
983	G	L1718	[Mutation audit (every rule has]	FOREIGN_DEFINITION by `__module__` / without the chain / walk only through functions / hop bound 3, 16 or 18	|| 
984	G	L1721	[Mutation audit (every rule has]	descriptor-only reads (isinstance or getattr reintroduced) / an `is` chain replaced by tuple membership	|| 
985	G	L1725	[Mutation audit (every rule has]	revision 3: `_commit` inside `_run`'s try (moved back before it) / `o.armed` in `_detach`'s check / the anchor test in `_detach`'s check / `_unwind_off` at the end of every reconciliation / `_unwind_off` in revision 2's order (test first)	|| 
986	G	L1726	[Mutation audit (every rule has]	revision 4: `o.frame` released by every `_detach` (revision 3's) / step 8's re-check deleted / its `'exiting'` test deleted / its claim test deleted / pruning of cores reached from anchors deleted / `_detach`'s test with the anchor before the event / `_named` by `==`	|| 
987	old	L1727	[Mutation audit (every rule has]	X146c / X145 (sweep) / X152 / X143 (sweep) / X143 (sweep) / X146e / X137d / X153 / X153 / X154 / X155 / X137e / X138b / V52 (the second transaction of any trace walks a cycle and never returns, so every case with an exit fails;	|| revision 7 audit
988	G	L1728	[Mutation audit (every rule has]	revision 6: `_register` with one name gate before all five exchanges (revision 5's form) / without the owner read after the last exchange / the builtin check on `_MON` at binding / the vocabulary check at binding / `_VERIFIED` widened to every 3.12 and 3.13 patch level / a rebinding without local ev	|| 
989	G	L1730	[Mutation audit (every rule has]	revision 7: a registration's repair of a replaced callback not counted in `_LOST` (revision 6's form) / the repair counted by X5 after the registration returns (the seventh critic's proposed form) / E4's re-take of its own id not counted (revision 6's prototype rule) / the binding check only when `_	|| 
990	G	L1731	[Mutation audit (every rule has]	X157c (a raising hook inside a reclaim; no greenlet needed)	|| 
991	G	L1732	[Mutation audit (every rule has]	revision 7 self-audit: a pending entry stored while no anchor is registered / an open taking the robust mutex / `_MON` re-read from `sys.monitoring` at each `coverage_trace()` / a non-str section compared before its refusal	|| 
992	G	L1733	[Mutation audit (every rule has]	revision 8: `_register`'s exchanges not read through `_tee` (revision 7's form: a replaced callback released before its count) / None previous callbacks not counted / a pending entry keyed by `id(frame)` without holding the frame (A13)	|| 
993	G	L1741	[Mutation audit (every rule has]	union over the declared section only / declared section, not gate name	|| 
994	G	L1754	[Mutation audit (every rule has]	no handler in the callbacks / no lock anywhere	|| 
995	G	L1763	[]	**Goal.** Show that the v5f exam detects *weakened* rules, not only deleted refusals.	|| 
996	G	L1763	[]	Round 4 found this was not so for v5e: 57/57 deletions were detected, but only 27/63 hand-aimed weakenings, and 29 of the survivors were witnessed exam holes.	|| 
997	G	L1765	[]	**Objectivity rule (revision 1).** Every quantity the gate reads is computed by a frozen script from frozen inputs: the catalog, the corpus, the seeds, N and the list of spec-fixed substrings.	|| 
998	G	L1765	[]	No step after the freeze asks a person to judge anything: not whether a mutant is equivalent, whether a line is reachable, which window a fault fell in, or which observables count.	|| 
999	G	L1771	[Frozen artifacts]	- **`rules_v5f.json`.** Every normative sentence of this spec as a rule atom with an id, including each crash-consistency ordering and claim (M1–M9, I1–I6).	|| 
1000	G	L1776	[Frozen artifacts]	- every exam case program;	|| 
1001	G	L1781	[Frozen artifacts]	Nothing is added after the freeze.	|| 
1002	old	L1782	[Frozen artifacts]	It marks every corpus entry with exactly one comparison class, assigned by a frozen rule from the entry's source and never by judgement:	|| revision 7 audit
1003	G	L1783	[Frozen artifacts]	- **`envelope`**: every entry that injects a fault or depends on timing.	|| 
1004	G	L1783	[Frozen artifacts]	That is every crash-sweep point; every exam case that uses the id-3 fault tool, the id-5 injector, a signal, `PyThreadState_SetAsyncExc`, a finalizer threshold or a thread race (X71c, X92b, X92c, X131–X133, X135, X138, X139, X140, X140f, X143, X143b, X143c, X144, X145, X146, X146b, X146c, X146d, X14	|| 
1005	G	L1784	[Frozen artifacts]	- **`equality`**: every other entry.	|| 
1006	G	L1787	[Frozen artifacts]	The direct-call coverage driver for the callback-only functions (G_COVER).	|| 
1007	G	L1788	[Frozen artifacts]	- **`traces_v5f/`.** The prebuilt `/3` traces for the scoring-only cases, generated by `ref_v5f.py` on 3.12.3 at freeze time.	|| 
1008	G	L1802	[SM1: catalog power, computed a]	Every SM1 run is such a process: admission condition 2 (the witness on `ref_v5f.py`), condition 3 (the witness on ref+W) and the kill test.	|| 
1009	G	L1802	[SM1: catalog power, computed a]	So is every SM2 run of instrument (a) on a mutant, which runs the whole exam in one fresh process per mutant, as `--mutation ALL`.	|| 
1010	G	L1802	[SM1: catalog power, computed a]	No two runs share a process, so the process-monotone cut, the tool id and every fixture wrapper start fresh in each (the second critic's MF4 on rev 2; the fourth critic's N7).	|| 
1011	G	L1804	[SM1: catalog power, computed a]	**Every mutant run twice (revision 8, N9).** Every SM1 run (admission conditions 2 and 3, and the kill test) and every SM2 instrument-(a) run is made twice, each in its own fresh process, and the two outcomes are compared.	|| 
1012	G	L1804	[SM1: catalog power, computed a]	A row counts as admitted, KILLED or UNWITNESSED, and an SM2 mutant as detected, only when both runs agree.	|| 
1013	G	L1804	[SM1: catalog power, computed a]	This rule is fixed before the freeze, and applying it needs no judgement.	|| 
1014	G	L1806	[SM1: catalog power, computed a]	**Isolation (D2).** An admitted W counts as **KILLED** only if its **named** witness fails on ref+W in the exam's mutation mode, whether by a wrong outcome, a crash or a timeout.	|| 
1015	G	L1806	[SM1: catalog power, computed a]	If only some other case fails, the row is **WITNESS_MISMATCH** and counts as not killed.	|| 
1016	old	L1808	[SM1: catalog power, computed a]	Before the freeze, the exam author must do one of two things for every UNWITNESSED row:	|| revision 7 audit
1017	G	L1810	[SM1: catalog power, computed a]	- file it as **EQUIVALENT_BY_SPEC**, with a written argument that no harness can observe the difference under this spec.	|| 
1018	G	L1812	[SM1: catalog power, computed a]	After the freeze, nothing is reclassified.	|| 
1019	G	L1812	[SM1: catalog power, computed a]	A surviving admitted row fails G_SEM, and the only remedy is a new prereg with a new exam.	|| 
1020	G	L1818	[SM1: catalog power, computed a]	- 100% of admitted rows are KILLED by their named witness on every version they are admitted for.	|| 
1021	G	L1821	[SM1: catalog power, computed a]	- every row of the Mutation-audit table above;	|| 
1022	G	L1822	[SM1: catalog power, computed a]	Their classification is fixed here, by id, so nothing is decided by reading code:	|| 
1023	G	L1833	[SM1: catalog power, computed a]	- **ADOPTED (1).** P03 "walk any type" is v5f's rule (Provenance (b)), so its patch has no site.	|| 
1024	G	L1833	[SM1: catalog power, computed a]	The successor weakening, "walk only through functions", is a row (V38).	|| 
1025	G	L1834	[SM1: catalog power, computed a]	- **RE-TARGETED (47).** Every other id.	|| 
1026	old	L1834	[SM1: catalog power, computed a]	P04 "no cycle detection" is expected UNWITNESSED: the walk is bounded at 16 hops and a revisited link repeats its own verdict, so the outcome cannot change.	|| revision 7 audit
1027	G	L1836	[SM1: catalog power, computed a]	The row is KILLED only if that invariant is among the sweep's failures; if only other invariants fail, it is WITNESS_MISMATCH:	|| 
1028	G	L1838	[SM1: catalog power, computed a]	- M2 no reconciliation → C7;	|| 
1029	G	L1847	[SM1: catalog power, computed a]	`'<'` skip widened to "no `__file__`" (X26e); sourceless accepted (X26f);	|| 
1030	G	L1849	[SM1: catalog power, computed a]	- cut and loop: CUT_MOVED only at exit (X141b);	|| 
1031	G	L1851	[SM1: catalog power, computed a]	X137 free variant from revision 5, where no pop leaves a stale event);	|| 
1032	G	L1852	[SM1: catalog power, computed a]	- revision 4: every row of the revision-4 line of the Mutation-audit table that revision 5 keeps, each with the witness named there.	|| 
1033	G	L1853	[SM1: catalog power, computed a]	- revision 5: every row of the revision-5 line of the Mutation-audit table, each with the witness named there.	|| 
1034	G	L1853	[SM1: catalog power, computed a]	All except the `_Txn` and facade rows were checked on `rev5/mech5.py`: under the spec every sweep trial and every hold case gives the stated outcome, and under each single-rule mutant the named witness differs, on 3.12.3 and 3.13.12 (`rev5/w5_witnesses.py`, identical verdicts on both versions).	|| 
1035	G	L1853	[SM1: catalog power, computed a]	`rev5/m5_modelcheck.py mutants` also finds a property violation for each model-level mutant except two: `ensure_nocount`, whose only effect is a note on a loss-free free (X137e witnesses it), and `prune_credit_stop`, the deleted rule restored, which changes nothing;	|| 
1036	G	L1854	[SM1: catalog power, computed a]	- revision 6: every row of the revision-6 line of the Mutation-audit table, each with the witness named there, including F14's row (the name-gated close read, X137f).	|| 
1037	G	L1855	[SM1: catalog power, computed a]	- revision 7: every row of the revision-7 lines of the Mutation-audit table, each with the witness named there.	|| 
1038	G	L1855	[SM1: catalog power, computed a]	`rev7/m7_modelcheck.py mutants` finds a property violation for `cbrep_nocount` and `count_after_register` (the `cb2:` configurations; the latter only with a fault) and for `blind_read_gated` in two new fault configurations; the Revision 7 table has the rest;	|| 
1039	G	L1856	[SM1: catalog power, computed a]	- revision 8: every row of the revision-8 line of the Mutation-audit table, each with the witness named there.	|| 
1040	G	L1859	[SM1: catalog power, computed a]	- boundary rows on both sides of every quantitative rule: 16 hops accepted (V11c) and 17 refused (X30b); the 10 s busy bound, 11 s refused MACHINERY_BUSY (X138) and 7 s accepted (V65), both measured from the waiter's start (revision 2, M12).	|| 
1041	G	L1862	[SM2: operator mutants on the r]	G_HYG checks that `styxx/protocol.py` defines no function or class outside this set and the v4-era set.	|| 
1042	G	L1864	[SM2: operator mutants on the r]	**Operators,** applied mechanically at every applicable site.	|| 
1043	G	L1872	[SM2: operator mutants on the r]	O7 every int constant ±1;	|| 
1044	G	L1872	[SM2: operator mutants on the r]	O7b every float constant ×0.5 and ×2 (revision 2, M12: `_BUSY_SECONDS` and the 0.2 ms retry sleep are floats)	|| 
1045	G	L1882	[SM2: operator mutants on the r]	- (b) the frozen crash sweep with the frozen stride (G_FI: every third point of the frozen enumeration);	|| 
1046	G	L1886	[SM2: operator mutants on the r]	**The normalizer compares only spec-fixed observables.** This list is closed:	|| 
1047	G	L1904	[SM2: operator mutants on the r]	The **noise mask** is computed over the manifest's `equality` entries only.	|| 
1048	G	L1910	[SM2: operator mutants on the r]	Nothing about an entry's class is decided after the freeze.	|| 
1049	G	L1914	[SM2: operator mutants on the r]	- **EXAM_HOLE:** only (d) sees a spec-fixed difference;	|| 
1050	G	L1915	[SM2: operator mutants on the r]	- **UNDISTINGUISHED:** nothing does.	|| 
1051	G	L1915	[SM2: operator mutants on the r]	No one judges whether it is equivalent, before or after the freeze, and it is not gated (judge 1).	|| 
1052	G	L1915	[SM2: operator mutants on the r]	The shipping claim states the count and says that G_SEM certifies nothing about these mutants.	|| 
1053	G	L1919	[SM2: operator mutants on the r]	- a clean baseline (every instrument passes the unmutated implementation);	|| 
1054	G	L1928	[SM3 (aux, carried): the indepe]	- Every round 1–4 blocker repro is replayed against both implementations, and none may reproduce.	|| 
1055	G	L1931	[Positive controls (frozen; run]	For this control only, any failing case counts as a witness.	|| 
1056	G	L1931	[Positive controls (frozen; run]	There are no named witnesses, so SM1's admission conditions 2 and 3 are replaced by condition 1 alone, and the only classes are **KILLED** (some v5e exam case fails) and **SURVIVED** (none does).	|| 
1057	old	L1932	[Positive controls (frozen; run]	**SM2 on v5e.** SM2's generator, run on v5e, with instruments (a) and (d) only, must report EXAM_HOLE > 0 (revision 2, M11):	|| revision 7 audit
1058	G	L1934	[Positive controls (frozen; run]	- (d) is the differential probe over the corpus programs that use only the public API v5e shares with v5f.	|| 
1059	G	L1936	[Positive controls (frozen; run]	SM2's clean-baseline condition applies to (a) and (d) only. v5e passes its own exam, and (d)'s mask is built from five unmutated v5e runs.	|| 
1060	G	L1936	[Positive controls (frozen; run]	On v5e the normalizer omits the `_v5_state()` fields and tool ownership, which v5e does not have, and compares every other listed observable.	|| 
1061	G	L1937	[Positive controls (frozen; run]	The committed results of `fault_injection_v5.py`, which the sweep extends, are cited for reference only: on 3.12, 2393 of 9369 points not clean, 132 of them HANG; on 3.13, 2041 of 8604 not clean, 127 HANG.	|| 
1062	G	L1942	[Companion gates]	- **G_FI (crash sweep, frozen).** Required on both verified interpreters (3.12.3 and 3.13.12), on two threads: every fault point is **clean**.	|| 
1063	G	L1942	[Companion gates]	Clean is defined by outcome alone, so no window has to be mapped to implementation offsets.	|| 
1064	G	L1944	[Companion gates]	**The invariants C1–C9 (revision 3, MF5; frozen with this spec).** They are stated only in terms of the public API, `_v5_state()` (M10) and I1–I6.	|| 
1065	G	L1944	[Companion gates]	`crash_sweep_v5f.py` implements them as written here and reads no other private name.	|| 
1066	G	L1944	[Companion gates]	D1's `crashcons/t_crash_sweep.py` was their source; its private reads (`reconcile_now()`, `leftovers()`, `_TH`, `sys.getprofile()` as a hook check) have no counterpart here.	|| 
1067	G	L1947	[Companion gates]	It constructs tracers only through `coverage_trace()`, catches nothing, and so stops at the first exception that reaches it.	|| 
1068	G	L1947	[Companion gates]	Revision 4 (MF3): every scenario joins every thread it started before any tracer's `__exit__`, so no open or close in it runs concurrently with an exit, and its baseline record is deterministic.	|| 
1069	G	L1947	[Companion gates]	Every scenario orders its machinery calls across threads with frozen barriers, so no `coverage_trace()`, enter, exit, `run()`/`run_async()` open or close, or `record()` on one thread overlaps a machinery call on another.	|| 
1070	G	L1947	[Companion gates]	The order in which opens and closes happen across threads is fixed by the scenario, so each machinery call finds the same registry state in every run: the same anchors, the same event, the same mutex owner.	|| 
1071	G	L1947	[Companion gates]	So the instruction path of every call on the faulted thread, and with it the point set, is the same in every run.	|| 
1072	G	L1947	[Companion gates]	Revision 5 has no such loop, and the mutex's wait loop is not reached in a sequential scenario.	|| 
1073	G	L1948	[Companion gates]	Every scenario names the one thread the injector faults.	|| 
1074	G	L1948	[Companion gates]	The injector's filter admits that thread's live ident only.	|| 
1075	G	L1948	[Companion gates]	The fork scenario names the child's only thread.	|| 
1076	G	L1949	[Companion gates]	`key` is a `_v5_faultpoints()` key, `offset` an instruction offset in that code object (it has no nested code objects: G_HYG, MF4), and k ≥ 1.	|| 
1077	G	L1949	[Companion gates]	A scenario's point set is every triple with k ≤ K = 2 whose offset a *discovery run* executed at least k times on the faulted thread.	|| 
1078	G	L1949	[Companion gates]	The discovery run is the baseline run with the injector armed in counting mode, raising nothing.	|| 
1079	G	L1949	[Companion gates]	G_FI runs every point.	|| 
1080	G	L1949	[Companion gates]	The *frozen stride*, used by SM2's instrument (b) and G_COVER's pass 2 only, is 3: the points whose index in that order is divisible by 3.	|| 
1081	G	L1951	[Companion gates]	`EXP_PROBE` declares one function, in a probe module that nothing else uses.	|| 
1082	G	L1952	[Companion gates]	In the background variant, the background thread is parked at a barrier outside its sections whenever a snapshot or check is taken; it runs only while the scenario runs.	|| 
1083	G	L1956	[Companion gates]	`record()` of every faulted tracer is read.	|| 
1084	G	L1959	[Companion gates]	Each faulted tracer whose `record()` refused TRACE_ACTIVE gets one more `__exit__(None, None, None)`, and its `record()` is read again.	|| 
1085	G	L1960	[Companion gates]	Every reference to the faulted tracers is dropped (the scenario's result, the records, the tracers), then `gc.collect()` runs, then a second probe cycle.	|| 
1086	G	L1964	[Companion gates]	- **C1, no over-credit.** In every record read at steps 2 and 5, no `calls` count exceeds the baseline record's count for the same section and target.	|| 
1087	G	L1964	[Companion gates]	Each frozen sentinel gate declares a fixture function that the scenario calls only outside that gate's declared section.	|| 
1088	old	L1964	[Companion gates]	Every sentinel gate scores NOT_EXERCISED or a refusal, never PASS.	|| revision 7 audit
1089	G	L1965	[Companion gates]	- **C2, no swallowing.** One of these holds:	|| 
1090	G	L1969	[Companion gates]	The scenario's own thread then sees no exception, and the worker's openings are finalised by exit's X3.	|| 
1091	G	L1972	[Companion gates]	- **C3, no hang and no held exclusion.** Step 3's probe cycle completes.	|| 
1092	G	L1973	[Companion gates]	At the end of the sweep its record has exactly one count per call it made, no problem, and no note.	|| 
1093	old	L1973	[Companion gates]	Since the sweep covers every faultpoint code, that allowance was always granted (the fourth critic's N4).	|| revision 7 audit
1094	G	L1973	[Companion gates]	Revision 4 removes it, because no instrument can cause MONITOR_LOST any more (M7, I6).	|| 
1095	G	L1974	[Companion gates]	- **C5, nothing left once the faulted tracers are gone.** After step 6, `_v5_state()` equals `S0` except that `cut` may have grown, `tool` may have been set, and, in the background variant, the `pending` field of the background tracer's target mint is not compared.	|| 
1096	G	L1974	[Companion gates]	If it raises after the last anchor closed, S is clear and nothing pops the entry until the mint retires, which the background tracer's hold prevents during the sweep (`critic4/a6_pending_left_fault_free.py`: 1 entry left with `anchors` 0 and `global_events` 0, both versions).	|| 
1097	G	L1975	[Companion gates]	- `mints` lists only the background tracer's target, if any, with `holders` 1;	|| 
1098	G	L1978	[Companion gates]	- `global_events` is 0, so no stale event (I3 at quiescence);	|| 
1099	G	L1981	[Companion gates]	Also every fixture function's `__code__` is its original, except the background target's, which is its minted code.	|| 
1100	G	L1982	[Companion gates]	- **C6, nothing else escapes.** No exception other than the injected one leaves the scenario, and no thread the scenario started ends with any exception other than the injected one (which C2's fourth alternative then accounts for).	|| 
1101	G	L1983	[Companion gates]	- **C7, only live holders, while the faulted facades are alive.** At step 4, every entry of `_v5_state()["mints"]` has `holders` equal to the number of live tracers declaring that target.	|| 
1102	G	L1983	[Companion gates]	A faulted tracer whose `record()` returned a record, or refused TRACE_INCOMPLETE, holds nothing.	|| 
1103	G	L1983	[Companion gates]	No entry has `holders` 0, and `installed` is True for every entry.	|| 
1104	G	L1984	[Companion gates]	- **C8, the envelope.** Every record read at step 2 is within I6 against its tracer's baseline record, or `record()` refused TRACE_INCOMPLETE or TRACE_ACTIVE.	|| 
1105	G	L1987	[Companion gates]	- notes within the baseline notes plus OPEN_AT_EXIT (revision 4: no MONITOR_LOST allowance;	|| 
1106	G	L1988	[Companion gates]	- problems equal to the baseline's, because no sweep scenario enters or exits a tracer from inside a transition.	|| 
1107	G	L1990	[Companion gates]	Every record read at step 5 meets the same test, and there TRACE_ACTIVE is a failure.	|| 
1108	G	L1991	[Companion gates]	- **C9, the forked child (fork scenario only).** Checked in the child and reported to the parent through a pipe:	|| 
1109	G	L1992	[Companion gates]	- `run()` passes through, returning fn's value with no exception;	|| 
1110	G	L1994	[Companion gates]	- every fixture function's `__code__` is its original, or is equal to it (`==`) and has no local events on `_v5_state()["tool"]`;	|| 
1111	G	L2000	[Companion gates]	The scenario runs in a fresh subprocess that starts no thread before the fork (N10).	|| 
1112	G	L2001	[Companion gates]	- *Callback-only functions (BF1, M8).* The injector faults the five callback-only functions at each instruction while the direct-call driver runs them.	|| 
1113	G	L2002	[Companion gates]	- *Injector rules.* The Exam harness rules apply: code read at arming, a thread filter on a live ident, no settrace, `restart_events()` before each trial, and a self-check.	|| 
1114	G	L2005	[Companion gates]	- a result value that differs on an iteration in which no asynchronous exception was delivered;	|| 
1115	G	L2010	[Companion gates]	It is measured in the three L-DELIVERY cells (H10), and cell (a), outside every section, is gated at 0.	|| 
1116	G	L2013	[Companion gates]	- no `with` statement, no `threading` lock and no `try … finally` in the v5 region, except `_run`/`_run_async`'s single `try/finally`;	|| 
1117	G	L2014	[Companion gates]	- no loop in any `try` body (CPython #130279).	|| 
1118	G	L2014	[Companion gates]	Each `finally` is exactly `_detach(o, (end, ()))` then `o.frame = None` (revision 4).	|| 
1119	G	L2014	[Companion gates]	Revision 5: no function reachable from a `try` body or a `finally` has a loop, other than `_run_async`'s `await`.	|| 
1120	G	L2014	[Companion gates]	The lint lists every backward jump in the code objects of `_commit`, `_detach`, `_unwind_on`, `_unwind_off`, `_take`, `_register`, `_set_local` and `_named`, and requires none (M7;	|| 
1121	G	L2015	[Companion gates]	- (revision 4, MF4) no region code object has a code object among its `co_consts`: no nested function, lambda, class or generator expression.	|| 
1122	G	L2016	[Companion gates]	- no `except` in the region other than the two chained user-code sites and `_exit`'s two `except GateSpecError` clauses around `_locked`.	|| 
1123	G	L2016	[Companion gates]	In the scoring functions, the only ones allowed are check_metrics' existing `except GateSpecError` report loops and `except OverflowError` (`_finite` and score's metric guard);	|| 
1124	G	L2017	[Companion gates]	- no `import` statement and no `importlib`/`__import__` call in the region, except `_resolve_target`'s single `importlib.import_module(module)` and `coverage_trace`'s `import asyncio`;	|| 
1125	G	L2018	[Companion gates]	- no type test by tuple membership (`type(x) in (…)`, `in _LAZY_TYPES`) in the region;	|| 
1126	G	L2019	[Companion gates]	- no `sys.monitoring` event other than PY_START, PY_RESUME, PY_RETURN, PY_YIELD and PY_UNWIND is named in the region;	|| 
1127	G	L2021	[Companion gates]	- (revision 2) `_forget_in_child` writes `__code__` only in its last loop, after every registry and event step;	|| 
1128	G	L2021	[Companion gates]	`_detach`'s only anchor pop is inside its `_unwind_off(fr)` call (revision 5);	|| 
1129	G	L2022	[Companion gates]	`_open` stores no anchor and calls no `_unwind_on`.	|| 
1130	G	L2022	[Companion gates]	`_unwind_on` is called only from `_commit`;	|| 
1131	G	L2024	[Companion gates]	1. one assignment whose targets are locals and whose values are `_TOOL[0]` and items `_MON[0][i]` with literal i (in `_take`, `_register` and `_set_local`, `t` is a parameter and only `_MON[0]` items are read).	|| 
1132	old	L2024	[Companion gates]	So `set_events` is callable only in `_unwind_on` and `_unwind_off`, and `_register` cannot write S.	|| revision 7 audit
1133	G	L2025	[Companion gates]	2. in `_register` only (revision 8, M1): `a, b = _tee(P)`, one assignment to two locals whose value is one `_tee` call.	|| 
1134	G	L2025	[Companion gates]	The statement builds, and reads and writes nothing;	|| 
1135	G	L2028	[Companion gates]	The statements after statement 1 (the *pipeline statements*) may name only: the M1 one-call vocabulary (`_map`, `_chain`, `_chain.from_iterable`, `_compress`, `_filter`, `_is`, `_not`, `_and`, `_setitem`, `_ARMED`, `_FLAGS`, `_VALUES`, `_CONSUME`, `_LOST_KEY`, `_TRUE`, `_PYU1`, `_ZERO1`, `_NONE1`, `	|| 
1136	G	L2028	[Companion gates]	Every call in them is a call of one of those names or of a `_MON[0]` local.	|| 
1137	G	L2028	[Companion gates]	No lambda, generator expression, comprehension, `def` or class appears in them.	|| 
1138	G	L2028	[Companion gates]	**Revision 8 (M2): no operator other than a call, those attribute reads and a tuple display appears in them.** That excludes a subscript, a comparison (`in`, `is`, `==` and the rest), a boolean, unary or arithmetic operator, a conditional expression, an assignment expression and a starred argument.	|| 
1139	G	L2028	[Companion gates]	`_CALLBACKS5` is data: its functions are arguments of `register_callback` and operands of `_is_not`, and the lint checks that `_CALLBACKS5` appears only as an argument of `_map` in `_register`.	|| 
1140	G	L2028	[Companion gates]	The literal None may appear as `_filter`'s first argument (revision 7) and as `_repeat`'s argument (revision 8).	|| 
1141	G	L2029	[Companion gates]	- (revision 6, M4 iv) `_Opening` and `_Core` each have a class body consisting of `__slots__ = (<string literals>)` listing exactly their M1 fields and one `def __init__`; nothing else, so no `__getattribute__`, `__getattr__`, property or other Python-level descriptor exists (M1);	|| 
1142	G	L2030	[Companion gates]	Within the step functions, each is called only by the steps whose frozen set binds it (revision 8, M3, above).	|| 
1143	G	L2030	[Companion gates]	`_retire` has no step 6.	|| 
1144	G	L2030	[Companion gates]	`_reconcile` ends with the prune of cores reached from anchors and then `if _TOOL[0] is not None: _unwind_off(None)` (revision 6, F20), in that order.	|| 
1145	G	L2031	[Companion gates]	- **Scope (revision 6).** This lint checks the pipelines' *shape* only.	|| 
1146	G	L2036	[Companion gates]	- K13: `_register` binding `set_events` and clearing S, gated on the name, with its None swallowed by `_filter(None, …)` (the frozen index set);	|| 
1147	G	L2040	[Companion gates]	`o.frame` is assigned only in `_open` and in `_run`'s and `_run_async`'s `finally`.	|| 
1148	G	L2040	[Companion gates]	`_detach`'s `UNWIND_LOST` test reads `o.armed`, then `get_events`, then `_ANCHORS.get(fr) is o`, in that order, and then calls `_unwind_off(fr)`, which is the only pop of an anchor in `_detach`;	|| 
1149	G	L2041	[Companion gates]	- (revision 5) `_TOOL_NAME` is tested only by identity: inside `_named`, whose test is `_MON[0][0](i) is _TOOL_NAME`, and in the one-call steps' `_is` gates;	|| 
1150	G	L2042	[Companion gates]	- (revision 5, N3) `_acquire` reads the clock only through `_monotonic` and sleeps only through `_sleep`; no function in the region reads `time.monotonic` or `time.sleep` at call time;	|| 
1151	G	L2043	[Companion gates]	- (revision 7, self-audit) no call of `sys.setprofile`, `sys.settrace`, `threading.setprofile`, `threading.settrace`, `threading.setprofile_all_threads` or `threading.settrace_all_threads`, and no read of those names, in the region.	|| 
1152	G	L2043	[Companion gates]	"What is removed from v5e" and R1-B4 claim this, and the leftover checks see only a lasting change, not a set-and-restore;	|| 
1153	G	L2044	[Companion gates]	- (revision 2; revision 6 names the list) exactly the names M1's reload paragraph lists are bound through `globals().get` (and `_TOOL_NAME` through `globals().get(...) or`); every other module-level name of the M1 block is a plain binding (N8; the sixth critic's M4 i, vi).	|| 
1154	G	L2044	[Companion gates]	`_MON` is assigned only in `coverage_trace()`'s binding, after the M0 builtin check.	|| 
1155	G	L2045	[Companion gates]	- **G_COVER.** Every executable line of the region runs in at least one of the four passes below.	|| 
1156	G	L2045	[Companion gates]	No pragma exempts a line.	|| 
1157	old	L2045	[Companion gates]	A line that no pass reaches is dead code and must be deleted.	|| revision 7 audit
1158	G	L2045	[Companion gates]	Revision 3 drops revision 2's "or folded onto its guard's line" (N12; see "Line coverage only").	|| 
1159	G	L2046	[Companion gates]	- **Executable lines.** For each region code object, meaning the `_v5_faultpoints()` code objects read before any tracer exists plus the scoring functions of the SM2 region: `{line for _, _, line in code.co_lines() if line is not None} - {code.co_firstlineno}`.	|| 
1160	G	L2046	[Companion gates]	Revision 4 (MF4): G_HYG guarantees these code objects have no nested code objects, so no line is counted through an enclosing function's line.	|| 
1161	old	L2047	[Companion gates]	It uses sys.monitoring tool id 0, which no exam tool or case uses, with global LINE events, because the self-trace mints some region functions and their minted copies must count.	|| revision 7 audit
1162	G	L2047	[Companion gates]	Its callback records `(co_filename, line)` for code whose `co_filename` is `styxx/protocol.py`'s path and returns None there.	|| 
1163	G	L2047	[Companion gates]	Revision 3 also needed its LINE events not to split `_unwind_off`'s one-line test-and-clear; from revision 4 nothing depends on that (M7).	|| 
1164	old	L2047	[Companion gates]	There is no settrace pass: settrace silences the id-5 injector (F1), so a crash sweep run under it never reaches the lines only a fault reaches.	|| revision 7 audit
1165	G	L2048	[Companion gates]	- **The four passes.** The tool is enabled at the start of every process each pass creates.	|| 
1166	G	L2049	[Companion gates]	**The exam's fast mode.** The frozen runner's `--fast` mode runs every exam case once, in its frozen placement (inside the self-trace, on the main thread before it, or in a fresh subprocess).	|| 
1167	G	L2050	[Companion gates]	This run's G_FI verdicts are discarded: G_FI's verdict comes from a run with no coverage tool (the injector rules).	|| 
1168	G	L2052	[Companion gates]	**The frozen direct-call driver `cover_driver_v5f.py`**, counted for the named list below only.	|| 
1169	G	L2054	[Companion gates]	Lines that the spec requires and that run only in a subprocess are reached in pass 1's subprocess cases: `_reclaim`'s body (X137 free variant), MONITOR_BUSY's raise (X36), and `_ensure_tool`'s id-3 fallback (X142, X137b).	|| 
1170	G	L2054	[Companion gates]	Revision 5: `_take`'s and `_register`'s lines are reached at every first enter, and in pass 1's subprocess cases X137, X137e and X155.	|| 
1171	G	L2054	[Companion gates]	`_set_local`'s lines are reached at every mint and retire.	|| 
1172	G	L2054	[Companion gates]	The one-call steps have no branch in bytecode, so line coverage reaches all of each; which of their inner outcomes happen is gated by the sweep witnesses (X143, X145, X152–X155), not by G_COVER.	|| 
1173	G	L2055	[Companion gates]	- **Line coverage only (N12, accepted in writing).** G_COVER measures lines, not branches.	|| 
1174	G	L2055	[Companion gates]	The machinery is written in that style throughout, so G_COVER shows only that no line of the region is dead.	|| 
1175	G	L2055	[Companion gates]	The only branch-level instrument on 3.12, BRANCH events, creates the #130279 skip (p13), and a gate on it would have to be read against that.	|| 
1176	G	L2056	[Companion gates]	- **The named list (revision 2, BF1), frozen here:** `_on_entry`, `_on_exit`, `_on_unwind`, `_outcome` and `_publish`, which run only inside sys.monitoring callbacks, and `_forget_in_child`, which runs only in a forked child's at-fork handler.	|| 
1177	G	L2056	[Companion gates]	The reason is measured: code inside a monitoring callback raises no events for any tool, and legacy settrace is suppressed there too. coverage.py 7.16.1 reports such lines as missing under both its sysmon and ctrace cores (critic2/`covt`;	|| 
1178	G	L2061	[Companion gates]	- no registered anchor;	|| 
1179	old	L2070	[Companion gates]	- **After the freeze.** An implementation line in a named function that no frozen driver scenario reaches is dead code, like any other, and must be deleted.	|| revision 7 audit
1180	G	L2070	[Companion gates]	No exemption is granted after the freeze, and none is needed.	|| 
1181	G	L2071	[Companion gates]	- **G_ATOM (revision 6, M4 and M5; rewritten in revision 7, M1; revision 8, M1–M4 and N3: the premise gate, dynamic, frozen and objective).** The one-call premise is a property of CPython, not of styxx's text, so it is gated by execution on every patch level in `_VERIFIED` (M0), on `ref_v5f.py` at t	|| 
1182	G	L2071	[Companion gates]	It reads only the G_ATOM interface (M10) and `_v5_faultpoints()`.	|| 
1183	G	L2071	[Companion gates]	Nothing in it is decided by judgement: every case, expected effect, threshold and control is listed here.	|| 
1184	G	L2072	[Companion gates]	An audit hook records every audit event raised on the step's thread.	|| 
1185	G	L2072	[Companion gates]	A `sys.setprofile` function records every `c_call` made from the step's own frame.	|| 
1186	G	L2072	[Companion gates]	The CALL events on the step's code are all recorded, not only the consumer's, each with whether the interval had already closed (revision 8, M2).	|| 
1187	G	L2072	[Companion gates]	Nothing else is.	|| 
1188	G	L2074	[Companion gates]	The product of: the id named or held by another tool; o's anchor registered or not; the global event set or clear; and the other registered opening, none, one armed or one unarmed (each with its own core). o itself is unarmed, as it is at `_commit`'s call.	|| 
1189	G	L2075	[Companion gates]	The product of: named or not; the key registered, a key not registered, or None; another anchor left after the pop or not; and the event set or clear.	|| 
1190	G	L2078	[Companion gates]	The id held by styxx with its five callbacks; held by styxx with its PY_UNWIND callback replaced by another party's (the B1 shape); the same two with the global PY_UNWIND **set** (revision 8, M3: revision 7's nine cases all began with S clear, so a `_register` that cleared S was invisible); held by 	|| 
1191	G	L2081	[Companion gates]	- (b) *Inside:* no PY_START on the step's thread and no audit event.	|| 
1192	G	L2082	[Companion gates]	- (c) *Outside:* between the step's start and its return, and outside the interval, no PY_START on the step's thread.	|| 
1193	G	L2082	[Companion gates]	The only `c_call`s the profile function sees from the step's frame are the consumer's, `itertools.chain.from_iterable`'s and, in `_register`, `itertools.tee`'s (revision 8).	|| 
1194	G	L2082	[Companion gates]	The latter two build lazy iterators and read nothing.	|| 
1195	G	L2082	[Companion gates]	**Revision 8 (M2):** every CALL event on the step's code is a call of `map`, `filter`, `itertools.chain`, `itertools.compress`, `itertools.repeat`, `itertools.tee`, `itertools.chain.from_iterable` or the consumer, and none comes after the interval has closed.	|| 
1196	G	L2082	[Companion gates]	So no Python function, C function, method descriptor or method-wrapper is called outside the one call.	|| 
1197	G	L2082	[Companion gates]	**What (c) does not see (revision 8, M2).** `sys.setprofile` reports `c_call` only for builtin functions and method descriptors, and CALL events report calls only.	|| 
1198	G	L2082	[Companion gates]	Revision 7 said that (c) established "no read or write of shared state happens outside the one call".	|| 
1199	G	L2084	[Companion gates]	- `_unwind_on` sets the event and captures iff named and o registered, and captures (flags `UNWIND_LOST` on the core of every armed registered opening) iff the event was also clear;	|| 
1200	G	L2085	[Companion gates]	- `_unwind_off` pops the key and clears iff named and no anchor is left;	|| 
1201	G	L2090	[Companion gates]	- held by styxx with one callback replaced: 5 exchanges, returns `[None, _TOOL_NAME]`, `len(_LOST)` + 1, and styxx's five callbacks afterwards;	|| 
1202	G	L2091	[Companion gates]	- held by another tool or unowned: no exchange; returns `[owner]`;	|| 
1203	G	L2092	[Companion gates]	- split case k: k exchanges, returns `[None, <other>]` (the k-th exchange found the other tool's callback), `len(_LOST)` + 1, and afterwards exactly the other tool's k-th callback is styxx's;	|| 
1204	G	L2094	[Companion gates]	- the finalizer case (revision 8, M1): 5 exchanges, returns `[None, None, _TOOL_NAME]`, `len(_LOST)` + 2, styxx's five callbacks afterwards, and the finalizer records required by (b).	|| 
1205	old	L2098	[Companion gates]	Each must be detected in the same run, or G_ATOM fails** (revision 8, M4: revision 7 said "the run is void" and gave no rule for what follows a void run; see "No void run" below).	|| revision 7 audit
1206	G	L2104	[Companion gates]	- K4: `_register` in revision 5's form, one name gate before all five exchanges, no owner read and no count.	|| 
1207	G	L2104	[Companion gates]	It fails all `_register` cases (9 of revision 7's matrix, 12 of revision 8's): in the split cases 6 − k of the other tool's callbacks are replaced, and its result has no owner element.	|| 
1208	G	L2112	[Companion gates]	- K13 (revision 8, M3): `_register` binding `set_events` and clearing S when the id is named, its None swallowed by `_filter(None, …)` (`critic8/c5`).	|| 
1209	old	L2115	[Companion gates]	**Static-only controls (revision 8, M2), run and reported, not gated here.** K12 (`_unwind_on` with its own-anchor gate read by `in` and a subscript in statement 1, `critic8/c2`) and K12b (the same read as a tuple display in the pipeline statement) pass all 67 cases on both versions, because G_ATOM 	|| revision 7 audit
1210	G	L2116	[Companion gates]	**Revision 8 (M4): every floor is a count, not a time.** Revision 7 ran part E for a fixed 3 s and part D2 for 3 s per form, and said a count below the floor voided the run.	|| 
1211	G	L2116	[Companion gates]	A machine about 30% slower, or a loaded host, would then void G_ATOM, and nothing said whether a void run could be repeated or how often.	|| 
1212	G	L2116	[Companion gates]	It fails G_ATOM if it reaches its own floor, or `T_MAX`, with none.	|| 
1213	G	L2117	[Companion gates]	Part A's floors count events, and it has no time bound to change.	|| 
1214	G	L2121	[Companion gates]	It passes iff every trial either completes with the full effect or raises RecursionError with no effect, and both outcomes occur for every step.	|| 
1215	old	L2121	[Companion gates]	Its control is K7's `_unwind_off`, which must show a trial that raises RecursionError after its pop (measured: n0 = 4,998 on both versions; 31 full and 30 none per step;	|| revision 7 audit
1216	G	L2121	[Companion gates]	A14's recursion claim is then gate-witnessed, not probe-only.	|| 
1217	G	L2123	[Companion gates]	- **No void run (revision 8, M4).** Every G_ATOM outcome is PASS or FAIL.	|| 
1218	G	L2123	[Companion gates]	A floor not reached by `T_MAX`, a control not detected, a hookup control that passes, or a case that fails is a FAIL, and nothing is re-run.	|| 
1219	G	L2124	[Companion gates]	The matrix checks the implementation's own five steps for Python code, audit events and calls outside their one C call, under the instruments above, in every listed gate outcome.	|| 
1220	G	L2124	[Companion gates]	Gc, signals and threads are tested only by the frozen t1 and D2 parts, which run reference pipelines built like the steps, not the implementation's steps.	|| 
1221	G	L2124	[Companion gates]	So G_ATOM shows that the implementation's steps have the construction, and that CPython runs nothing inside that construction under these conditions.	|| 
1222	G	L2125	[Companion gates]	The hookup controls fail as required: K4 12 of 67 cases, K5, K6 and K7 24 each (every `_unwind_off` case), K8 3, K9 3, K10 12, K11 12, K13 2 and K14 1.	|| 
1223	G	L2126	[Companion gates]	- **Would have caught.** Revision 5's premise, "no one-call step raises an audit event", fails criterion (b) on `_register` itself (B1).	|| 
1224	G	L2133	[]	- **G_EXAM_FROZEN.** Every `FROZEN_*_SHA256` in the prereg matches, read from the prereg as first committed.	|| 
1225	G	L2133	[]	The freeze commit contains the runner, `ref_v5f.py`, the SM1 catalog, `sm1_result.json` and every gate script.	|| 
1226	G	L2137	[]	- **G3.** No v4/v5 disagreement over the pairable committed results.	|| 
1227	G	L2144	[]	- **G_XVER.** Every case's outcome is identical on the two verified interpreters, 3.12.3 and 3.13.12 (revision 7, M2: revision 6 said "3.12 and 3.13").	|| 
1228	G	L2144	[]	On 3.10.20 and 3.11.15 (revision 8, N8), X37 refuses UNSUPPORTED_VERSION and every scoring-only case matches.	|| 
1229	old	L2147	[]	- **G_ATOM (revision 6; revision 7 criteria; revision 8 matrix, floors and controls)** passes on every patch level in `_VERIFIED`, run on `ref_v5f.py` at the freeze and on the implementation before any scoring: all 67 cases pass, the t1, D2 and R parts meet their count floors within `T_MAX`, K1–K3 a	|| revision 7 audit
1230	G	L2147	[]	Every outcome is PASS or FAIL; no run is void or repeated (revision 8, M4).	|| 
1231	G	L2147	[]	A patch level is added to `_VERIFIED` only with a passing G_ATOM run committed beside it, naming its build.	|| 
1232	G	L2148	[]	- **G_COVER = 1.0**, with no pragmas; the callback-only functions are measured by the frozen direct-call driver (revision 2, BF1).	|| 
1233	G	L2151	[]	Every round-4 blocker and defect repro no longer reproduces, or ends in its documented refusal.	|| 
1234	G	L2152	[]	The shipping claim names every UNDISTINGUISHED mutant (as uncertified, not as equivalent) and every residual.	|| 
1235	H	L2162	[]	**L-DELIVERY as possible program corruption.** A PY_UNWIND callback that raises replaces the in-flight exception, and on 3.12.3 and 3.13.12 its `__context__` is None.	|| 
1236	H	L2164	[]	Their soundness rests on one CPython property: no Python code, thread switch, signal handler, finalizer, audit hook, instrument callback or greenlet switch runs inside a C call that makes no Python-level call.	|| 
1237	H	L2164	[]	Revision 6 refuses every patch level but 3.12.3 and 3.13.12, so attack those two, and attack G_ATOM itself: a patch level joins `_VERIFIED` only through it (revision 7 corrects revision 6's advice to check other patch levels, which the spec now refuses).	|| 
1238	H	L2167	[]	The named alternative is still first-LINE confirmation with local events only, at a per-line cost inside declared functions.	|| 
1239	H	L2176	[]	Also check that no settrace/setprofile activity in the sweep process silences the injector (`rev2/p5b_settrace_blinds_injector.py`).	|| 
1240	H	L2179	[]	**Over-blocking breadth.** Refusing 3.11 moves every lab harness.	|| 
1241	H	L2179	[]	Count the committed preregs and harnesses affected, and check that every refusal message gives a working remedy.	|| 
